# Naavik Development Roadmap

> **Single source of truth for project progress.** Phases describe the long arc; per-phase wave/task tables are checked off as work lands. Tracking-only (per `AGENTS.md` § Single-doc-tracking).
>
> **Last updated:** 2026-05-22. (**0.7.0.43 SHIPPED via PR #209 squash `4596972`** — orchestrator strips inherited POSIX ACLs from `.naavik/db/` before PG starts. Owner-reported bug: PG14+ FATAL on inherited-default-ACL-induced 0770 mode. Round-1 strategy broke first-install bootstrap per architect HIGH (services-flake gates initdb on `! -d $PGDATA`); round-2 fold-in fix strips PARENT default ACL only (`setfacl -k .naavik`) + wraps child cleanup in `if [ -d .naavik/db ]` so first-boot lets services-flake's initdb create the dir cleanly under the de-ACL'd parent. RUNBOOK § 2.13 entry added. Empirically verified in /tmp sandbox. Reviewers round-2: hacker APPROVE (0 findings), architect APPROVE_WITH_NOTES (2 LOW non-blocking). **0.4.5 CYCLE ✅ COMPLETE** — all 14 in-cycle rows closed via plan 86 PR #207 + plan 87 PR #208. Plan 87 SHIPPED via PR #208 squash `5b2d351`: 0.4.5.03 conftest autouse → opt-in marker invert. 162 test files annotated with `pytestmark = pytest.mark.uses_sample_data_shims` across 10 batched commits + `_SERVICE_DIRECT_TEST_MODULES` allow-list deleted + marker registered in `pyproject.toml` + contract test pins inversion + negative test verified. Engineer D1 deviation kept `autouse=True` with marker-gated body early-return (same semantics, lower boilerplate). Tests 2216 → 2218 (+2). Reviewers: hacker APPROVE (0 findings); architect APPROVE_WITH_NOTES (3 LOW cosmetic; cycle-close GO). 14 in-cycle rows shipped total: 0.4.5.01/02/03/06/07/08/09/10/11/12/13/14/15/16 + decomposed 0.4.5.04 + moot 0.4.5.05. Future sub-rows 0.4.5.04a-d (per-adapter Playwright; multi-week each) remain `[ ]` for future plans. 9 deferred follow-ups filed (0.4.5.01a/02a/08c/16a-d/17/18). Earlier in cycle: **Plan 86 SHIPPED via PR #207 squash `d909702`**: 13 in-cycle rows closed (0.4.5.01/02/06/07/08/09/10/11/12 + R1/R2/R3/R5 round-2 fold-in rows 0.4.5.13/14/15/16) + 0.4.5.04 DECOMPOSED into 04a-d per-adapter sub-rows + 0.4.5.05 CLOSED-AS-MOOT (Google Sheets — n8n closeout). 2 review rounds: round 1 hacker APPROVE_WITH_NOTES (1 MED + 1 LOW + 1 INFO; deferred); architect APPROVE_WITH_NOTES (4 HIGH + 2 MED + 1 LOW + 2 INFO). Manager autonomous REQUEST_CHANGES on 3 HIGH user-surface gaps + 1 MED plan-spec divergence per user standing directive "dont stop until all of 0.4.5 is done." Round 2 fold-in 5 commits c470fa5 → 1745e8b: hacker APPROVE_WITH_NOTES (2 new MED — cost-cap placeholder propagation gaps; non-blocking), architect APPROVE_WITH_NOTES (3 LOW + 1 INFO; all R1-R5 closed + R4 process-honesty satisfied via verified QA screenshot md5 `f0ce5c3f187f03288c45a07eb7722eda`). Tests 2182 → 2216 (+34 net new). New on-disk surface: ApiUsage rows with `error_kind='cost_cap_slot_placeholder'` (atomicity guard during LLM call windows; NULL-safe-excluded from `today_cost_usd` widget). 9 deferred follow-ups filed: 0.4.5.01a (advisory-lock upgrade), 0.4.5.02a (cache breakpoint wiring), 0.4.5.08c (bullet ownership join), 0.4.5.16a-d (placeholder propagation to recent_usage/usage_summary + dg.is_cost_capped + orchestrator + orphan cleanup cron), 0.4.5.17/18 (deviation log + DATA_MODEL.md submission_artifacts graduation). **Plan 87 (0.4.5.03 conftest-marker-invert) NEXT** (separate PR for ~138-file reviewer-tractability). Earlier: **0.4.0 CYCLE ✅ COMPLETE — all 7 plans (76-82) shipped via PRs #191/#192/#193/#194/#195/#206 + plan 82 manager-direct ROADMAP closer.** Plan 81 / `0.4.0.07+10+12+16` SHIPPED via PR #206 squash `1569153` — application detail polish + postmortem modal + AppEvent timeline view + KPI analytics dashboard. Tests 2143 → **2182** (+39 net new). 4 distinct md5 manual QA screenshots. Reviewers: hacker APPROVE_WITH_NOTES (2 LOW; net security IMPROVES — `<pre>` swap kills XSS surface); architect APPROVE_WITH_NOTES (3 findings; 7 deviations all ACCEPTED). 0.4.0 cycle close: 0.4.0.01 + 0.4.0.02 [x] close-as-shipped (substrate Wave 5 / plan 56); 0.4.0.05/06/18 + 3 review-derived follow-ups + 1 stale-fixture + 2 hardening rows + 1 catalog reconciliation MOVED to 0.4.5 (rows 0.4.5.04-12). **6 cycle PRs merged this session** (#194/#195/#198/#200/#204/#205/#206). Earlier: 0.7.0.39 fresh-install signup UX cluster SHIPPED via PR #205; 0.3.3 phase closed (21/24 rows); plan 85 housekeeping batch SHIPPED via PR #204. 3 bugs in the user-visible signup flow plan 83+84 was supposed to deliver: HX-Redirect URL typo (`/auth/login` → `/login`, plan-75 regression); browser-direct unauth got bare 401 instead of redirect (new 307 + `Location: /login`); HTMX 4xx not swapped on error card (new `hx-target-error="#login-card"` form attribute). Tests 2143 → 2143 (1 net; manual QA 4 distinct md5 screenshots verified end-to-end signup → onboarding). Reviewers: hacker APPROVE_WITH_NOTES (0 actionable; 4 INFO); architect APPROVE_WITH_NOTES (1 LOW + 3 INFO; D.1/D.2/D.3 PASS). New follow-ups filed: 0.7.0.41 (MED — parallel-dispatch worktree isolation convention); 0.7.0.42 (LOW — Playwright email-fixture chr-concat pattern doc). Earlier: plan 85 / housekeeping batch SHIPPED via PR #204 squash `be07069` — 3-cluster fix (0.3.3.24 + 0.4.0.21 + 0.4.0.23) closes 0.3.3 phase ✅ (21 of 24 rows shipped; 3 MOVED to 0.4.5). Earlier: plan 84 / `0.7.0.37` SHIPPED via PR #200 squash `cb60020` — alembic 0001 `create_all` race fixed; owner-visible signup flow VERIFIED end-to-end. Closes the chain that started with plan 83. `0001_initial.py` 48 → 390 LOC via synthetic-MetaData helper + documented delta map; chain-replay test verifies fresh-install schema equivalence under `NAAVIK_LIVE_DB=1`. Pre-existing VARCHAR(32) alembic_version overflow ALSO fixed via env.py fold-in. Tests 2141 → 2142. **Manual QA: `rm -rf .naavik/` + `nix run .#dev` → /login → signup → onboarding round-trip with 4 screenshots** — the user-visible flow plan 83 wired but plan 84 unblocks. New 0.7.0.38 MEDIUM follow-up filed (Issue #201) for renaming 4 over-32-char revision IDs so existing operators on VARCHAR(32) can upgrade. Reviewers: hacker APPROVE_WITH_NOTES (delta map exhaustive verified; no security regression); architect APPROVE_WITH_NOTES (D.1+D.2+D.3 PASS; synthetic-helper + VARCHAR(64) fold-in both ACCEPT; 1 MED + 1 LOW filed). Earlier: plan 83 / `0.7.0.36` SHIPPED via PR #198 squash `3255caf` — auto-seed dev user deleted; first-time signup is the canonical first-run flow. Owner-directive out-of-cycle interrupt during 0.4.0. `src/db/seed.py` + `tests/test_seed.py` + `~/.naavik/dev-credentials` + `NAAVIK_DEV_PASSWORD` env + lifespan echo + WARN block all DELETED. `flake.nix` orchestrator collapses to `db-init → db → migrate → app`. `services/first_run.py` collapses to 1-signal probe. New regression-lint guards `from db.seed import *`. Tests 2148 → 2141. Production self-hosters unaffected (never wrote the file). **HARD GAP surfaced by manual QA:** fresh-install (`rm -rf .naavik/` + `nix run .#dev`) is BLOCKED on a pre-existing alembic race — `0001_initial`'s `SQLModel.metadata.create_all(checkfirst=False)` snapshots CURRENT model state (creates every column on every later migration's add target), then `0002_settings_multi_users` `op.add_column` already-extant column → `DuplicateColumnError`. Architect filed `0.7.0.37` HIGH / Issue #199 for the race; **the owner-visible signup flow doesn't actually work end-to-end on fresh install until 0.7.0.37 ships**. Reviewers: hacker APPROVE_WITH_NOTES (1 MED severable; security posture IMPROVES — removes plaintext-credential-on-disk surface); architect APPROVE_WITH_NOTES (4/4 spec match D.1+D.2+D.3 PASS; D.4 PIVOT to TestClient + Playwright file://; filed Issue #199). Earlier today: plan 80 / `0.4.0.09` bulk actions SHIPPED via PR #195 squash `187e9e8`; plan 79 / `0.4.0.11` retry-failed SHIPPED via PR #194 squash `7fa4b6b`. **2 0.4.0 plans remaining (81 detail polish + postmortem UI + timeline + analytics; 82 bookkeeping closer) PLUS new HIGH 0.7.0.37 alembic blocker that gates the user-visible signup flow.**)

---

## Index

| Section | Purpose |
|---|---|
| [Maintenance](#maintenance) | Rules for editing this doc; cross-refs to material that USED to live here |
| [Priority Queue](#priority-queue-derived-view--post-a29) | Sorting recipe; how `naavik-ops task next-unblocked` derives "what's next" |
| [Phases](#phases) | The 7 release-version sections (0.1.0 → 0.7.0), each with a task table |
| [Backlog (unprioritized)](#backlog-unprioritized) | Future-eligible work not on any release; parking lot |
| [Agent System (mirror conventions)](#agent-system-mirror-conventions) | How ROADMAP maps to the GitHub Project v2 board |

### Phase status (one-glance)

| Phase | Goal | Status | Shipped |
|---|---|---|---|
| **0.1.0** | Foundation + MVP (11 screens + backend + 6-subagent system + A.29 numbering migration) | ✅ Complete | 2026-05-18 |
| **0.1.1** | Legacy bash → Python rewrite + CHANGELOG sanitization | ✅ Complete | 2026-05-19 |
| **0.2.0** | Job Scraping & Discovery (vault sunset + 6 site scrapers + AI extraction + dedup + scheduler + UI + notifications + rate-limiting + release ceremony) | ✅ Complete (2026-05-20) | All 14 core rows + all 6 follow-up letter rows shipped; 0.2.0.14 closed-as-moot; 0.2.0.15 subsumed by 0.2.6 tag-cut; 0.2.0.17 observation window IN-PROGRESS (post-tag-cut) |
| 0.2.1 | Security cleanup (DEF) | ✅ Complete (2026-05-20) | 0.2.1.04 + 0.2.1.05 shipped; 0.2.1.01 moved to 0.2.7.07; 0.2.1.02 moved to 0.8.0.01 |
| 0.2.2 | UI cleanup (DEF) | ✅ Complete (2026-05-20) | 0.2.2.02/03/04 shipped; 0.2.2.01 light mode moved to 0.8.0.02 |
| 0.2.3 | ATS / scraper cleanup (DEF) | ✅ Complete (2026-05-20) | 0.2.3.02 shipped; 0.2.3.01 moved to 0.2.7.10; 0.2.3.03 moved to 0.2.7.11 |
| 0.2.4 | Tracking cleanup (DEF) | ✅ Complete (2026-05-20) | 0.2.4.01-04 shipped; 0.2.4.05 moved to 0.2.7.12 |
| 0.2.5 | Observability cleanup (DEF) | ✅ Complete (2026-05-20) | 0.2.5.02/03/04 shipped; 0.2.5.01 closed moot; 0.2.5.05+06 moved to 0.2.7.06 (paired UI editors) |
| 0.2.6 | Tooling cleanup (DEF + A.28a) | ✅ Complete (2026-05-20) | 0.2.6.06/07/08/09 shipped; 0.2.6.01-05 moved to 0.2.7.13-17 |
| 0.2.7 | Deferred-from-0.2.x catch-all | ✅ Complete (2026-05-21) | All in-cycle rows shipped (incl. design-heavy sweep PRs #162/#163/#164/#165); 6 small follow-ups deferred to 0.8.0.33-38 |
| 0.3.0 | Scoring substrate (hybrid layered: tag → semantic → LLM-judge) | ✅ Complete (2026-05-21) | All 6 rows shipped via PR #166; 4 follow-ups filed to 0.3.3 |
| 0.3.1 | Tailored resume + cover letter FREE-tier (voice grounding + ATS-friendly Typst + adaptive cover) | ✅ Complete (2026-05-21) | All 6 rows shipped via PR #167; 5 follow-ups filed to 0.3.3 |
| 0.3.2 | UI polish + score analytics + 8-bug regression sweep | ✅ Complete (2026-05-21) | All 4 polish rows + 3 HIGH bug-fix rows (0.3.3.12-14) shipped via PR #169 |
| 0.3.3 | Deferred-from-0.3.x catch-all | ✅ Closed (2026-05-21) | 24 rows total; 21 closed (PRs #169 + #170 + #192 + #204); 3 MOVED to 0.4.5 (0.3.3.04/09/16) |
| 0.3.4 | PREMIUM-tier Claude-mythos generation (adversarial detector loop + 3-agent council + multi-persona critique + tool-loop + parser ensemble) | ✅ Complete (2026-05-21) | All 6 rows shipped via PR #168; 2 follow-ups filed to 0.3.3.10-11 |
| 0.4.0 | Application Tracking & Auto-Apply | ✅ Complete (2026-05-21) | 7 plans shipped (76/77/78/79/80/81/82); all in-cycle rows closed; 4 rows MOVED to 0.4.5 (0.4.0.05/06/18 + 3 follow-ups); 0.4.0.19 stays parked at 0.5.0 |
| **0.4.5** | Deferred-from-0.3.x/0.4.x catch-all (cycle parking lot) | ✅ Complete (2026-05-22) | All 14 in-cycle rows closed via plan 86 PR #207 + plan 87 PR #208 (13 shipped + 0.4.5.04 decomposed into 04a-d future sub-rows + 0.4.5.05 moot). 9 deferred follow-ups filed (0.4.5.01a/02a/08c/16a-d/17/18). Future sub-rows 0.4.5.04a-d remain `[ ]` per per-adapter multi-week scope. |
| 0.5.0 | Email Monitoring & Outreach | ⚪ Future | — |
| 0.6.0 | Optimization & Polish (observability, LaTeX, ML calibration — light mode moved to 0.8.0) | ⚪ Future | — |
| 0.7.0 | Agent-system follow-ups (formerly Phase 2.5) | 🟢 Active (pickup queue) | rolling |
| **0.8.0** | Future enhancements — long-tail product capabilities not blocking near-term roadmap (OIDC self-hosted + light mode + others as filed) | ⚪ Future (low priority) | — |

### Active conventions

- **CLI sunset:** do NOT extend `src/cli/` or vault. Phase 2 tasks 0.2.0.01 + 0.2.0.02 delete them. (AGENTS.md § Key Conventions § CLI.)
- **Single-doc-tracking:** all task ledger lives in `ROADMAP.md` only. Plans describe the work; ROADMAP records completion.
- **Deviations gate:** every plan in `docs/plans/` MUST have a `## Deviations from plan` section before archive. Enforced by `naavik-ops plan archive` (post-0.7.0.21).
- **GitHub Mirror:** ROADMAP → Project v2 is one-way. Manager syncs FROM ROADMAP TO Project, never reverse. (`docs/AGENT_OPS.md § 6`.)
- **Parallel reviewer invariant:** PR_REVIEW_GATE reviewer dispatches (hacker + architect) MUST land in a single assistant tool-use response. (`.claude/agents/manager.md § Parallel reviewer invariant`.)
- **Backlog vs Todo:** `## Backlog (unprioritized)` section holds deferred work; release-section rows are active cycle (see `## Backlog` below).
- **Visual contract frozen:** Inter + JetBrains Mono + Lucide (stroke 1.5) + indigo/cyan + dark mode primary. (`DESIGN.md`.)

---


## Maintenance

This doc is **tracking-only** (per AGENTS.md § Single-doc-tracking). The maintenance rules live in **`AGENTS.md` § Roadmap Maintenance Rules** — read once, follow always. Quick summary:

- `[ ]` → `[~]` on start. `[~]` → `[x]` + deliverable note on completion.
- Edit the table directly when scope changes — don't bury in commits.
- Bump "Last updated" on every meaningful edit.
- **GitHub Project mirror:** the agent system mirrors this onto GitHub Project #4. See § Agent System (mirror conventions) below. Sync via `.claude/naavik-ops gh sync --apply` after ROADMAP edits.

Pointers for content that USED to be in this doc (carved 2026-05-16 to keep ROADMAP tracking-only):

| Was here | Now lives in |
|---|---|
| Vision + Competitive Context | `README.md` § What is Naavik? + § Why Naavik? |
| Architecture diagram + Tech Stack table + Design decisions | `docs/ARCHITECTURE.md` |
| Data Model sketches | `docs/design/DATA_MODEL.md` (canonical) |
| Repository Structure tree | `docs/ARCHITECTURE.md` § 3 layer responsibilities |
| Deployment (4 paths) | `docs/DEPLOYMENT.md` |
| n8n Migration Strategy | `docs/ARCHITECTURE.md` § 4.7 External integrations |
| Portfolio Integration | `docs/ARCHITECTURE.md` § 4.7 External integrations |
| UI Screens & Design narrative | `docs/design/SCREENS.md` + `DESIGN.md` + `docs/design/WORKFLOW.md` |


---

## Priority Queue (derived view — post-A.29)

**Replaced by `.claude/naavik-ops task next-unblocked 0.2.0` derived view** per A.29 D.13 caller-rewrite scope. The canonical "what should I work on next" surface is now release-version-grouped (`0.2.0` is the active release; `0.1.0` shipped; `0.1.1` queued; `0.2.X` thematic patches interleave with `0.2.0`).

Sort key per `docs/design/PHASE_NUMBERING.md` § 1: **release-version ASC → priority DESC (HIGH > MED > LOW > unset) → position ASC**, gated by deps. The two HIGH markers under `0.2.0` are `0.2.0.01` (vault deprecation, Tier-2 wave 1) and `0.2.0.05` (SQLModel Job models, hard dep for every scraper). All other `0.2.0.NN` tasks are equally required for the `0.2.0` cut.

**Maintenance:** drift-free by construction — `naavik-ops task next-unblocked <version>` reads `.claude/github-issue-map.json:issues` + `:priorities` + `:deps` and emits the sorted view on demand. Manager invokes it at every gate transition; `/standup` consumes it. The canonical source remains the per-release tables below.

---

## Phases

### 0.1.0 — Foundation + MVP + Pre-Phase-2 paper cuts + Phase A bootstrap (legacy Phase 0 + Phase 1 + PC + A.1–A.29)
> **Goal:** First full Naavik bundle — reproducible dev environment + 11-screen MVP UI + backend substrate + 6-subagent system + this A.29 phase-numbering migration.
> **Status:** ✅ Complete (cut as `0.1.0` tag during Wave 5 of A.29 PR).
> **Shipped:** 2026-05-18 (A.29 migration apply; legacy phases shipped 2026-04-25 through 2026-05-18).
> **Plan archive:** `docs/plans/archive/01-…` through `docs/plans/archive/24-…`.
> **Position cap rule:** D.1 Option B collapse — Phase 1's 5 Waves fold to 14 deliverable-level positions; sub-task detail preserved in `docs/plans/archive/10-backend-impl.md` and Notes-column prose. 48 of 99 slots used; 2 reserved (0.1.0.48–0.1.0.49) for future micro-fixes before tag cut.

| # | Task | Status | Priority | Legacy ID | Notes |
|---|---|---|---|---|---|
| 0.1.0.01 | Nix flake: devShell with Python 3.12, uv, typst, postgresql, ruff, pre-commit hooks | [x] | — | 0.1 | `nix/devshell.nix` — verified all tools available in `nix develop` |
| 0.1.0.02 | pyproject.toml + uv lockfile with all Python deps | [x] | — | 0.2 | 56 packages installed via `uv sync` |
| 0.1.0.03 | Dockerfile (multi-stage, uv-based, Python 3.12 slim) | [x] | — | 0.3 | Builder + runtime stages, typst in runtime |
| 0.1.0.04 | Docker Compose: FastAPI app + PostgreSQL (pgvector/pgvector:pg17) | [x] | — | 0.4 | Health check on db, app depends on healthy db |
| 0.1.0.05 | NixOS service module (`nix/module.nix`) — Lumino-compatible | [x] | — | 0.5 | Reads `settings.servicesConfig.apps.tools.naavik`, full systemd hardening, SOPS, Traefik, PostgreSQL ensure |
| 0.1.0.06 | Nix package derivation (`nix/package.nix`) | [x] | — | 0.6 | `nix build` produces `result/bin/naavik`, typst wrapped in PATH |
| 0.1.0.07 | FastAPI app skeleton — main.py + config.py + db/session.py + static files + Jinja2 templates | [x] | — | 0.7 | Sidebar drawer layout (Tailwind + DaisyUI + HTMX), dashboard placeholder, 5 nav stubs |
| 0.1.0.08 | Alembic setup (async, reads DATABASE_URL from settings) | [x] | — | 0.8 | `migrations/env.py` uses async_engine_from_config |
| 0.1.0.09 | .env.example with all env vars documented | [x] | — | 0.9 | DATABASE_URL, SECRET_KEY, all LLM keys, OAuth, integrations |
| 0.1.0.10 | Phase 1 Wave 1 — 5 design docs graduated (COMPONENTS + BACKEND + DATA_MODEL + INTERACTIONS + SAMPLE_DATA) | [x] | — | Phase 1 Wave 1 | Plans 03–07 GRADUATED + archived; ~5300 lines of canonical contract; DESIGN.md bumped v1.3 (DRAFT row added) |
| 0.1.0.11 | Phase 1 Wave 2 — 85-component partial library + base.html + macros + base.js + `/_design/components` fixture page | [x] | — | Phase 1 Wave 2 | All 100 tests pass; `docs/plans/archive/08-stage-2-impl.md` |
| 0.1.0.12 | Phase 1 Wave 3 — 11 page templates + sample_data accessors + stub fragment/JSON endpoints + Discover keyboard map + Playwright snapshots | [x] | — | Phase 1 Wave 3 | All screens render at desktop + mobile; `docs/plans/archive/09-stage-3-impl.md` |
| 0.1.0.13 | Phase 1 Wave 3a — Stage 3 bugfix + Discover redesign triage (Lucide diagnostics, sidebar mobile drawer, typed application questions, scroll-spy, native dialog backdrop, touch swipe) | [x] | — | Phase 1 Wave 3a | `docs/plans/archive/09a-stage-3-bugfix.md` |
| 0.1.0.14 | Phase 1 Wave 4 — 20 SQLModel entities + Alembic 0001_initial + pgvector extension + 25 model tests | [x] | — | Phase 1 Wave 4 (substrate) | `docs/plans/archive/10-backend-impl.md` § B |
| 0.1.0.15 | Phase 1 Wave 4 — Auth (bcrypt + JWT + CSRF + rate limit) + AES-256-GCM vault + key rotation CLI + Settings · Deployment vault-locked banner | [x] | — | Phase 1 Wave 4 (auth+vault) | 18 auth tests + 22 vault tests; cost=12 prod / cost=4 tests; HS256 JWT; HttpOnly+Secure+SameSite=Strict cookies |
| 0.1.0.16 | Phase 1 Wave 4 — LLM provider abstraction (Anthropic + OpenAI + Ollama) + structured-output retry policy + 10 prompt skeletons + `ApiUsage` cost tracker | [x] | — | Phase 1 Wave 4 (LLM) | Anthropic tool-use / OpenAI json_schema / Ollama JSON mode; `tracked_call` wraps every provider call; 15 tests |
| 0.1.0.17 | Phase 1 Wave 4 — Profile + Settings services + per-field autosave + `NAAVIK_PERSISTENCE=db` DB-backed accessor swap (12 high-traffic accessors) | [x] | — | Phase 1 Wave 4 (services+swap) | DB-backed CRUD per tab; PUT /api/v1/settings/llm flows API key through vault |
| 0.1.0.18 | Phase 1 Wave 4 — `db/seed.py` consuming `db/sample_data.py` (idempotent ON CONFLICT DO NOTHING) + `ats_credentials` service | [x] | — | Phase 1 Wave 4 (seed) | 372 rows seeded across 20 entities; bumps every PK sequence after seed; CLI `uv run python -m db.seed` |
| 0.1.0.19 | Phase 1 Wave 5 — `extraction` service (PDF → AI → Profile + SSE event emission) | [x] | — | Phase 1 Wave 5 (extraction) | `services/extraction.py` — PDF parse via pypdf + LLM `extract_resume` + SSE generator |
| 0.1.0.20 | Phase 1 Wave 5 — `document_generator` (bullet selection + AI trim + Typst compile + native page-count validation + DRAFT reuse heuristic) + Typst templates (`onepage.typ`, `cover_letter.typ`) + native page-count validator | [x] | — | Phase 1 Wave 5 (doc-gen) | NEU-style 1-page resume + 4-section letter; both with `<naavik-meta>` page-count label; `typst query <input.typ>` (the spec'd `--emit metadata` flag doesn't exist in 0.14) |
| 0.1.0.21 | Phase 1 Wave 5 — `application_service` (DRAFT lifecycle + submit/discard + ATS dispatch + `validate_submittable` + `process_auto_apply_queue`) + multi-axis state derivation | [x] | — | Phase 1 Wave 5 (application) | Full DRAFT lifecycle + computed-state ownership + 25 unit tests |
| 0.1.0.22 | Phase 1 Wave 5 — `notifications` (Discord webhook + Telegram outbound + in-app toast routing) + `portfolio_sync` (public CV API + filtered + debounced regen + Netlify webhook) | [x] | — | Phase 1 Wave 5 (notif+portfolio) | Discord embed + Telegram + toast queue; Allowlist-based portfolio filter + post-condition assert + 30 tests |
| 0.1.0.23 | Phase 1 Wave 5 — ATS adapters (Greenhouse + Lever + Ashby; Workday / LinkedIn / Indeed / Generic deferred to 0.2.3) + APScheduler cron registration (auto_apply / aggregate_costs / cleanup_stale_docs / daily_db_snapshot / refresh_oauth_tokens) | [x] | — | Phase 1 Wave 5 (ATS+cron) | All 3 adapters with public APIs + factory + 18 mock-HTTP tests; APScheduler with PostgresJobStore lifespan-managed |
| 0.1.0.24 | Pre-Phase-2 paper cut — Process-compose: confirm app logs + cold-start reliability | [x] | — | PC.1 | **Plan 10a (2026-05-02 + orphan-fix 2026-05-03):** `setsid -w` + `coreutils` + `pkill` cleanup; root cause was TTY/SIGTTIN not alembic async wedge |
| 0.1.0.25 | Pre-Phase-2 paper cut — `uv run fastapi dev` (no path) should just work | [x] | — | PC.2 | **Plan 10a:** 2-line `app.py` shim at repo root re-exports `src.main:app` |
| 0.1.0.26 | Pre-Phase-2 paper cut — Playwright local capture on NixOS | [x] | — | PC.3 | **Plan 10a:** `nodejs_22` + `PLAYWRIGHT_NODEJS_PATH`; Playwright pinned `>=1.58.0,<1.59`; 20 baseline PNGs committed |
| 0.1.0.27 | Pre-Phase-2 paper cut — Phase-1 finalization (orchestrator greenlet/libstdc++ + NAAVIK_PERSISTENCE=db default + working dev credential + signup endpoint + `naavik` CLI subcommands + Settings · LLM Provider form-wiring + README rewrite) | [x] | — | PC.4 | **Plan 10b EXECUTED 2026-05-03** — 9 paper cuts shipped; 475 tests pass (~12 new); ruff clean |
| 0.1.0.28 | Pre-Phase-2 paper cut — `SECRET_KEY` boot-time enforcement | [x] | — | PC.5 | **Plan 17 EXECUTED 2026-05-16 via PR #49** — `Settings._enforce_secret_key` validator at `src/config.py:45-66` refuses module import when `SECRET_KEY` is shipped default or <32 bytes unless `NAAVIK_DEBUG=1` |
| 0.1.0.29 | Pre-Phase-2 paper cut — Password complexity (min 12 chars, digit + letter) + must-change-on-first-login flag | [x] | — | PC.6 | **Plan 18 EXECUTED 2026-05-17 via PR #50** — `validate_password_complexity` + `User.must_change_password` (alembic `0003`) + `require_password_complete` dep + `POST /api/v1/auth/change-password` + cookie/CSRF rotation + dev-credential file cleanup on rotation |
| 0.1.0.30 | Pre-Phase-2 paper cut — First-time setup ergonomics (`NAAVIK_PERSISTENCE=db` parity + login signup-link promotion + signup-disabled banner + persisted `~/.naavik/dev-credentials` + lifespan credential echo) | [x] | — | PC.7 | **Plan 10c EXECUTED 2026-05-12** — three sub-items shipped; 478 tests pass (3 new pages tests + 2 new live-DB seed tests); new config field `Settings.debug` |
| 0.1.0.31 | Phase A — 6 subagent prompts (`.claude/agents/`) | [x] | — | A.1 | Frontmatter (name/description/tools/model/color) + body (principles, operating loop, tracing format, escalation); models: manager+architect+hacker on opus-4-7; engineer+devops+designer on sonnet-4-6 |
| 0.1.0.32 | Phase A — 13 slash commands (`.claude/commands/`) | [x] | — | A.2 | `/build`, `/plan`, `/discuss`, `/triage-bug`, `/review-pr`, `/threat-model`, `/design-screen`, `/groom`, `/standup`, `/bootstrap`, `/sync-roadmap`, `/budget`, `/runs` |
| 0.1.0.33 | Phase A — `.claude/naavik_ops/gh.py` Projects v2 helper | [x] | — | A.3 | `init` / `bootstrap` / `sync` / `milestone-status` / `add-item` / `create-issue` / `set-status` / `next-unblocked` / `runs`; idempotent; user-or-org auto-detected |
| 0.1.0.34 | Phase A — Token budget config + ledger | [x] | — | A.4 | `.claude/budget.json` (caps) + `.claude/budget-ledger.json` (manager-managed); halt at projected-spend-exceeds-cap; `/budget` to inspect |
| 0.1.0.35 | Phase A — Trace system + `watch.sh` (tmux) + `runs.log` index + `MANIFEST.json` per run | [x] | — | A.5 | Run-id format `YYYY-MM-DDTHH-MM-SS_<6hash>`; per-agent log format frozen in each agent's prompt; `/runs` to list history |
| 0.1.0.36 | Phase A — `docs/AGENT_OPS.md` — single operational guide | [x] | — | A.6 | Bootstrap → daily workflow → commands reference → agent reference → GitHub Mirror conventions → tracing → budget → troubleshooting → extending |
| 0.1.0.37 | Phase A — `.github/` templates (3 Issue forms + PR template) | [x] | — | A.7 | PR template aligns with AGENTS.md § Workflow step 7 (deviations summary required) |
| 0.1.0.38 | Phase A — First end-to-end `/build` shipping a real paper cut (satisfied via PC.5 PR #49) | [x] | — | A.8 | 5 skills invoked; git `prepare-commit-msg` hook auto-appended `Closes #7`; deviations promoted into plan 17; plan archived. Manager → architect → engineer → hacker → devops → ROADMAP update → Issue close → Project advance loop exercised |
| 0.1.0.39 | Phase A — Agent system v2 (cold-start hook + per-agent skill suite + git commit automation) | [x] | — | A.11 | **Plan 16 EXECUTED 2026-05-17** — `.claude/hooks/cold-start.sh` SessionStart + `.claude/skills/naavik-cold-start/SKILL.md` + Skill tool on all 6 agents + `.claude/hooks/git/prepare-commit-msg` auto-`Closes #N` + 29-skill per-agent suite |
| 0.1.0.40 | Phase A — Map cache + single-writer governance (gh-project.sh hardening) | [x] | — | A.12 | `find_issue_by_prefix` + `ensure_milestone` consult `.claude/github-issue-map.json` first; new `refresh-map` subcommand reconciles from authoritative GitHub state; duplicate epics #46/#47 closed; "GitHub state — single writer" rule codified |
| 0.1.0.41 | Phase A — Tracing contract ERROR + BUILT/REVIEWED event family + MANIFEST schema extensions | [x] | — | A.13 | `ERROR step=<what> kind=<retry\|skip\|halt\|pivot> reason='<line>' attempt=<n>/<max>` + `BUILT` / `REVIEWED` last-line summaries; MANIFEST gains `outcome` / `halt_reason` / `what_built` / `errors_encountered` |
| 0.1.0.42 | Phase A — Task Playbook (`docs/PLAYBOOK.md`) — strict 9-category if-then classification | [x] | — | A.14 | **Shipped 2026-05-17 via PR #51 squash `ab9f2589`** — STATUS / INSPECT / 3 gate responses / PRODUCT_WORK / BUG_TRIAGE / CONTRACT_CHANGE / BOOKKEEPING; default-deny allow-list-only for BOOKKEEPING |
| 0.1.0.43 | Phase A — Agent memory + learning system | [x] | — | A.15 | **Plan 19 EXECUTED 2026-05-17 via PR #53** — `.claude/memory/` substrate (6 stores) + single-writer `.claude/naavik_ops/memory.py` + 4 memory-aware skills + 2 slash commands (`/memory`, `/learn`); 50/50 smoke pass |
| 0.1.0.44 | Phase A — Machine-readable wording rewrite of agent-system files | [x] | — | A.16 | **Plan 22 EXECUTED 2026-05-18 via PR #68** — 57 files rewritten; -9.7% / ~-8.5k tokens per ingest cycle |
| 0.1.0.45 | Phase A — `agent-memory.sh` hardening (flock + jq sandbox + alias regex + manifest escape) | [x] | — | A.17 | **Plan 21 EXECUTED 2026-05-18 via PR #66** — 5 hacker findings closed; 30/30 concurrent writes persist (was 13/30 pre-fix); 14 new test assertions |
| 0.1.0.46 | Phase A — `agent-memory.sh` alias regex widening to include uppercase tokens | [x] | — | A.17a | **EXECUTED 2026-05-18 via PR #68 (A.16 fold-in)** — `ALIASES_RE` widened at `.claude/naavik-ops memory:119`; uppercase + `#` accepted; newline + `---` injection still blocked |
| 0.1.0.47 | Phase A — Board restructure (Backlog status + Phase 2.5 milestone + Priority Queue section) | [x] | — | A.28 | **Plan 20 EXECUTED 2026-05-17 via PR #63** — 5 waves; `Backlog` status added to Project v2; `Phase 2.5` milestone + epic created; ROADMAP surgery; `manager-backlog-promote` skill |
| 0.1.0.48 | (reserved buffer slot) | — | — | — | Reserved for post-A.29 micro-fixes before 0.1.0 tag cut |
| 0.1.0.49 | (reserved buffer slot) | — | — | — | Reserved for post-A.29 micro-fixes before 0.1.0 tag cut |
| 0.1.0.50 | Phase numbering system + `.claude/naavik-ops` Python dispatcher — 4-level semver task IDs replace Project Priority field; full retroactive renumber + release ceremony tooling | [x] | HIGH | A.29 | **Plan 24 EXECUTED 2026-05-18 via PR #73 squash `c5ac58f`** — Waves 1-4 shipped; Wave 5 (migration `--apply` + `0.1.0` release cut) runs post-merge per this diff patch + `step_7_rewrite_roadmap()` in `.claude/migrations/A.29-phase-renumber.py`. **This row is the closer for 0.1.0** — running `.claude/naavik-ops release cut 0.1.0` after Wave 5 cuts the first `0.1.0` tag |

**Deliverable (0.1.0):** Reproducible dev environment + 11-screen MVP + 20-SQLModel-entity backend substrate + 3 ATS adapters + Typst PDF generation + 6-subagent system + agent memory substrate + machine-readable rewrite + board restructure + this A.29 phase-numbering migration. Tag cuts in Wave 5 via `.claude/naavik-ops release cut 0.1.0`. Closes `Phase A` + `Pre-Phase-2 paper cuts` + `Phase 1 deferred items` (whose rows distribute into `0.2.X` thematic patches) + `Phase 2.5` (whose rows defer to `0.7.0`) GitHub Milestones.

**Verification log (Wave 5 post-merge):**
- Migration script applies cleanly: `python .claude/migrations/A.29-phase-renumber.py --apply` exits 0.
- `.claude/naavik-ops task check` exits 0 (no ROADMAP-vs-Issue-title drift; pyproject.toml + nix/package.nix at `0.1.0`; latest tag `0.1.0`).
- `git tag --list 0.1.0` returns `0.1.0` (cut via `.claude/naavik-ops release cut 0.1.0`).
- All 50 ROADMAP-rows-becoming-`0.1.0.NN` have Issue titles rewritten from `[A.X]` / `[PC.X]` to `[0.1.0.NN]` per the migration runbook step 6.
- `.claude/github-issue-map.json:redirects` populated with every legacy ID → new ID mapping.

---

### 0.1.1 — Legacy bash → Python rewrite + CHANGELOG sanitization (legacy A.30 + A.31)
> **Goal:** Replace legacy bash (`scripts/gh-project.sh` 1469 LOC + `scripts/agent-memory.sh` 843 LOC + `scripts/roadmap_parser.py` 304 LOC) with native Python under `.claude/naavik_ops/`; harden CHANGELOG.md markdown escaping before future closed-Issue ingestion lands.
> **Status:** ✅ Complete (2026-05-19). Shipped via PR #91 squash `494ffae`; tag `0.1.1` + GH release published at https://github.com/crizzy9/naavik/releases/tag/0.1.1.
> **Plan:** `docs/plans/archive/25-0.1.1-bash-to-python.md`.

| # | Task | Status | Priority | Legacy ID | Notes |
|---|---|---|---|---|---|
| 0.1.1.01 | Python rewrite of legacy bash scripts (`gh-project.sh` 1469 LOC + `agent-memory.sh` 843 LOC + `roadmap_parser.py`) into `.claude/naavik_ops/` Python modules + full `scripts/` → `.claude/` migration | [x] | MEDIUM | A.30 | **EXECUTED 2026-05-19 via PR #91 squash `494ffae`** (closed Issue #72). 7 commits W0-W6: W0 `.gitignore` + plan 25 + fixture dir; W1 `lib/changelog.py` sanitize + `lib/roadmap.py` writer-half inline; W2 native `gh.py` rewrite (20 callable subcommands + 1 helper `get_issue()`); W3 native `memory.py` (A.17 jq sandbox byte-for-byte) + 5 mutating `task` ops (atomic 3-store mutation + rollback under `~/.naavik/naavik-ops.lock`); W4 ~34 caller rewrites + skill frontmatter cleanup; W5 delete `scripts/{gh-project.sh,agent-memory.sh,roadmap_parser.py}` + `tests/test_agent_memory.sh` + `tests/test_naavik_ops/test_{gh,memory}_wrapper.py` (-2616 LOC); W6 PR_REVIEW_GATE reviewer pairing `hacker + devops` → `hacker + architect` (folded-in contract refactor; new § PR review mode in `.claude/agents/architect.md`). Hacker: APPROVE_WITH_NOTES (2 LOW + 1 INFO, none exploitable; R11 jq sandbox + R12 close-issue argv validated). Devops: PASS 12/12 gates (zero regressions; 210 tests passing in `tests/test_naavik_ops/`). 2 engineer deviations promoted into archived plan (W3 column-order swap + W3 collision guard). Closes A.29 Deviation 1 (mutating subcommands stubbed). |
| 0.1.1.02 | CHANGELOG.md markdown sanitization — escape commit-msg bodies before `.claude/naavik_ops/lib/changelog.py:62` renders to released artifact | [x] | LOW | A.31 | **EXECUTED 2026-05-19 via PR #91 W1 commit `8657655`** (closed Issue #74). `ReleaseEntry.__post_init__` escapes CommonMark special chars + collapses whitespace + rejects CR; `parse_changelog` round-trip avoids double-escape via `ReleaseEntry.from_rendered`. 7 sanitize tests in `tests/test_naavik_ops/test_changelog.py`. Hacker LOW finding from PR #75 review: CommonMark escape regex covers 19/30 spec chars (missing `~`/`=`/`&`); setext-h2 (`---`/`===`) gap defused by whitespace collapse — documented as design intent, no fix needed. |
| 0.1.1.03 | Close stale pre-A.29 epic Issues using new `close-issue` helper | [x] | LOW | — | **EXECUTED 2026-05-19 post-merge bookkeeping** (closed Issue #90). 8 stale epics closed via `naavik-ops gh close-issue <N> --reason completed`: `#1 [Epic] Phase A`, `#6 [Epic] Pre-Phase-2 paper cuts`, `#9 [Epic] Phase 2`, `#22 [Epic] Phase 1 deferred items`, `#65 [Epic] Phase 2.5`, `#76 [Epic] 0.1.0` (release shipped 2026-05-18), `#77 [Epic] 0.1.1` (release shipped 2026-05-19). Permission system blocked raw `gh issue close` at PR #75 (single-writer rule); the new dispatcher subcommand from 0.1.1.01 W2 closes the architectural gap. |

**Deliverable (0.1.1):** Legacy bash scripts deleted; `.claude/naavik_ops/` becomes the sole agent-system tooling surface; `scripts/` reserved for project-wide use only; CHANGELOG.md safe against markdown injection.

---

### 0.2.0 — Job Scraping & Discovery (legacy Phase 2)
> **Goal:** Automated multi-source job discovery with AI extraction; vault deprecation + CLI sunset.
> **Status:** 🟡 Queued (post-0.1.0 tag cut).
> **Plan:** `docs/plans/11-phase-2-scrapers.md` (to be authored after Phase 1 ships). Splits cleanly into 11a (LinkedIn + Greenhouse + Lever + Ashby) and 11b (Workday + Indeed + Generic + n8n migration).
> **Implementation contract:** `docs/design/BACKEND.md` § J (scraping architecture), § I (cron catalog), § K (auto-apply pipeline). Wave 6 services + Phase 2 sub-prompts of plan 10.
> **Estimated effort:** 2–3 weeks.

| # | Task | Status | Priority | Legacy ID | Notes |
|---|---|---|---|---|---|
| 0.2.0.01 | **Vault deprecation → env-only secrets.** Delete `src/services/vault.py` (AES-256-GCM + PBKDF2 + lockfile + audit log) and the encrypted vault entirely. Switch to standard self-hosted-app pattern: secrets via env vars sourced from a gitignored `.env` (loaded by `nix develop:shellHook`, `flake.nix:devEnv`, and Docker Compose `env_file:`). Ship `.env.example` with all secret slots commented. **DB migration:** alembic 0003 drops `Settings.*_configured` booleans + `Settings.*_fingerprint` columns; secret presence becomes runtime-derived from env. **API surface:** `PUT /api/v1/settings/llm` becomes read-only — exposes "Anthropic configured via env: ✓ / ✗" without an input field. **UI:** drop the API-key input from `_settings_llm.html`; drop the vault-locked banner from `_settings_deployment.html`. **Files removed:** `src/services/vault.py`, `src/cli/vault.py`, `src/cli/init.py`, `tests/test_vault.py`, vault sections of `tests/test_cli.py`. **Operational surface gone:** `~/.naavik/secrets.enc{,.lock,.bak.*}`, `~/.naavik/key.bin`, `~/.naavik/logs/vault-audit.log`. **UX regression to flag in changelog:** rotating an LLM API key is now "edit `.env` + restart". | [x] | HIGH | 2.12 | **Plan 26 EXECUTED 2026-05-19 via PR #92 squash `b91f50b` (closes #20).** Shipped Alembic 0004 instead of 0003 (numbering aligned w/ existing migrations). Migration story: Option A (pure docs path — no shim, no script). Per locked decision recorded in `.claude/memory/decisions.jsonl` (`0.2.0.01-migration-story`). 8 commits W0-W5 + W6/W7/W8 delta. Reviewers cleared at APPROVE (hacker round-1 1 LOW + 2 INFO defer-recommendations; architect FULL spec match post-delta). 7 substantive deviations promoted into archived plan. **Blocks `0.2.0.02`** unlocked — CLI now collapses to `naavik serve` only. **`0.2.1.03` (Argon2id) auto-closes as moot.** |
| 0.2.0.03 | Broader `require_password_complete` gate — extend to `src/api/profile.py` + `src/api/settings.py` + `src/ui/routes/*` | [x] | — | PC.6a | **Plan 23 EXECUTED 2026-05-18 via PR #69 — FROZEN done position.** New `require_authed_session` wrapper in `src/services/auth.py:297-371` applied to **41 mutation routes** across `src/api/profile.py` (7) + `src/api/settings.py` (5) + `src/ui/routes/{settings 9, discover 9, outreach 8, tracking 1, email 1, integrations 2}`. 2 onboarding routes skipped. 13 new tests. Hacker APPROVE_WITH_NOTES (1 MEDIUM onboarding bypass → PC.6b → 0.2.0.04). |
| 0.2.0.04 | Onboarding bypass — `/api/v1/profile/from-extraction` lets flagged user replace real-JWT with fake-session (close hacker MEDIUM from PR #69) | [x] | — | PC.6b | **Plan 42 EXECUTED 2026-05-20 via PR #132 squash `116923f` (closes #70).** `post_profile_from_extraction` (`src/ui/routes/auth.py:223-274`) now runs `select(func.count()).select_from(User)` count probe BEFORE setting the `naavik_session=fake-1` cookie; if `existing_count > 0` returns 409 + HTMX-swappable error card with inline `<a href="/login">` link (no HX-Redirect — strictly safer than plan per hacker NOTE 2). Fresh-install path (count==0) preserves 204 + `HX-Redirect: /` + cookie SET. Pattern matches existing `_compute_signup_disabled:54-65` + `post_signup:196-203`; no broad-except wraps the probe (transient DB hiccup surfaces as 500, not silent demotion). 3 LOW hacker notes from PR #69 folded in same PR: (1) `tests/test_auth.py:494` monkeypatch guard comment; (2) `tests/test_auth_gate.py:184-237` positive-assertion-pinned route after negative sweep; (3) `src/services/auth.py:309-310` docstring annotation-tightening note. 4 new tests (`test_from_extraction_{blocked_when_user_exists,blocked_when_flagged_user_exists,allowed_on_fresh_db,409_response_is_htmx_swappable}`); +4 cases net delta. Manual QA via Playwright captured (`traces/.../qa/`) — populated-DB 409 + fresh-DB 204 paths both verified. Reviewers: hacker APPROVE_WITH_NOTES (2 non-blocking: redundant flagged-user test + HX-Redirect deviation promoted to plan archive); architect APPROVE FULL spec match. 2 deviations promoted to archived plan. **No new operational surface** — pure logic fix on existing route. **Closes the multi-user expansion blocker** flagged at PR #69 review. |
| 0.2.0.05 | SQLModel: Job, StatusHistory models + migration | [x] | HIGH | 2.6 | **Plan 27 EXECUTED 2026-05-19 via PR #95 squash `693bb1e` (closes #15).** Reframed from "StatusHistory" → `JobScrapeRun` (scrape-side observability; AppEvent already audits Application.status). Job hardened: 6 new cols + partial-unique constraint + 5 new enums (JobSource 10v / VisaRestriction 4v / RemotePolicy 4v / SeniorityLevel 7v / JobScrapeRunStatus 5v). New `src/services/job_service.py` (8 fns) + Alembic 0005. Graduated to `docs/design/JOB_MODEL.md` (470 LOC × 11 sections). 53 plan-27 tests + 731 full suite passing. Reviewers: architect APPROVE FULL spec match; hacker APPROVE_WITH_NOTES (1 MED + 2 LOW + 2 INFO — all forward-looking, filed as 0.7.0.15-0.7.0.19). 4 engineer deviations promoted to archived plan. **Unblocks `0.2.0.06`–`0.2.0.14`** (scraper chain). |
| 0.2.0.06 | Crawl4AI setup + generic scraper base class | [x] | — | 2.1 | **Plan 29 EXECUTED 2026-05-19 via PR #96 squash `90e2928` (closes #10).** Shipped substrate-only: `ScraperBase` ABC + `RawJob` Pydantic DTO (17 fields, extra=forbid) + `Crawl4AIClient` wrapper (stealth + streaming) + `SampleScraper` test fixture + `scraper_service.run_scraper` lifecycle orchestrator. `crawl4ai==0.8.6` exact pin (litellm hotfix floor); `playwright` promoted to base deps. Graduated to `docs/design/SCRAPER_BASE.md` (~330 LOC × 11 sections). 7 commits W0-W6 (W6 delta for architect HIGH on RawJob→Job adapter). 785 tests passing (+49 new across 5 test files). 6-field rename adapter `RawJob.to_upsert_payload()` with `set(payload).issubset(_JOB_CREATE_FIELDS)` contract assertion. Reviewers: architect APPROVE post-delta (R1 HIGH+LOW resolved); hacker APPROVE_WITH_NOTES (2 MED + 2 LOW filed as `0.2.0.06a` #97 — must ship BEFORE 0.2.0.07). 4 engineer + 1 delta deviation promoted to archived plan. **Unblocks `0.2.0.07`–`0.2.0.14`.** Replaces Browserless. |
| 0.2.0.06a | Scraper hardening — SSRF/LFI scheme allowlist + errors[] redaction + max_listings cap + asyncio fix | [x] | MEDIUM | A.37 (hacker PR #96) | **Plan 31 EXECUTED 2026-05-19 via PR #98 squash `8657bcb` (closes #97).** All 4 hacker findings closed: (1) `pydantic.HttpUrl` + `TypeAdapter` validation in `Crawl4AIClient.fetch_html` rejects `file:`/`ftp:`/`gopher:`/`data:`/`javascript:` while preserving `http://localhost` dev; (2) new `src/scraper/redaction.py` with `safe_url`/`safe_exc` helpers wired into `scraper_service.run_scraper` errors[] composition + `crawl4ai_client.py` `log.warning` callsites; (3) `ScrapeQuery.max_listings: Field(default=200, ge=1, le=10_000)`; (4) `asyncio.get_running_loop()` swap at 2 callsites. Graduated cross-refs added to `docs/design/SCRAPER_BASE.md § E.1` + new `§ H.4`. 8 net-new tests across 3 files. Reviewers: architect APPROVE (full spec match); hacker APPROVE_WITH_NOTES (3 LOW residual — `stream_many` SSRF + log-redaction at 2 callsites + `safe_exc` control-char strip — filed as `0.2.0.06b` HIGH priority follow-up gating `0.2.0.07`). 2 engineer deviations promoted to archived plan. |
| 0.2.0.06b | Scraper hardening round 2 — `stream_many` SSRF allowlist + `safe_msg` log redaction + `safe_exc` control-char strip | [x] | HIGH | (hacker PR #98) | **Plan 32 EXECUTED 2026-05-19 via PR #101 squash `1df4fd4` (closes #100).** All 3 LOW residuals from PR #98 closed: (1) `Crawl4AIClient.stream_many` per-URL `HttpUrl` validation via existing `_HTTP_URL_ADAPTER`; rejected URLs log + skip; (2) new `safe_msg(s: str \| None) -> str` helper in `src/scraper/redaction.py`; both `crawl4ai_client.py` `log.warning` callsites swapped; (3) `_strip_control_chars` extracted from `safe_exc`; strips ANSI CSI + C0 (preserves `\t` + `\n`) + DEL; runs BEFORE 200-char cap (length-truncation-injection-safe). SCRAPER_BASE.md § H.4 updated. 3 net-new tests. Both reviewers APPROVE (state=COMMENTED self-approval pivot per knowledge entry). Forward refs for 0.2.0.07: userinfo URL reject + RFC1918/IMDS/link-local IP denylist captured in plan 32 § Forward refs. |
| 0.2.0.07 | Site scrapers: LinkedIn + Workday + Greenhouse + Lever + Ashby + Indeed + URL guard | [x] | — | 2.2 | **Plan 33 EXECUTED 2026-05-19 via PR #102 squash `998a735` (closes #11).** 6 per-source `ScraperBase` subclasses at `src/scraper/sites/{linkedin,workday,greenhouse,lever,ashby,indeed}.py`; LinkedIn uses guest-API + Crawl4AI stealth primary + RSShub fallback. New `src/scraper/url_guard.py:is_safe_destination` closes 0.2.0.06b forward refs (userinfo URL reject + RFC1918/IMDS/link-local IP denylist); wired into `Crawl4AIClient.fetch_html` + `stream_many`. New `_BaseSiteScraper._maybe_enrich` lazy-imports `services.job_extractor` (no-op until 0.2.0.08 wires extraction). `ScraperBase.__init__` extended to accept `session/user_id/provider` (all optional). Registry populated for 6 sources keyed by `JobSource.value`. Lint regression guard `tests/test_no_direct_http_imports.py`. 5 new env-var slots added to `Settings` + `.env.example` + README. Graduated to `docs/design/SCRAPER_SITES.md`. 89 net-new tests across 9 files + 13 HTML fixtures. Reviewers: architect APPROVE_WITH_NOTES (2 LOW — rate_limit_per_minute int vs float + CLAUDE.md slot propagation); hacker APPROVE_WITH_NOTES (2 MEDIUM filed as 0.2.0.07a — slug-validate URL components in Workday tenant + Greenhouse/Lever/Ashby company template substitution; 2 LOW filed as 0.2.0.07b + 0.2.0.13a + 1 INFO debug-mode loopback port). 5 engineer deviations promoted. **Unblocks 0.2.0.08 (extraction)**, 0.2.0.09 (dedup), 0.2.0.10 (scheduler), 0.2.0.11 (UI), 0.2.0.12 (notifications). |
| 0.2.0.07a | Slug-validate URL components in 6 site scrapers + lift to `scraper.url_guard._make_url(template, **slugs)` helper | [x] | MEDIUM | (hacker PR #102) | **Plan 43 EXECUTED 2026-05-20 via PR #133 squash `d72d0a1` (closes #103).** Both hacker MEDIUMs from PR #102 closed via new `scraper.url_guard._make_url(template, **slugs) -> str` chokepoint with regex `^[a-zA-Z0-9][a-zA-Z0-9_-]*$` validation BEFORE template substitution + typed `InvalidSlugError(ValueError)` rejection. New `_BaseSiteScraper._compose_url(template, *, stage, **slugs)` wrapper mirrors existing `_maybe_enrich` shape — returns `None` on invalid slug + appends to `self._errors` with `_strip_control_chars`-redacted value. 4 affected scrapers (Workday, Greenhouse, Lever, Ashby) routed through wrapper; LinkedIn + Indeed received comment-only edits (no slug substitution in their URL templates). SCRAPER_BASE.md + SCRAPER_SITES.md updated. **0.7.0.28 hook regex gap** folded into this PR (sha `1801e83` on branch). Two-layer defense: `_make_url` closes composition-bug vector (URL never composes when slug is hostile); existing 0.2.0.06b `is_safe_destination` closes DNS-resolution vector. 17 files changed; +31 net tests (plan promised +16; parametrize block yielded 15 hostile shapes inline). All 4 per-scraper integration tests assert BOTH `rawjobs == []` AND `client.fetch_calls == []` on hostile inputs — load-bearing invariant proves no outbound HTTP. Manual QA: 49/49 PASS in `traces/.../qa/slug-validate-integration.txt`. Reviewers: hacker APPROVE (0 actionable findings; 2 INFO observations); architect APPROVE (D.1-D.7 spec match PASS, coverage PASS across 6 promises). 4 engineer + 2 architect/hacker INFO deviations promoted to archived plan. **No new operational surface** — pure internal hardening. **Closes the multi-tenant cloud blocker** flagged by hacker. |
| 0.2.0.07b | Expand `test_no_direct_http_imports.py` — prefix match + sibling libs + AST attribute walk | [x] | LOW | (hacker PR #102) | **EXECUTED 2026-05-20 via PR #146 squash `3b386f7` (closes #104).** Plan 46 batched closeout (item 1). Prefix-match helper `_is_forbidden(name)` + `urllib.parse` whitelist + AST walk of `ast.Import` (alias.name) + `ast.ImportFrom` (node.module) over expanded `FORBIDDEN_MODULES = {"requests", "httpx", "urllib", "urllib3", "http.client", "httpcore", "niquests", "aiohttp"}`. 12 new parametrized test cases cover positive (each bypass shape rejected) + negative (`urllib.parse` allowed). ~170-line test diff. |
| 0.2.0.13a | Replace `url_guard._resolve_host` LRU cache with TTLCache(ttl=60) to bound DNS rebinding TOCTOU window | [x] | LOW | (hacker PR #102) | **CLOSED via fold-in to PR #118 (`0.2.0.13`) squash `19a3dbf` 2026-05-19.** `_DNS_CACHE` swapped from `@lru_cache(maxsize=256)` to `cachetools.TTLCache(maxsize=256, ttl=60.0)` at `src/scraper/url_guard.py:66` per plan 38 § D. New base dep `cachetools>=5.0,<6`. Thread-safety TODO documented inline (multi-worker scrape orchestration). Issue #105 closed 2026-05-20. |
| 0.2.0.08 | AI job extraction: HTML → JobInfo (company, position, location, visa, salary, skills) | [x] | — | 2.3 | **Plan 30 EXECUTED 2026-05-19 via PR #106 squash `2c30367` (closes #12).** New `src/services/job_extractor.py:enrich_raw_job(session, *, user_id, provider, raw_job, settings=None) -> RawJob` — bs4 boilerplate strip (conservative `_DROP_TAGS` preserves `<aside>`/`<main>`/`<article>` for LinkedIn JD right-rail) + 30k-char cap + `services.llm_tracker.tracked_call` wrap (closes the cost-cap blind spot the deleted skeleton bypassed) + structured output via new `JobExtraction` Pydantic v2 model (15 fields, `extra="forbid"`). Old `src/llm/prompts/extract_job.py:ExtractedJob` skeleton + bypassing `extract_job()` function DELETED. Source-agnostic prompt with 9-tag vocabulary enum-string canonical-lowercase guidance. Merge semantics: OVERWRITES `description_text` / `*_hint` enum trio / `location_raw` / `posted_at`; PRESERVES scraper identity quad (`source` / `external_id` / `source_url` / `board`); MERGES scorer arrays (`skills_required` / `criteria` / `tags` / `salary_min/max`) into `raw_meta`. 7-row failure-mode table: `extraction_skipped` marker on empty-html / schema-invalid / provider-failure. Integration with `_BaseSiteScraper._maybe_enrich` (shipped 0.2.0.07) verified — lazy-import resolves cleanly; 6 production scrapers now AI-enrich enabled when `session/user_id/provider` are passed to `__init__`. 15 net-new tests + 5 HTML fixtures + `_FakeLLMProvider` duck-type. Reviewers: architect APPROVE (full spec match; 3 deviations accepted — 14→15 field plan-prose off-by-one + 15>12 tests + `EXTRACT_JOB_PROMPT` alias); hacker APPROVE_WITH_NOTES (1 LOW — `JobExtraction.tags` Literal constraint deferred per OQ.5; filed as `0.2.0.08a` #107). 3 engineer deviations promoted. Doc graduation to `docs/design/JOB_EXTRACTION.md` deferred to bookkeeping. **Unblocks 0.2.0.09 dedup + 0.2.0.10 scheduler.** |
| 0.2.0.08a | Harden `JobExtraction.tags` to `Literal[...]` constraint (9-tag vocabulary enforcement at schema layer) | [x] | LOW | (hacker PR #106) | **EXECUTED 2026-05-20 via PR #146 squash `3b386f7` (closes #107).** Plan 46 batched closeout (item 2). `tags: list[TagLiteral]` where `TagLiteral = Literal["ai-ml","backend","frontend","devops","data-eng","genai","leadership","platform","product"]` — closed 9-tag vocab. LLM hallucinations now fail-fast at Pydantic boundary with `ValidationError`. ~22-line source + 33-line test. |
| 0.2.0.09 | Job deduplication (URL-based + fuzzy title/company) | [x] | — | 2.4 | **Plan 34 EXECUTED 2026-05-19 via PR #108 squash `929f234` (closes #13).** Tier-3 fuzzy cross-source dedup wedge in `upsert_job` pre-INSERT: pg_trgm GIN candidate filter (`lower(company) % :norm_company` similarity > 0.3) + rapidfuzz `token_set_ratio` precision-score @ 88.0 threshold (weighted 0.6 × company + 0.4 × role). New `src/services/dedup.py:find_duplicate(session, *, user_id, raw_job) -> Job | None` (145 LOC) mirrors `contact_tracker` shape. New `Job.duplicate_of_id` FK column (ON DELETE SET NULL); both rows survive (no soft-delete); `list_jobs` default filters via `JobFilter.include_duplicates: bool = False`. Alembic 0006: `CREATE EXTENSION IF NOT EXISTS pg_trgm` + FK + GIN trigram index `ix_job_company_trgm`; sqlite test fallback uses LIKE matching. New base dep `rapidfuzz>=3.10.0,<4` (3.14.5; MIT; nixpkgs `python312Packages.rapidfuzz`). 14 new tests + 3 alembic round-trip tests. Reviewers: architect APPROVE FULL spec match; hacker APPROVE 0 findings (`DEDUP_CANDIDATE_LIMIT=20` caps trigram-bomb, parameterized text() bindings, multi-user isolation verified). 6 engineer deviations (all benign — `_FakeSession.bind` substrate + sqlite FK skip + LIKE fallback + scraper-test cold-cache +1 + lockfile note + docstring refresh). **Unblocks 0.2.0.10 scheduler.** |
| 0.2.0.10 | APScheduler: periodic scraping per source | [x] | — | 2.5 | **Plan 35 EXECUTED 2026-05-19 via PR #109 squash `a6c441c` (closes #14).** New `src/scheduler/scraping.py:register_scraping_jobs` registers 6 per-source crons (5 CronTrigger LinkedIn/Workday/Greenhouse/Lever/Ashby + 1 IntervalTrigger(minutes=90) for Indeed — `*/90` invalid cron). All 6 carry `jitter=30` + `max_instances=1` + `misfire_grace_time=300`. Per-user dispatch iterates Settings rows; LLM provider resolved per-firing per-user; `provider=None` graceful degrade on LLMProviderError. Alembic 0007 adds 5 nullable Settings cols (`linkedin_keywords`, `linkedin_location`, `indeed_keywords`, `indeed_location`, `consecutive_scrape_failures: dict[str, int]`). Failure handling: counter increments on FAILED; scrape ALWAYS runs (no skip-on-counter — closes hacker HIGH); notify_admin_error fires ONCE on threshold-cross (counter 2→3), silent thereafter until SUCCESS resets. Top-level broad-except emits admin signal. Initial round APPROVE_WITH_NOTES (architect) + REQUEST_CHANGES (hacker HIGH auto-skip deadlock + 2 MEDIUM + 1 LOW); delta commit `c89ab2d` closed all 3 in-scope findings; pickle-RCE deferred to `0.2.0.10b`. Final reviewers APPROVE clean. 5 deviations (JSON counter on Settings vs JobScrapeRun history scan; single commit vs 4-wave; SCHEDULER.md graduation deferred; D.5 contract reshape; alembic 0007 = 5 cols not 4). 32 net-new tests. |
| 0.2.0.10a | Add `/api/v1/scheduler/*` endpoints (list / run / pause / resume) — plan 35 § D.7 stub never implemented | [x] | MEDIUM | (architect PR #109) | **Plan 47 EXECUTED 2026-05-20 via PR #147 squash `0ede6c9` (closes #110).** New `src/api/scheduler.py` (200 LOC) exposes 4 endpoints under `/api/v1/scheduler/`: `GET /jobs` (list registered APScheduler jobs), `POST /jobs/{id}/run` (one-off via transient `add_job(DateTrigger(now), id="<orig>-manual-<uuid8>")` — preserves cron `next_run_time`), `POST /jobs/{id}/pause`, `POST /jobs/{id}/resume`. 3 Pydantic v2 response models co-located. Auth: `require_authed_session` for all; mutations also `Depends(require_csrf)` mirroring `src/api/auth.py:283-291`. `JobLookupError` → 404; `get_scheduler()==None` → 503. 9 tests via `_FakeScheduler` namespace + monkeypatch. README § Operations §§ scheduler-endpoints (operator surface). 4-line note. Reviewers: hacker APPROVE 0 findings (16/16 checklist clean; 3 observational notes — per-IP rate limit deferred + job_id flag-for-refactor + bookkeeping); architect APPROVE FULL spec match (D.1-D.5 plan decisions all PASS). Both via self-approval pivot (state=COMMENTED + verdict in body). No material engineer deviations (test count 9 vs floor 8). |
| 0.2.0.10b | Replace `SQLAlchemyJobStore` pickle deser with safer alternative (MemoryJobStore or JSON serializer) | [x] | MEDIUM | (hacker PR #109) | **Plan 48 EXECUTED 2026-05-20 via PR #148 squash `59110f0` (closes #111).** Option B locked: new `NaavikJsonJobStore(SQLAlchemyJobStore)` subclass (~140 LOC) with JSON encode/decode + `FUNC_REF_ALLOWLIST` frozenset (11 module:name refs — 5 admin + 6 scraping) closing the secondary `apscheduler.util.ref_to_obj → __import__` vector. Overrides 3 public methods (`add_job`, `update_job`, `_reconstitute_job`); private `_pickle_job` hook does not exist in APScheduler 3.x. Per-trigger codec for `DateTrigger`/`CronTrigger`/`IntervalTrigger`. Engineer corrected plan bug: `DateTrigger.run_date.tzinfo` not `DateTrigger.timezone` (slot doesn't exist). Alembic 0009 decodes existing pickle BYTEA + re-encodes JSON + corrupt-row skip + downgrade reverses. Allowlist self-introspection test (`test_allowlist_matches_register_all`) fails CI when new scheduled job ships without allowlist update. 15 tests (11 jobstore + 4 alembic). Manual QA: 11 jobs register / 0 pickle rows post-migration / 11 reconstituted. POST_PHASE_1.md Tier-3 entry added. Reviewers: hacker APPROVE 0 findings (3 JSON-injection bypass attempts rejected); architect APPROVE FULL spec match (4/4 deviations ACCEPTABLE). Both via self-approval pivot. **Pickle elimination PASS** — `src/` runtime pickle-free; migration is sole pickle import (one-shot decode-and-re-encode). |
| 0.2.0.11 | HTMX UI: job list with filters, job detail view | [x] | — | 2.7 | **Plan 36 EXECUTED 2026-05-19 via PR #112 squash `47d78ec` (closes #16).** Wired `job_service.list_jobs` into existing `/discover` swipe queue (replaces `db.sample_data` shim). New 6-axis filter chip-row toolbar (sticky top): `source` / `remote_only` / `visa` / `seniority` / `score_min` / `include_duplicates`. HTMX fragment-swap into `#discover-main` with `hx-push-url=true` for bookmarkable URLs. New `/jobs/{job_id}` read-only Job-detail route (separate from `/discover/{id}` application workspace). Offset+limit pagination. **Folded in 0.7.0.15** — `archive_job` + `restore_job` now carry `user_id` boundary check (raise `PermissionError` on cross-user mutation). 8 new templates/macros (`filter_toolbar.html`, `filter_chip` macro, `_filter_hidden_inputs.html`, `job_topbar.html`, `_job_detail_body.html`, `job_detail.html`, etc.) + new `src/ui/routes/jobs.py` + `src/ui/jobs_ctx.py`. 20 net-new tests (16 route + 2 IDOR + 2 lint). Manual QA: Playwright captured `/discover`, filtered Discover, `/jobs/101` at 1440×900 + 375×812. Reviewers: architect APPROVE_WITH_NOTES (1 MEDIUM Wave 5 doc graduation deferred → `0.2.0.11a`; D.1-D.5 + IDOR all PASS); hacker APPROVE_WITH_NOTES (1 MEDIUM pre-existing CSRF gap on `/api/v1/discover/save/skip` PR widens but doesn't introduce → `0.2.0.11b`; 1 LOW `raw_meta` exposure → `0.2.0.11c`). 8 engineer deviations promoted. **0.7.0.15 closed via fold-in.** |
| 0.2.0.11a | Doc graduation for plan 36 — `docs/design/JOB_UI.md` + SCREENS.md screen #12 + COMPONENTS.md registry entries | [x] | MEDIUM | (architect PR #112) | **EXECUTED 2026-05-20 via PR #134 squash `9b6773a` (closes #113).** New canonical design doc `docs/design/JOB_UI.md` (272 LOC, 10 H2 sections A-J: contract / surface inventory / 6-axis JobFilter URL contract / component composition / HTMX patterns / data accessors + IDOR boundary / build invariants / mockup status / forward-pointers / pointer index). Screen #12 (Job detail, `/jobs/{id}`) added to `docs/design/SCREENS.md`. COMPONENTS.md inventory bumped 85→88 (Discover group 8→11) with full specs for `filter_toolbar` + `_filter_hidden_inputs.html` + `job_topbar.html` + `filter_chip` macro registry entry. Cross-ref pointers added to `BACKEND.md § D.3` (IDOR note + HTML-page-routes sub-table) + `JOB_MODEL.md § K` (pointer-index row). 5 files modified + 1 new = 426 insertions / 12 deletions. Architect single-dispatch (no engineer needed — pure design-doc work). Reviewers: hacker APPROVE (0 findings, 3 notes); architect APPROVE (graduation contract PASS, 10/10 H2 sections substantive). Self-approval pivot in effect (architect=author; state=COMMENTED + verdict in body). **No new operational surface.** 2 INFO NOTES from reviewers (JOB_MODEL.md companion-docs header missing JOB_UI.md, PR-description-only path typo) — non-blocking, doc cosmetic. |
| 0.2.0.11b | CSRF + IDOR on `/api/v1/discover/save/skip` — `Depends(require_csrf)` + migrate sample_data → job_service with user_id boundary | [x] | MEDIUM | (hacker PR #112) | **Plan 44 EXECUTED 2026-05-20 via PR #138 squash `57fef25` (closes #114) — PART (a) ONLY.** `Depends(require_csrf)` added to `post_skip` (`src/ui/routes/discover.py:96`) + `post_save` (`:109`) + `post_auto_submit` (`:123`); pattern mirrors canonical `post_change_password` at `src/api/auth.py:283-291`. 6 new tests in `tests/test_discover_csrf.py` (reject-missing + accept-matching per endpoint); 6 pre-existing tests in `tests/test_stub_endpoints.py` + `tests/test_auth_gate.py` patched to thread CSRF cookie+header through swipe POSTs so the gate-under-test isn't masked by CSRF 403. Manual QA: 4-case curl matrix (no-auth 401 / fake-session-only 403 / mismatched 403 / matching 200). 1108 tests passing / 26 skipped / 0 failing. **Part (b) (job_service + user_id boundary) DEFERRED** until fake-session retirement (multi-PR refactor; not gated on this PR). **Surfaced PRE-EXISTING defect at manual QA gate**: empty `csrf_token` Jinja context → production HTMX swipes 403 (same defect affects plan-18 change-password page since 2026-05-17). **Filed `0.2.0.11d` HIGH (#139)** for the cross-cutting context-wiring fix; merged this PR with that follow-up per both reviewers' independent calls (security boundary is correct + valuable; UX regression is pre-existing). Reviewers: hacker APPROVE_WITH_NOTES (1 MED surfaced-defect; 16/16 checklist clean); architect APPROVE_WITH_NOTES (9/9 spec match PASS; deviation 3 deferred). 3 engineer deviations promoted to archived plan. |
| 0.2.0.11c | Switch GET `/api/v1/jobs/{id}` to `JobRead` projection — drop `raw_meta` JSONB exposure | [x] | MEDIUM | (hacker PR #112) | **EXECUTED 2026-05-20 via PR #146 squash `3b386f7` (closes #115).** Plan 46 batched closeout (item 3). `src/ui/routes/jobs.py:get_job_json` now uses `JobRead.model_validate(job).model_dump(mode="json")` with `ConfigDict(from_attributes=True)`; `raw_meta` removed from `JobRead`. New regression test `test_api_v1_jobs_get_json_uses_jobread_projection` asserts `raw_meta not in body`. Engineer also updated `docs/design/JOB_MODEL.md:359` to reflect projection change (folded in same PR per architect review). 35-field stub rewrite in `test_stub_endpoints.py::test_jobs_get_by_id` (engineer deviation 1 — `JobRead.model_validate` required all fields). Hacker NOTE-1 LOW filed as `0.2.7.01` — parallel `raw_meta` leak vectors in `src/ui/routes/discover.py:256/271/282/287/292` (sample-data routes today; will leak when wired to real DB Job; pre-emptive defense-in-depth deferred). |
| 0.2.0.11d | Jinja `csrf_token` template-context injection — close HTMX-403 gap on Discover Save/Skip + change-password page | [x] | HIGH | (reviewers PR #138) | **Plan 45 EXECUTED 2026-05-20 via PR #140 squash `54934c3` (closes #139).** Starlette `Jinja2Templates(context_processors=[_csrf_token_ctx])` registered at `src/ui/templates_setup.py`. Processor reads `request.cookies.get(CSRF_COOKIE, "")` and injects into every `TemplateResponse(request, ...)` ctx — one place, ~12 LOC, covers all 40 callsites with no per-handler edits. Empty-string fallback on absent cookie is intentional — preserves `validate_csrf`'s empty-rejects-empty invariant for unauthenticated landing pages. 3 new tests in `tests/test_csrf_jinja_ctx.py` (discover-with-cookie + change-password-with-cookie + empty-fallback regression guard). Full pytest suite 1111 passed / 26 skipped / 0 failed (zero regression). Provenance: engineer dispatch killed mid-execution by session interrupt; manager-direct close-out per user request (option 1 of 3 surfaced at session resume); engineer's edits matched plan 45 § D.3 + § D.4 byte-for-byte; manager verified via ruff + full pytest. Reviewers: hacker APPROVE (0 findings); architect APPROVE (spec match 11/11 PASS). 1 engineer deviation (manager-direct provenance) promoted to archived plan. **No new operational surface.** Closes the cross-cutting empty-`csrf_token` defect that PR #138's manual QA gate surfaced. |
| 0.2.0.12 | Discord + Telegram notifications for new jobs | [x] | — | 2.8 | **Plan 37 EXECUTED 2026-05-19 via PR #116 squash `81b9a77` (closes #17).** Post-scrape `notify_scrape_run_summary` fan-out to Discord embeds + Telegram plain-text + Toast. Reuses Phase 1 `Settings.notifications_enabled` JSONB. Top-5 newly-inserted jobs per scrape run; duplicates excluded; URL list with company/role. Score-blind (n8n parity; scoring is 0.3.0). `notify_telegram` helper added. `_settings_notifications.html` swapped `.items()` for explicit 7-event catalog (closes pre-existing rendering hole for new users). BACKEND.md § L.3 + § L.4 + § H.3 updated. 17 net-new tests. Hacker MEDIUM (Telegram Markdown injection via scraper-controlled `role`/`company`) closed via delta commit `c4d4154`: dropped `parse_mode=Markdown` from `_send_telegram_scrape_run` + dropped header `*...*` bolding. Symmetric pre-existing path in `_telegram_text_for_event` deferred as `0.2.0.12a`. 3 deviations (BACKEND.md section + notifications catalog rewrite + D.5 Markdown→plaintext) promoted to archived plan. |
| 0.2.0.12a | Apply symmetric Telegram parse_mode/escape fix to `_telegram_text_for_event` + `send_telegram` (wave-6 high-score path) | [x] | LOW | (hacker PR #116) | **EXECUTED 2026-05-20 via PR #146 squash `3b386f7` (closes #117).** Plan 46 batched closeout (item 4). `_telegram_text_for_event` drops `*...*` bolding on `EVENT_NEW_HIGH_SCORE` + `EVENT_APPLICATION_SENT` branches; `send_telegram` drops `parse_mode=Markdown`. Symmetric with PR #116 c4d4154 fix on scrape-run summary path. Hostile-string test asserts verbatim rendering. ~21-line source + 53-line test. |
| 0.2.0.13 | Rate limiting + anti-detection (random delays, throttling) | [x] | — | 2.9 | **Plan 38 EXECUTED 2026-05-19 via PR #118 squash `19a3dbf` (closes #18 + #105 / 0.2.0.13a).** New `Settings.scraper_rate_limits` JSONB col (alembic 0008; per-source `rpm`/`delay_lo`/`delay_hi` Pydantic v2 `RateLimitConfig` validates) lets operators tune without code edits; `scraper.rate_limit.resolve_rate_limit(settings, source)` returns override > class-attr fallback (with `isinstance(dict)` guard for hostile JSONB). `Crawl4AIClient` adopts `crawl4ai.RateLimiter` for 429/503 exponential backoff in `stream_many`'s `MemoryAdaptiveDispatcher` (`arun()` doesn't accept rate_limiter kwarg in 0.8.6 so `_enforce_min_interval` floor stays for `fetch_html`). New constructor kwargs `user_agent` + `use_undetected_adapter`; telemetry counters surface in `JobScrapeRun.raw_meta["rate_limit"]` + `["adapter_used"]`. `ScraperBase.rate_limit_per_minute: int → float` (LinkedIn 1 → 0.4 effective). New `src/scraper/user_agents.py` curated 8-UA round-robin. New base dep `cachetools>=5.0,<6` — `url_guard._DNS_CACHE` swap to `TTLCache(maxsize=256, ttl=60)` closes 0.2.0.13a DNS-rebind TOCTOU. `ScraperBase.use_undetected_adapter: bool = False` wiring + telemetry; engagement deferred to `0.2.0.13c`. Robots.txt explicit no-honor in SCRAPER_BASE.md § G.10. Initial round REQUEST_CHANGES (hacker HIGH HTTP endpoint silently drops `scraper_rate_limits` + 4 plan-35 kwargs + MEDIUM resolver AttributeError + LOW thread-safety); delta commit `6b96e89` wires 8 kwargs + 422 conversion + isinstance guard + TODO comment. **Carries forward a plan-35 fix** (the LinkedIn/Indeed `*_keywords`/`*_location` route kwargs were also silently dropped). 41 + 7 new tests. Graduated to `docs/design/SCRAPER_BASE.md § G`. 7 deviations promoted (alembic id length cap, RateLimiter dispatcher-only, AsyncPlaywright import path, backoff_total_s wall-clock proxy + 3 delta-round). |
| 0.2.0.15 | 0.2.0 release ceremony — `naavik-ops release cut 0.2.0` + version bump + CHANGELOG curation + git tag + release notes | [x] | HIGH | (release-readiness audit 2026-05-20) | **SUBSUMED BY `0.2.6` TAG CUT 2026-05-20 per user directive to batch ship all 0.2.x in one session.** All 0.2.0 + 0.2.1 + 0.2.2 + 0.2.3 + 0.2.4 + 0.2.5 + 0.2.6 work shipped in single sweep (10 PRs); `naavik-ops release cut 0.2.6` cuts ONE tag covering everything since `0.1.1`. Sequential per-version ceremonies (6 separate tag-cuts) would have been operationally noisy; user directive prioritized speed + batching. CHANGELOG.md will reflect all 0.2.x work as a single 0.2.6 release block. |
| 0.2.0.16 | Self-host first-run walkthrough — README scraper config section + Settings · Sources sub-tab showing configured-vs-unconfigured scrapers | [x] | HIGH | (release-readiness audit 2026-05-20) | **Plan 49 EXECUTED 2026-05-20 via PR #149 squash `63b227b` (closes #142).** Two surfaces shipped: (a) new `/settings/sources` dedicated route + `_source_row.html` partial (89-partial catalog bumped 88→89) with per-source configured/unconfigured indicator + last-run timestamp + status chip; (b) README § Configuration "Scraper sources" + § Operations "Verify scrapers run" narrative. Rate-limit JSONB editor deferred to `0.2.5.05` per plan 49 OQ.3 lock; LinkedIn/Indeed keywords editor deferred to `0.2.5.06`. New canonical design doc `docs/design/SOURCES_UI.md` (140 LOC, 9 H2 sections). `job_service.list_recent_scrape_runs_by_source` + `env_secrets.scraper_source_configured` service helpers. 14 net-new tests. Reviewers: hacker APPROVE_WITH_NOTES (1 MED latent IDOR — `user_id=1` hardcoded in `_build_sources_view`, not exploitable in Phase 1 single-user MVP but cross-user info-disclosure on multi-user, filed as `0.2.7.02`; 1 LOW silent session=None fallthrough, filed as `0.2.7.03`; 1 NOTE README/popover env-var asymmetry on Workday `WORKDAY_COMPANIES` vs `Settings.workday_companies` filed as `0.2.7.04`); architect APPROVE_WITH_NOTES (1 LOW SCREENS.md § 11 pre-rewrite text needs update, filed as `0.2.7.05`). 6 engineer deviations promoted to archived plan. |
| 0.2.0.17 | Post-tag-cut anti-detection observation window — 7-14 day monitor of `JobScrapeRun.failure_count` + per-source `rate_limit_hits` + `backoff_total_s`; LinkedIn contingency plan | [~] | MEDIUM | (release-readiness audit 2026-05-20) | **IN PROGRESS 2026-05-20 — observation queries + LinkedIn contingency runbook filed at `.claude/memory/runs-analysis/0.2.0-observation-window.md` (gitignored per-fork).** SQL queries per task (1) + alerting threshold per task (2) + LinkedIn fallback runbook per task (3) shipped. Window ticking starts after 0.2.0.15 release cut. Operator runs daily queries; records findings in the same file. Mark `[x]` when 14 days elapsed without 3+ consecutive FAILED on any source. |
**Deliverable (0.2.0):** Jobs scraped on schedule, AI-extracted, deduplicated, shown in dashboard with notifications. Vault deprecated; secrets via `.env`; CLI removed. Tag-cut via 0.2.0.15; self-host walkthrough via 0.2.0.16; production-stability observation via 0.2.0.17.

---

### 0.2.1 — Security cleanup (legacy DEF security rows)
> **Goal:** Defer-bucket cleanup for security-themed paper cuts from Phase 1 (JWT signing-key rotation, OIDC stub, Argon2id, JWT denylist).
> **Status:** 🟡 Queued (post-0.2.0; can interleave).
> **Plan:** Per-row plans authored when scheduled.

| # | Task | Status | Priority | Legacy ID | Notes |
|---|---|---|---|---|---|
| 0.2.1.03 | Argon2id vault upgrade (vs PBKDF2) | [x] | — | DEF-17 | **Auto-closed 2026-05-19 as moot — vault deleted in `0.2.0.01` (PR #92 squash `b91f50b`).** No PBKDF2 to upgrade to Argon2id; secrets now read from `.env` via pydantic-settings. Locked decision at PLAN_GATE for plan 26. From plan 10 Q6. |
| 0.2.1.04 | JWT denylist on password rotation (defense-in-depth for stolen pre-rotation JWTs) | [x] | — | DEF-26 | **Plan 50 EXECUTED 2026-05-20 via PR #150 squash `77484ee`.** New `RevokedJwt` SQLModel + alembic 0010 + `services/auth.py` `revoke_jwt` / `is_jwt_revoked` / `cleanup_expired_revoked_jwts`. `change-password` endpoint inserts OLD jti into `revoked_jwt` table IN SAME TRANSACTION as password mutation (atomic — either both succeed or both rollback). JWT verification middleware checks `is_jwt_revoked` → 401 if revoked. Cleanup cron `admin.cleanup_revoked_jwts` registered at 03:30 UTC daily; `FUNC_REF_ALLOWLIST` updated. 8 tests in `tests/test_jwt_revocation.py`. Reviewers: hacker APPROVE 0 findings (race-safety verified via single-transaction); architect APPROVE 2 INFO notes. 6 engineer deviations promoted. Hacker filed 1 NOTE (concurrent change-password race could trigger `IntegrityError` on `uq_revoked_jwt_jti` — UX bug, not security) — folded into 0.2.7 multi-tenant follow-up bucket. |
| 0.2.1.05 | **CLI sunset** — depends on `0.2.0.01`. After vault deprecation ships, the only `naavik` subcommand left is `serve`; `init` / `vault status` / `vault rotate-key` are gone with the vault. Final cleanup: delete `src/cli/`, drop `[project.scripts] naavik = "cli.main:main"` from `pyproject.toml`, collapse the server entrypoint to `python -m main` or the Nix flake's `apps.default` output. Keep `naavik-alembic` (alembic's own CLI surface, not a Naavik feature). | [x] | — | — | **Plan 50 EXECUTED 2026-05-20 via PR #150 squash `77484ee`.** `src/cli/` deleted (3 files); `pyproject.toml [project.scripts] naavik` repointed to `main:main` (preserves operator UX of `naavik` CLI invocation — now just uvicorn launcher); `[tool.setuptools] packages` drops `"cli"`. New regression-lint `tests/test_no_cli_imports.py` (mirrors `tests/test_no_vault_imports.py`). README + DEPLOYMENT updated. CHANGELOG `[Unreleased]` got dedicated 0.2.1 sub-section. **Policy in AGENTS.md § Key Conventions § CLI codifies the "do not extend" rule.** |
**Deliverable (0.2.1):** Self-hosted OIDC option + post-vault-sunset auth-hardening cleanup.

---

### 0.2.2 — UI cleanup (legacy DEF UI rows)
> **Goal:** UI polish + light mode + icon/sidebar stability paper cuts from Phase 1.
> **Status:** 🟡 Queued.
> **Plan:** Per-row plans authored when scheduled.

| # | Task | Status | Priority | Legacy ID | Notes |
|---|---|---|---|---|---|
| 0.2.2.02 | Restore Lucide via CDN | [x] | — | DEF-19 | **Plan 51 EXECUTED 2026-05-20 via PR #151 squash `b74a19f`.** jsDelivr CDN @ `lucide@0.469.0` (version pinned to vendored banner) primary + `/static/lucide.min.js` `onerror` fallback. ~5 LOC `base.html` + 3 tests. SRI deferred (consistent with existing htmx/Sortable/Tailwind play posture — backlog candidate). |
| 0.2.2.03 | Sidebar mobile-toggle reliability after navigation | [x] | — | DEF-20 | **Plan 51 EXECUTED 2026-05-20 via PR #151 squash `b74a19f`.** `syncSidebarAria()` wired to `DOMContentLoaded` + `htmx:afterSwap` (was only on click). Hamburger ARIA stays consistent after back-button restore / history pop / hx-boost swap. ~8 LOC `base.js` + 1 test in `tests/test_mobile_sidebar.py`. |
| 0.2.2.04 | Discover card max-w cap on ultra-wide screens | [x] | — | DEF-21 | **Plan 51 EXECUTED 2026-05-20 via PR #151 squash `b74a19f`.** `2xl:max-w-[1400px] 2xl:mx-auto` on Discover wrapper. Tailwind 2xl = ≥1536px so 1440 monitors unaffected. ~3 LOC + 2 tests in new `tests/test_discover_max_w_cap.py`. |

**Deliverable (0.2.2):** Light mode shipped; sidebar stable; Lucide CDN-served; Discover card width-capped.

---

### 0.2.3 — ATS / scraper cleanup (legacy DEF scraper rows)
> **Goal:** Long-tail ATS adapters (Workday/LinkedIn/Indeed/Generic) + scraping diagnostics that didn't make 0.2.0.07.
> **Status:** 🟡 Queued.
> **Plan:** Per-row plans authored when scheduled.

| # | Task | Status | Priority | Legacy ID | Notes |
|---|---|---|---|---|---|
| 0.2.3.02 | Postmortem-on-failure: HTTP trace + AI summary on ATS failure | [x] | — | DEF-03 | **Plan 52 EXECUTED 2026-05-20 via PR #152.** New `src/services/ats_postmortem.py:capture_postmortem` runs from `application_service._record_failure` on every DRAFT submission failure. Writes `<DATA_DIR>/data/postmortems/<application_id>/<utc-ts>/{trace.json,analysis.md}` atomically (tempfile + rename). LLM analysis via `tracked_call(prompt_name="ats_postmortem")` with `PostmortemAnalysis` Pydantic v2 schema (Literal[failure_kind] + bounded summary/action). LLM-unconfigured → trace-only postmortem with placeholder. Best-effort: any internal exception → log + return None (never raises). New `GET /api/v1/applications/{id}/postmortem/{ts}` returns envelope; IDOR scoped 404; path-traversal blocked via strict regex + `Path.resolve().relative_to(data_root)`. 16 tests. `up_next_card.html` restructured for valid HTML (wrapper div + absolute card link + chip-link sibling). Reviewers: architect APPROVE 0 findings; hacker APPROVE_WITH_NOTES (1 MED `_redact` walks KEYS not VALUES — filed as 0.2.7.18; 1 LOW regex `^$` vs `\Z` cosmetic). Plan reframed Playwright → HTTP trace because 3 shipped adapters are HTTP-API; `screenshot_path` reserved for 0.2.7.10 Workday/LinkedIn/Indeed/Generic adapters. |

**Deliverable (0.2.3):** Full ATS coverage + scraper diagnostics + LinkedIn proxy hardening.

---

### 0.2.4 — Tracking cleanup (legacy DEF tracking rows)
> **Goal:** Tracking UI gap-fills + DRAFT-lifecycle hardening + manual-entry modal.
> **Status:** 🟡 Queued.
> **Plan:** Per-row plans authored when scheduled.

| # | Task | Status | Priority | Legacy ID | Notes |
|---|---|---|---|---|---|
| 0.2.4.01 | Stale-DRAFT cleanup cron (`admin.cleanup_stale_drafts`) | [x] | — | DEF-02 | **Plan 53 EXECUTED 2026-05-20 via PR #153.** New `application_service.cleanup_stale_drafts` + cron `admin.cleanup_stale_drafts` Sunday 03:30 UTC; archives DRAFTs idle >30 days. `StatusChangeTrigger.CLEANUP_STALE` enum value added. `FUNC_REF_ALLOWLIST` 12th entry added to `src/scheduler/json_jobstore.py`. 8 tests. |
| 0.2.4.02 | Manual job entry modal (full) | [x] | — | DEF-04 | **Plan 53 EXECUTED 2026-05-20 via PR #153.** New `_manual_job_entry_modal.html` (DaisyUI) + POST `/api/v1/jobs/manual` (now with `Depends(require_csrf)` after hacker HIGH delta fix) + GET `/_modal/manual-job` HTMX fragment. `JobCreate.url` Pydantic `AfterValidator` allows only http/https/manual:// schemes (closes hacker HIGH XSS finding). Synthetic URL `manual://entry/<uuid12>` for missing-URL case. 10 tests + 6 delta tests. |
| 0.2.4.03 | Application detail slide-over | [x] | — | DEF-05 | **Plan 53 EXECUTED 2026-05-20 via PR #153.** New `_application_detail.html` (154 LOC) + GET `/tracking/{application_id}` + GET `/_fragments/tracking/application/{id}`. IDOR via `_effective_user_id` returning 404 on cross-user. Template href guard for `startswith('http://'|'https://')` defense-in-depth after hacker LOW delta fix. 10 tests + 1 delta. |
| 0.2.4.04 | `Show drafts` filter UI on Tracking | [x] | — | DEF-08 | **Plan 53 EXECUTED 2026-05-20 via PR #153.** Filter chip on Tracking page; `show_drafts: bool` query param; ctx-builder branch reads `sample_data.draft_applications` accessor. 9 tests. |

**Deliverable (0.2.4):** Tracking UI feature-complete vs SCREENS.md spec; stale-DRAFTs auto-cleaned.

---

### 0.2.5 — Observability cleanup (legacy DEF observability rows)
> **Goal:** Cost-cap dashboard + submission-result analytics + rate-limit dial.
> **Status:** 🟡 Queued.
> **Plan:** Per-row plans authored when scheduled.

| # | Task | Status | Priority | Legacy ID | Notes |
|---|---|---|---|---|---|
| 0.2.5.01 | `Settings.scraper_aggressiveness` (rate-limit dial) | [x] | — | DEF-11 | **CLOSED-AS-MOOT 2026-05-20 — superseded by 0.2.0.13 `Settings.scraper_rate_limits` JSONB dial** (per-source `rpm`/`delay_lo`/`delay_hi` shipped via PR #118 squash `19a3dbf`). The original single-knob aggressiveness setting was preempted by the more granular JSONB approach in plan 38. |
| 0.2.5.02 | Submission-result observability dashboard (failure-kind aggregates) | [x] | — | DEF-16 | **Plan 54 EXECUTED 2026-05-20 via PR #154.** New `submissions` tab key in `_TAB_TEMPLATES`; `application_service.aggregate_submission_failures(session, *, user_id)` aggregates by `(board, failure_kind)` via `func.jsonb_extract_path_text` typed expression API; soft-delete + `has_key("last_failure")` predicates. 15 tests in `tests/test_settings_observability_dashboards.py`. |
| 0.2.5.03 | `Settings.daily_llm_cost_cap_usd` dashboard widget | [x] | — | DEF-22 | **Plan 54 EXECUTED 2026-05-20 via PR #154.** New `_llm_cost_cap_widget.html` partial folded into Settings · LLM tab. `llm_tracker.today_cost_usd` aggregates `ApiUsage.total_cost_usd` for current day with `func.coalesce(func.sum(...), 0.0)` + midnight-UTC boundary. Three-state widget: under-cap / approaching / over-cap. 11 tests. |
| 0.2.5.04 | Scraper-run history UI table — Settings · Sources sub-tab showing recent `JobScrapeRun` rows | [x] | — | (release-readiness audit 2026-05-20) | **Plan 54 EXECUTED 2026-05-20 via PR #154.** Expansion drawer per source in Settings · Sources tab (composes with plan 49 `_source_row.html`). `job_service.list_recent_scrape_runs(session, *, user_id, source)` ORDER BY started_at DESC LIMIT 50. `JobScrapeRunRead.model_validate` projection drops `raw_meta` (defense-in-depth re: PR #146 0.2.0.11c pattern). 8 tests. |

**Deliverable (0.2.5):** Operator-facing scrape + LLM cost + ATS-failure analytics dashboards.

---

### 0.2.6 — Tooling / paper-cut cleanup (legacy DEF tooling rows + A.28a)
> **Goal:** Pre-existing dev-experience papercuts (ruff cleanup, DB-test gating, NAAVIK_PERSISTENCE removal) + post-A.28 script hardening.
> **Status:** 🟡 Queued.
> **Plan:** Per-row plans authored when scheduled.

| # | Task | Status | Priority | Legacy ID | Notes |
|---|---|---|---|---|---|
| 0.2.6.06 | Pre-existing ruff errors in `migrations/` + `.claude/naavik_ops/lib/roadmap.py` (UP007 / UP035 / I001) | [x] | MEDIUM | DEF-24 | **Plan 55 EXECUTED 2026-05-20 via PR #155.** `ruff check --fix .` applied to `migrations/versions/0001_initial.py` + `migrations/versions/0002_settings_multi_users.py` + `.claude/naavik_ops/lib/roadmap.py` + `.claude/naavik_ops/plan.py`. Scope expanded by 3 SIM rules (SIM108/103/105) in `.claude/naavik_ops/` per plan-blessed inclusion clause. 11 violations → 0. |
| 0.2.6.07 | DB-test gating gap — 11 test files lack `_skip_if_no_db()` (asyncpg `InvalidPasswordError` on `localhost:5432`) | [x] | MEDIUM | DEF-25 | **Plan 55 EXECUTED 2026-05-20 via PR #155 — SHIPPED AS NO-OP.** Default-env `pytest -x` already green (1212/26) — prior PRs (likely 0.2.0.07b lint fix or DB-test gating already shipped indirectly). Engineer verified via direct `pytest` run; no failures from the 11-file gap manifested. |
| 0.2.6.08 | `scripts/A.28-board-restructure.sh` hardening — eval pattern + rollback doc + apply default | [x] | — | A.28a | **Plan 55 EXECUTED 2026-05-20 via PR #155.** All 3 plan fixes applied: (a) `eval` → arg-array `"$@"` (9 callers rewritten); (b) 4-line rollback contract header comment added; (c) `DRY_RUN=true` flipped to file-level default; explicit `--apply` required. Plus engineer caught pre-existing REPO_ROOT path-resolution bug (`/..` → `/../..`) load-bearing post-A.29 file relocation. New `tests/test_a28_script_shape.py` covers 7 invariants. |
| 0.2.6.09 | Backup / disaster-recovery docs for self-hosters — `.env` + `~/.naavik/data/snapshots/` off-site backup story | [x] | LOW | (release-readiness audit 2026-05-20) | **Plan 55 EXECUTED 2026-05-20 via PR #155.** New `## Backups + disaster recovery` section in `docs/DEPLOYMENT.md` covering canonical backup set (`.env` + `~/.naavik/data/snapshots/`), off-site rotation examples (restic, borgbackup, S3), recovery walkthrough, `chmod 0600 .env` invariant, vault-deleted note. RUNBOOK.md pointer added; POST_PHASE_1 #7 strikethrough with pointer back. |

**Deliverable (0.2.6):** Repository-wide tooling cleanup; pre-existing CI/test gaps closed; script hardening complete; backup story documented.

---

### 0.2.7 — Deferred-from-0.2.x catch-all
> **Goal:** Catch-all for items deferred during the 0.2.0–0.2.6 sequential sweep (2026-05-20). Themes: multi-tenant cloud features (real Phase 2+ scope), Phase 6 polish (light mode, semantic match, ML calibration), multi-week ATS adapter work (Workday/LinkedIn/Indeed/Generic), Phase 4 auto-apply refinements, and small optional polish that didn't pull weight in 0.2.x release-version contexts.
> **Status:** ✅ Complete (2026-05-21) — design-heavy sweep shipped 0.2.7.07/10/11/14/16 via PRs #162/#163/#164/#165; remaining small follow-ups (0.2.7.13a/13b/17a/24/25/26) deferred to 0.8.0.33-38 per user directive on 2026-05-21.
> **Plan:** Per-row plans authored when picked up.
>
> **Why 0.2.7 vs 0.7.0?** 0.7.0 is the agent-system follow-up pickup queue (post-Phase-2 quality-of-life on the dev process itself). 0.2.7 is the PRODUCT-side catch-all for items deferred from the 0.2.x cleanup sweeps that aren't agent-system work. Keeps the two surfaces distinct: 0.7.0 = how we ship; 0.2.7 = what we ship later.

| # | Task | Status | Priority | Legacy ID | Notes |
|---|---|---|---|---|---|
| 0.2.7.01 | Apply `JobRead.model_validate` projection to discover.py callsites — close pre-emptive `raw_meta` leak vectors at `src/ui/routes/discover.py:256/271/282/287/292` | [x] | LOW | (hacker PR #146 NOTE-1) | **Plan 57 EXECUTED 2026-05-20 via PR #158.** `JobRead.model_validate(job).model_dump(mode='json')` projection applied to 5 discover.py callsites. 5 regression tests assert `raw_meta not in response`. Pre-dispatch ship (commit `03b7c49`). |
| 0.2.7.02 | Thread real `user.id` into `_build_sources_view` instead of hardcoded `user_id=1` (Sources panel latent IDOR) | [x] | MEDIUM | (hacker PR #149 MED-1) | **Plan 56 EXECUTED 2026-05-20 via PR #156** — **Filed 2026-05-20 via PR #149 hacker review.** `src/ui/routes/settings.py:290-291` calls `settings_service.get_or_create(user_id=1)` + `list_recent_scrape_runs_by_source(user_id=1)`. `_user` dep is collected but never threaded. Not exploitable in Phase 1 single-user MVP; becomes cross-user info-disclosure on multi-user. Fix: thread `_effective_user_id(user)` per existing `src/ui/routes/jobs.py:30-44` pattern. ~5 LOC + 2 tests. Couples with the fake-session retirement multi-PR refactor (same as 0.2.7.01). |
| 0.2.7.03 | Silent `session is None → []` fallthrough in `_build_sources_view` — fail-loudly defense-in-depth | [x] | LOW | (hacker PR #149 LOW-2) | **Plan 56 EXECUTED 2026-05-20 via PR #156** — **Filed 2026-05-20 via PR #149 hacker review.** `src/ui/routes/settings.py:285-288` returns empty list when `Depends(get_session)` returns None. Production unreachable today; future refactor dropping the dep would render empty panel instead of erroring loudly. Replace fallthrough with explicit raise. ~3 LOC + 1 test. |
| 0.2.7.04 | README + Workday popover env-var asymmetry — popover says `WORKDAY_COMPANIES=…` but cron reads `Settings.workday_companies` per-user DB | [x] | LOW | (hacker PR #149 NOTE-1) | **Plan 56 EXECUTED 2026-05-20 via PR #156** — **Filed 2026-05-20 via PR #149 hacker review.** README "Operator workflow" step 2 + Workday `<details>` popover advertise env-var syntax but `Workday._compose_query` actually reads DB `Settings.workday_companies` (per-user). Operator following the popover hits no-op. UX bug — non-security. Either (a) wire env-var → Settings table seed during boot OR (b) amend popover + README to clarify the DB-stored config path. ~10 LOC docs + 1 test. |
| 0.2.7.05 | SCREENS.md § 11 — Sources sub-tab text still describes pre-rewrite scope (enable/disable/schedule); needs alignment to plan 49 `_source_row.html` contract | [x] | LOW | (architect PR #149 LOW-1) | **Plan 56 EXECUTED 2026-05-20 via PR #156** — **Filed 2026-05-20 via PR #149 architect review.** `docs/design/SCREENS.md` § 11 line 629 still has pre-rewrite description ("scraping source list with enable/disable/schedule"). `SOURCES_UI.md § I` points at SCREENS.md but reverse pointer missing. Cosmetic doc surgery: rewrite § 11 to match plan 49's actual contract + add forward-pointer from SOURCES_UI.md back. ~30 LOC docs. Could fold into 0.2.5.05/06 work if scheduled. |
| 0.2.7.06 | Settings · Rate-limit JSONB editor + LinkedIn/Indeed keywords editor (paired UI editors deferred from 0.2.5.05 + 0.2.5.06) | [x] | LOW | (plan 49 OQ.3 lock 2026-05-20) | **Plan 58 EXECUTED 2026-05-20 via PR #159.** Paired UI editors (rate-limit JSONB + LinkedIn/Indeed keywords) inline within Sources tab `<details>` popovers (Option C — no new routes). New partials `_rate_limit_editor.html` + `_keywords_editor.html`; `_source_row.html` rewrite swaps read-only → writable forms. `put_sources` form-encoded handler with CSRF + IDOR via `_effective_user_id` + flat-field unpack + `RateLimitConfig` Pydantic validation. 12 tests in `tests/test_paired_editors.py`. SOURCES_UI.md + COMPONENTS.md doc bumps. Reviewers: hacker APPROVE 0 findings; architect APPROVE 0 findings. |
| 0.2.7.07 | Multi-tenant JWT signing-key rotation | [x] | — | DEF-13 | **Plan 62 EXECUTED 2026-05-21 via PR #163** (re-scoped from "multi-tenant" → "single-tenant infrastructure; multi-tenant resolver deferred to 0.8.0" per hacker path b). New `src/models/tenant.py` + `src/models/tenant_signing_key.py` SQLModels (RS256 keypair per D2; kid-tagged; ACTIVE/RETIRING/RETIRED enum per D8). Alembic 0014 (tables + env-legacy backfill from `SECRET_KEY`) + 0015 (partial unique index `WHERE status='ACTIVE'` per MED-1 fix). New `src/services/jwt_rotation_service.py` (RSA generation + rotation lifecycle + `ensure_active_key` guard per LOW fix). `src/services/auth.py` async kid-aware signer/verifier + legacy fallback. New `/settings/security` tab + `POST /api/v1/settings/security/rotate-jwt-key` (CSRF + IDOR via `_effective_user_id`; `_SELF_HOST_TENANT_ID=1` constant per HIGH-3 fix). APScheduler `admin.expire_retiring_signing_keys` nightly grace-expiry. 38 new tests (1470 total). Reviewers (round 1): hacker REQUEST_CHANGES (3 HIGH 2 MED 1 LOW); architect APPROVE_WITH_NOTES (1 HIGH 1 MED 2 INFO; 9/10 spec match). Reviewers (round 2 delta): both APPROVE 0 findings. 6 follow-ups filed (0.8.0.11-16). |
| 0.2.7.10 | Workday / LinkedIn / Indeed / Generic ATS adapters | [x] | — | DEF-01 | **Plan 63 EXECUTED 2026-05-21 via PR #164** — SCAFFOLD-ONLY per architect D.11 lock + user "defer→0.8.0" directive. New `src/services/ats/` extension: `_browser_pool.py` (ATSBrowserPool singleton + Semaphore(4)) + `_playwright_base.py` + 4 stub adapters (`workday.py`, `linkedin_apply.py`, `indeed.py`, `generic.py`) — all `can_submit() → False` + `submit() → FAILURE_AUTH_REQUIRED` envelope (inert). Generic adapter wires `tracked_call(prompt_name="ats_generic_form_fill")` w/ `extra="forbid"` Pydantic schema. New `src/llm/prompts/ats_generic_form_fill.py`. Postmortem extended w/ `raw["screenshot_b64"]` flow (atomic write; inherits 0.2.3.02 path-traversal gauntlet). Alembic 0016 adds `Settings.auto_apply_adapter_confidence_threshold` (default 0.7). 4 new env slots: `WORKDAY_LOGIN_TOKEN` / `LINKEDIN_SESSION_COOKIE` / `INDEED_SESSION_COOKIE` / `ATS_GENERIC_LLM_CONFIDENCE_THRESHOLD`. Settings UI presence indicators via `env_secrets.py`. 90 new tests (1560 total). Reviewers: architect APPROVE_WITH_NOTES (1 MED on D.5 enforcement gap + 1 LOW; 11/11 spec match); hacker APPROVE_WITH_NOTES (5 LOW). 12 follow-ups filed (0.8.0.17-28). Per-adapter implementations Workday→0.4.0.24; LinkedIn/Indeed/Generic-production→0.8.0.25-27. |
| 0.2.7.11 | LinkedIn proxy support | [x] | — | DEF-15 | **Plan 64 EXECUTED 2026-05-21 via PR #165** (round 1 + delta — hacker REQUEST_CHANGES 2 HIGH proxy-creds-in-exception leaks; delta inline fix applied + both reviewers APPROVE round 2). New `src/scraper/proxy.py` (`ProxyURLConfig` provider-agnostic HTTP/SOCKS5; `resolve_proxy_config` env-driven; `safe_proxy_host` redaction helper). Extended `src/scraper/redaction.py` (new `safe_exc` chain walker + `safe_msg` URL-strip + pre-existing `safe_url` userinfo-leak FIX shipped same-PR). `Crawl4AIClient` accepts `proxy_config` via `CrawlerRunConfig.__init__`. LinkedIn scraper wires proxy + fails LOUDLY on proxy error per D.6. `JobScrapeRun.raw_meta["proxy"] = {used, provider_hint}` telemetry per D.9. New env: `LINKEDIN_PROXY_URL` + `LINKEDIN_PROXY_PROVIDER_HINT`. Settings UI presence indicator. Engineer SHIPPED `docs/design/LINKEDIN_PROXY.md` design-doc graduation (positive deviation — first of 4 to graduate inline). 81 new tests (1641 total). Reviewers (round 1): hacker REQUEST_CHANGES (2 HIGH 1 MED 2 LOW); architect APPROVE 11/11 spec match. Reviewers (round 2): hacker APPROVE_WITH_NOTES (1 partial LOW); architect APPROVE_WITH_NOTES (1 LOW). 4 follow-ups filed (0.8.0.29-32). |
| 0.2.7.12 | Auto-apply immediate dispatch on right-swipe | [x] | — | DEF-10 | **Plan 59 EXECUTED 2026-05-20 via PR #160.** Option D shipped: `Settings.auto_apply_immediate_dispatch BOOLEAN NOT NULL DEFAULT FALSE` (alembic 0011) + right-swipe handler schedules one-off `add_job(DateTrigger=now, id='auto-apply-immediate-{app_id}-{uuid8}', func='scheduler.jobs:auto_apply')` when toggle enabled. HTMX tri-state idiom for the new toggle. 5 tests. `FUNC_REF_ALLOWLIST` not modified (`auto_apply` already there). Default OFF preserves 5-min cron behavior. Reviewers: hacker APPROVE_WITH_NOTES (2 LOW non-blocking notes on PRE-EXISTING gaps — filed as `0.2.7.25` CSRF on put_auto_apply/put_llm/put_notifications + `0.2.7.26` IDOR via `user_id=1` hardcode on same 3 routes); architect APPROVE 0 findings. |
| 0.2.7.13 | Onboarding offline retry buffer for autosave | [x] | — | DEF-07 | **Plan 57 EXECUTED 2026-05-20 via PR #158.** New `src/ui/static/offline_queue.js` (~210 LOC) — IndexedDB-backed retry queue for `/onboarding*` autosave. Path-gated server-side; prefix-allowlist for replay; 5-retry exponential backoff; `naavik:offline-queue-drop` CustomEvent. 14 tests. Browser-side IndexedDB tests deferred to `0.2.7.13a`. |
| 0.2.7.14 | `ProfileAnswer` reuse cache | [x] | — | DEF-09 | **Plan 61 EXECUTED 2026-05-20 via PR #162** (combined w/ 0.2.7.16). New `src/models/profile_answer.py` SQLModel keyed by `(user_id, question_fingerprint)`; new `src/services/profile_answer_service.py` w/ exact-normalized fingerprint (lowercase+strip-punct+remove-company+Porter-stem+SHA-1) per D6; new `src/api/profile_answer.py` route (CSRF gap on `POST /accept` filed as `0.8.0.04`); new `src/ui/templates/components/_profile_answer_diff.html` prompt-with-diff partial (D7 — never autofill). Alembic 0012. 11 new tests. Reviewers: architect APPROVE_WITH_NOTES (1 HIGH DATA_MODEL.md graduation missing — filed as `0.8.0.03`); hacker APPROVE_WITH_NOTES (1 MED CSRF + 1 LOW lint + 1 INFO poison-vector future risk). |
| 0.2.7.15 | Portfolio API versioning | [x] | — | DEF-12 | **Plan 57 EXECUTED 2026-05-20 via PR #158.** `GET /api/portfolio/cv` accepts `?version=v1` via Pydantic `Literal["v1"]`; default v1; unknown→422. 5 tests. |
| 0.2.7.16 | `JobEmbedding` semantic match | [x] | — | DEF-14 | **Plan 61 EXECUTED 2026-05-20 via PR #162** (combined w/ 0.2.7.14). New `src/models/job_embedding.py` sibling table `job_embedding(job_id PK FK)` w/ pgvector `vector(768)` + HNSW cosine index `m=16, ef_construction=200` per D4+D5. New `src/services/embedding_service.py` (similarity search + batch embed); `LLMProvider.embed(text) -> list[float]` ABC extension (openai/ollama implement; anthropic raises). Alembic 0013. APScheduler nightly batch `embed_pending_jobs` 02:00 UTC + `embed_orphan_sweep` 03:00 UTC. New `Settings.embedding_provider` + `semantic_match_threshold` (0.65 default per D9) + `semantic_match_sync_on_upsert` (default OFF — wire to upsert filed as `0.8.0.06`) + 4th column shipped vs 3 planned. 34 new tests. New `pgvector>=0.4.0,<0.5` dep; zlib added to nix LD_LIBRARY_PATH (engineer dev 1 — surface propagated to flake.nix + nix/devshell.nix). |
| 0.2.7.17 | NAAVIK_PERSISTENCE env-var full removal | [x] | — | DEF-23 | **Plan 60 EXECUTED 2026-05-20 via PR #161** (manager-direct merge, no reviewer pair per budget cap). `NAAVIK_PERSISTENCE` env-var fully REMOVED from `flake.nix` + `nix/devshell.nix` + `.env.example` + `README.md` + `docs/RUNBOOK.md` + `docs/ARCHITECTURE.md` + `CLAUDE.md`. 18 `_is_db_mode()` dual-path branches collapsed in `src/db/sample_data.py`. New service modules `outreach_service` + `email_service` + `overview_service` + `user_service` + 23 new fns. New regression lint `test_no_sample_data_imports_in_routes.py` (with `_KNOWN_LEGACY_OFFENDERS` allowlist tracking 13 files needing route migration). 20 new parity tests. 1110 tests pass. **Significant deviation:** plan's commit-2 (full route+ctx rewire across 8 routes + 5 ctx-builders + 14 test-file `dependency_overrides[get_session]` fixture work) was descoped from PR #161 — routes still call `sd.*` accessors which now always read in-memory (no env gate). Operator-facing goal (env-var removal) achieved; route migration filed as `0.2.7.17a` follow-up. |
| 0.2.7.18 | Postmortem `_redact()` walks VALUES not just KEYS — Bearer/JWT/Set-Cookie patterns in scraper-controlled HTTP error bodies leak into `trace.json` + LLM prompt | [x] | MEDIUM | (hacker PR #152 MED-1) | **Plan 56 EXECUTED 2026-05-20 via PR #156** — **Filed 2026-05-20 via PR #152 hacker review.** Current `_redact()` walks dict keys but `response_body_excerpt` is a raw string — ATS error bodies can contain echoed auth headers (rare but observed). Fix: value-shape regex pass for Bearer/JWT/`Set-Cookie:`/`Authorization:` BEFORE the 32KB truncate. ~30 LOC + 4 tests. Defer until first board reports leakage OR alongside 0.2.7.10 (multi-adapter expansion). |
| 0.2.7.19 | Twin CSRF gaps at `discover.post_job_by_url` + `tracking.post_manual_application` — extend hacker PR #153 HIGH-1 fix pattern | [x] | MEDIUM | (hacker PR #153 OUT-OF-SCOPE note) | **Plan 56 EXECUTED 2026-05-20 via PR #156** — **Filed 2026-05-20 via PR #153 hacker review (out-of-scope note).** Pre-existing twin endpoints lack `Depends(require_csrf)` — same vector as the PR #153 HIGH-1 fix on `/api/v1/jobs/manual`. Pattern is now established; one-line fix per endpoint + tests. ~10 LOC + 4 tests. |
| 0.2.7.20 | `/settings/llm-provider` unauthed read — add `Depends(require_authed_session)` to match Submissions/Sources siblings | [x] | LOW | (hacker PR #154 LOW-1) | **Plan 56 EXECUTED 2026-05-20 via PR #156** — **Filed 2026-05-20 via PR #154 hacker review.** Dedicated `/settings/llm-provider` route (PR #154 engineer deviation #2) lacks auth dep; mirrors pre-existing unauthed `/settings/{tab}` catch-all. Aggregate `today_cost_usd` + `cost_cap_usd` readable unauthed. No secrets but inconsistent with Submissions/Sources auth posture. ~3 LOC + 1 test. Could fold into broader Settings auth-tightening row. |
| 0.2.7.21 | SOURCES_UI.md + SCREENS.md doc gaps from plan 54 — 0.2.5.04 history table location vs. plan-49-anticipated per-row link; SCREENS.md still says "6 tabs" not 7 | [x] | LOW | (architect PR #154 LOW+INFO) | **Plan 56 EXECUTED 2026-05-20 via PR #156** — **Filed 2026-05-20 via PR #154 architect review.** Cosmetic doc surgery: (a) `docs/design/SOURCES_UI.md:7` anticipated 0.2.5.04 would add "View history → link from each row"; impl appended single table at tab bottom; doc needs ~5-LOC update. (b) `docs/design/SCREENS.md:575-576` still says "6 tabs"; now 7 (submissions added). Could fold with 0.2.7.05. |
| 0.2.7.22 | Pre-existing `ruff format --check` misses in `tests/test_naavik_ops/{test_plan,test_roadmap}.py` | [x] | LOW | (architect PR #155 LOW) | **Plan 56 EXECUTED 2026-05-20 via PR #156.** `uv run ruff format` applied; scope expanded to include `tests/test_a28_script_shape.py` to close `ruff format --check .` cleanly. |
| 0.2.7.23 | Sibling helpers in `settings.py:228/236/244` still have `user_id=1` hardcoded — identical-shape IDOR pattern to 0.2.7.02 | [x] | MEDIUM | (hacker PR #156 OBSERVATION) | **Plan 57 EXECUTED 2026-05-20 via PR #158.** 3 sibling helpers now require `*, user_id: int` keyword-only. Routes thread `_effective_user_id(_user)`. 9 tests. Catch-all `/settings/{tab}` still has hardcode — filed as `0.2.7.24`. |
| 0.2.7.13a | Playwright IndexedDB round-trip test for offline_queue.js | — | — | — | **MOVED 2026-05-21 → 0.8.0.33** per user "defer rest of 0.2.x not done → 0.8.0" directive. |
| 0.2.7.13b | Re-read `meta[name=csrf-token]` at offline-queue replay time | — | — | — | **MOVED 2026-05-21 → 0.8.0.34.** |
| 0.2.7.24 | Catch-all `/settings/{tab}` still uses hardcoded `user_id=1` | — | — | — | **MOVED 2026-05-21 → 0.8.0.35.** |
| 0.2.7.25 | Pre-existing CSRF gap on `put_auto_apply` + `put_llm` + `put_notifications` | — | — | — | **MOVED 2026-05-21 → 0.8.0.36.** |
| 0.2.7.26 | Pre-existing IDOR via `user_id=1` hardcode on `put_auto_apply` + `put_llm` + `put_notifications` | — | — | — | **MOVED 2026-05-21 → 0.8.0.37.** |
| 0.2.7.17a | Route + ctx rewire — migrate 13 files off `_KNOWN_LEGACY_OFFENDERS` allowlist | — | — | — | **MOVED 2026-05-21 → 0.8.0.38.** |

**Deliverable (0.2.7):** ✅ Complete (2026-05-21). All in-cycle rows shipped or deferred to 0.8.0. Catch-all closed: design-heavy sweep (0.2.7.07/10/11/14/16) + small-batched (0.2.7.01-23) + paired editors (0.2.7.06) + auto-apply immediate (0.2.7.12) + offline retry (0.2.7.13) + portfolio API versioning (0.2.7.15) + persistence env-var removal (0.2.7.17). 6 small follow-ups deferred to 0.8.0.33-38.

---

### 0.3.0 — Scoring substrate (legacy Phase 3 cut 1 — CRITICAL; unblocks 0.4.0 auto-apply)
> **Goal:** Hybrid layered scoring (tag overlap → semantic cosine → LLM-as-judge) writes `Job.score` + `Job.score_explanation` + `Job.match_breakdown` for every job in the queue. Cost-cap-aware with graceful fallback to layers 2-3 composite.
> **Status:** ✅ Complete (2026-05-21) — all 6 rows shipped via PR #166 (plan 65). Reviewers: architect APPROVE_WITH_NOTES 18/18 spec match (1 LOW + 1 INFO cosmetic); hacker APPROVE_WITH_NOTES (3 MED + 1 LOW filed as 0.3.3.01-04). 4 doc graduations landed in-PR (BACKEND.md + DATA_MODEL.md + SCREENS.md + JOB_MODEL.md).
> **Plan:** `docs/plans/archive/65-0.3.0-scoring-substrate.md` (EXECUTED 2026-05-21).
> **Implementation contract:** `docs/design/BACKEND.md` § H.1 (`scorer` service), § M.3 (`score_job` prompt). DATA_MODEL.md § C (`Job.score`, `Job.score_explanation`, `Job.match_breakdown`). Re-uses 0.2.7.16 `JobEmbedding` substrate. New `ProfileEmbedding` sibling table.
> **Estimated effort:** ~6 dev-days · ~1500 LOC across 6 rows.
> **Locked decisions (2026-05-21 user lock-in post-research memo):** OQ-1 hybrid layered · OQ-2 LLM tier user-configurable via `Settings.llm_provider` (default Claude Sonnet 4.6 on cloud; skip layer 4 if no provider configured) · OQ-3 LinkedIn MCP deferred to `0.5.0.12.1` · OQ-4 3-sub-release cut · OQ-5 silent fallback + banner on Settings · LLM tab · OQ-6 on-edit + nightly idempotent refresh · OQ-7 1-2 sentence + 3-5 gap bullets for Discover card; structured `{strengths, gaps, visa_note}` for full match modal.

| # | Task | Status | Priority | Legacy ID | Notes |
|---|---|---|---|---|---|
| 0.3.0.01 | Visa filter consolidation + Discover `visa_concern` chip wiring | [x] | HIGH | 3.3 | **Plan 65 EXECUTED 2026-05-21 via PR #166.** `src/services/scorer/visa.py` extracted from legacy `scorer.py`; layer-1 of orchestrator. `swipe_card.html` renders visa chip when triggered. `SCREENS.md § 7` visa-chip line graduated. |
| 0.3.0.02 | Tag-based matching layer (weighted overlap, asymmetric) | [x] | HIGH | 3.1 | **Plan 65 EXECUTED 2026-05-21 via PR #166.** `src/services/scorer/tag_layer.py` ships T3 weighted-overlap formula (favors profile-coverage of job tags). `Settings.score_per_dim_weights` JSONB column (validator clamps `[0, 3]`; unknown keys dropped). |
| 0.3.0.03 | Profile embedding sibling table + service | [x] | HIGH | (new) | **Plan 65 EXECUTED 2026-05-21 via PR #166.** New `src/models/profile_embedding.py` (mirrors `JobEmbedding` shape; vector(768) HNSW cosine m=16/ef_construction=200; PK keyed by `user_id`). `embedding_service.embed_profile` + `search_similar_profile_bullets`. Alembic 0017. `embed_pending_profiles` nightly 02:00 UTC cron. On-edit hook in `profile_service.py`. DATA_MODEL.md § B entity 22→23. |
| 0.3.0.04 | Semantic scoring layer (cosine + ProfileEmbedding lookup) | [x] | HIGH | (new) | **Plan 65 EXECUTED 2026-05-21 via PR #166.** `src/services/scorer/semantic_layer.py` ships pgvector cosine + T4 `_TAG_FLOOR = 0.10` gate. T5 composite `0.4 × tag + 0.6 × semantic ≥ 0.50` to promote to layer 3. |
| 0.3.0.05 | LLM-as-judge layer + `JobScore` schema expansion | [x] | CRITICAL | 3.2 | **Plan 65 EXECUTED 2026-05-21 via PR #166.** `src/services/scorer/llm_judge.py` ships LLM-judge with T1 per-job cost-cap pre-flight probe + graceful skip when no LLM provider configured (returns None → composite layers 1-3 returned with `judge_skipped=true`). Extended `JobScore` Pydantic: `per_dimension`/`strengths`/`gaps≤5`/`suggested_bullets≤8`/`visa_concern`/`visa_note`/`layers_run`/`judge_skipped`/`layer_4_provider`. Hallucinated-bullet-ID defense via `_filter_valid_bullet_ids` (JOINs through `Experience.profile_id`). |
| 0.3.0.06 | Score orchestrator + `jobs.score_pending` cron wiring | [x] | CRITICAL | (new) | **Plan 65 EXECUTED 2026-05-21 via PR #166.** `src/services/scorer/orchestrator.py` ships `score_job_layered(job_id, session, source_trust_weight=1.0)`. `Job.match_breakdown` 18-key JSONB with `schema_version=1`. T9 `source_trust_weight` forward-compat seam to 0.8.0.42. T10 3 re-scoring triggers: `jobs.score_pending` (15min) + `score.recompute_stale` (nightly 03:30 UTC) + `POST /api/v1/jobs/{id}/rescore` (promoted 501→real-auth: CSRF + IDOR via `_effective_user_id` + `_job_or_404`). T11 `scorer/` package w/ backward-compat `__init__.py` lazy `__getattr__` re-exports. Visa filter re-applied post-LLM (belt-and-suspenders). |

**Deliverable (0.3.0):** Every job in the queue scored with per-dimension breakdown + explanation persisted to `Job.match_breakdown`; LLM-judge gated by daily cost cap with graceful fallback; UI consumer (`SCREENS.md § 7`) ready to render `MATCH · 0.86`.

---

### 0.3.1 — Tailored resume + cover letter FREE-tier (CRITICAL; depends on 0.3.0.05+.06)
> **Goal:** Ship the FREE-tier resume + cover letter generation pipeline — voice-grounded constitution preamble (corpus + ProfileAnswer + summary) + ATS-friendly Typst (PDF/A-1b + new `onepage_ats.typ`) + recruiter-priority headline + adaptive cover letter + ethics pre-flight + page-valid retry-drop. Survives parsing across 9 ATS systems (>85% field-extraction fidelity); evades the 5 major AI-detector classifiers via voice grounding (the highest-ROI lever — it's truth, not evasion). Cost: ~$0.06-0.12/app at Sonnet 4.6 + prompt caching.
> **Status:** ✅ Complete (2026-05-21) — all 6 rows shipped via PR #167 (plan 66). Reviewers: architect REQUEST_CHANGES round 1 (HIGH constitution-dead-code) → delta APPROVE round 2 24/24 spec match; hacker APPROVE_WITH_NOTES (4 LOW; 3 fixed inline, 1 → 0.3.3.06).
> **Plan:** `docs/plans/archive/66-0.3.1-free-tier-generation.md` (EXECUTED 2026-05-21).
> **Locked decisions (2026-05-21 user lock-in post-synthesized memo):** OQ-1 2-tier FREE/PREMIUM (this row = FREE) · OQ-2 council N=3 (PREMIUM-only) · OQ-3 tool-loop cap N=3 (PREMIUM-only) · OQ-4 hybrid Claude-as-detector + Originality.ai spot-check (PREMIUM-only) · OQ-5 adaptive cover letter format (Claude picks per JD) · OQ-6 TIER-2 evasion all opt-in via Settings · Advanced · OQ-7 smart-default parse-fidelity tiers (>90% silent / 75-90% toast / <75% loop+surface) · OQ-8 2 templates (`onepage.typ` + new `onepage_ats.typ`; auto-select by `Application.board`) · OQ-9 `Settings.ai_writing_voice_samples` field (no profile migration) · OQ-10 graceful skip with warning on cost-cap exhaustion.
> **Estimated effort:** ~6 dev-days · ~1850 LOC across 6 rows.

| # | Task | Status | Priority | Legacy ID | Notes |
|---|---|---|---|---|---|
| 0.3.1.01 | Bullet selection pipeline + JobScore integration + corpus voice grounding | [x] | HIGH | 3.4 | **Plan 66 EXECUTED 2026-05-21 via PR #167** (round 1 + delta). Wires `select_bullets_sota.py` end-to-end via `bundle_generator`. Uses `JobScore.suggested_bullets` + `selection_override` + tag-rank fallback. Voice corpus assembled from 5 sources (Bullet + Profile + ProfileAnswer + Settings.ai_writing_voice_samples + past trimmed_lines) cached via Anthropic prompt-caching `cache_system=True`. Constitution preamble threaded into LLM `system=` kwarg per delta fix. |
| 0.3.1.02 | Bullet trim w/ voice grounding + burstiness target + AI-tell blocklist | [x] | HIGH | (new) | **Plan 66 EXECUTED 2026-05-21 via PR #167.** `trim_bullet_sota.py` w/ constitution + corpus + burstiness target. New `ai_tell_blocklist.py` 30-entry list + em-dash regex; `strip_violations` invoked post-LLM. New `burstiness_validator.py` std-dev check threshold 6 (regen path deferred to 0.3.3.05). |
| 0.3.1.03 | ATS-friendly Typst template variant (`onepage_ats.typ`) + parse-fidelity validator | [x] | HIGH | (new) | **Plan 66 EXECUTED 2026-05-21 via PR #167.** New strict-single-column template — plain `•` bullets, MM/YYYY dates, no header/footer, PDF/A-1b output, ligatures-disabled Helvetica. New `services/ats_parser_fidelity.py` (pdfplumber heuristic + 8-field round-trip score; test corpus at `tests/data/ats_corpus/`). Smart-default fail-mode per OQ-7. Auto-select template by `Application.board`. pdfplumber promoted from transitive to base dep. |
| 0.3.1.04 | Recruiter-priority headline + summary generator + keyword coverage validator | [x] | HIGH | (new) | **Plan 66 EXECUTED 2026-05-21 via PR #167.** New `services/recruiter_optimization.py` + `llm/prompts/tailor_headline.py` — LLM-generated 1-line pitch (title + years + specialty + sponsorship signal — 10.6× interview-signal per research). Gated on `JobScore.score ≥ 0.50`; 100-char cap via Pydantic `mode="before"` validator. New `services/keyword_coverage.py` top-30% scan + 0.75 threshold + one retry. |
| 0.3.1.05 | Cover letter SOTA (Hook/Match/Close + voice grounding + adaptive format + hiring-manager personalization) | [x] | HIGH | 3.5 | **Plan 66 EXECUTED 2026-05-21 via PR #167** (delta switched `document_generator.generate_cover_letter` from old `draft_cover_letter` to new `draft_cover_letter_sota` prompt). Voice-grounded via `cache_system=True` constitution preamble. Adaptive format (Claude picks Pain-Letter vs Standard per JD pain-point verbiage). New `services/hiring_manager_extractor.py` (bounded regex + LLM fallback wrapped via `tracked_call`). Backward-compat: old `draft_cover_letter.py` retained as deprecated wrapper. |
| 0.3.1.06 | One-click generation endpoint + Discover wiring + page-count retry loop + ethics pre-flight | [x] | HIGH | (new) | **Plan 66 EXECUTED 2026-05-21 via PR #167** (delta added CSRF + `_effective_user_id` IDOR + `_app_or_404`). `POST /api/v1/applications/{id}/generate-bundle` ships with `Depends(require_csrf)` + `Depends(require_authed_session)`. Ethics pre-flight drops bullets w/ unsupported claims (max 2 dropped → 422). Cost-cap mid-flight probe between stages → graceful skip + `degraded_mode` flag (OQ-10). Citation-grounded audit trail in new `Application.generation_trace` JSONB (alembic 0018; 18-key schema w/ `schema_version=1`). 5 new Settings columns w/ `max_length=5000` on `ai_writing_voice_samples`. Discover HTMX two-step wiring deferred to 0.3.3.07. NEW canonical `docs/design/RESUME_GENERATION.md` design doc (373 lines) graduated in-PR + 4 cross-refs (BACKEND.md + DATA_MODEL.md ×2 + SCREENS.md). Reviewers: round 1 architect REQUEST_CHANGES (1 HIGH constitution dead-code + 1 MED cost-math); round 2 delta architect APPROVE 24/24 spec match. Round 1 hacker APPROVE_WITH_NOTES (4 LOW); delta added CSRF + max_length + tracked_call wrap fixing 3 of 4 inline. |

**Deliverable (0.3.1):** One-click from Discover swipe to ATS-passable PDF resume + cover letter, voice-grounded, parse-validated across 9 ATS, page-valid via retry-drop, citation-grounded audit. Cost ~$0.06-0.12/app.

---

### 0.3.4 — PREMIUM-tier Claude-mythos generation (MEDIUM; opt-in via Settings · Generation; depends on 0.3.1.06)
> **Goal:** PREMIUM-tier opt-in adds Claude-mythos force-multiplier layer on top of FREE substrate: adversarial detector loop + 3-agent voting council + multi-persona recruiter critique + tool-loop orchestrator + ATS parser ensemble + Settings UI tier-toggle. Cost: ~$0.40-1.20/app. User toggles per-app for high-priority jobs (score > 0.85) OR globally in Settings · Generation.
> **Status:** ✅ Complete (2026-05-21) — all 6 rows shipped via PR #168 (plan 67). Reviewers: round 1 hacker REQUEST_CHANGES (HIGH API-key-wipe + 2 MED + 1 LOW) + architect APPROVE_WITH_NOTES (HIGH ats-ensemble-unwired + 2 INFO; 15/16 spec); round 2 delta both APPROVE_WITH_NOTES (16/16 spec; all 4 blockers closed; dead-code-recurrence guard test asserts orchestrator code-path). NEW canonical `RESUME_GENERATION.md § Q-T` + 3 cross-refs in-PR. Closes 0.3.3.07 inline.
> **Plan:** `docs/plans/archive/67-0.3.4-premium-mythos.md` (EXECUTED 2026-05-21).
> **Estimated effort:** ~6 dev-days · ~1700 LOC across 6 rows.

| # | Task | Status | Priority | Legacy ID | Notes |
|---|---|---|---|---|---|
| 0.3.4.01 | Adversarial detector loop (Claude-as-detector + refine) + hybrid real-detector spot-check | [x] | MEDIUM | (new) | **Plan 67 EXECUTED 2026-05-21 via PR #168** (round 1 + delta). `services/detector_loop.py` runs Claude-as-detector per bullet + cover letter; refines if flag-confidence > 0.25; max 3 iterations w/ budget-aware exit. Hybrid: `llm/providers/originality.py` at convergence when `Settings.originality_api_key` set; graceful degrade to Claude-only. 64KiB response cap per delta MED-2 fix. |
| 0.3.4.02 | 3-agent voting council for bullet selection (Borda count, batch API) | [x] | MEDIUM | (new) | **Plan 67 EXECUTED 2026-05-21 via PR #168.** `services/council.py` — 3 heterogeneous personas (Pragmatic recruiter + Hiring manager + Cultural-fit assessor). Borda count merge w/ lower-bullet-id tie-break per T3. Anthropic batch API via `messages.batches.create` (50% cost reduction); `custom_id` correlation prevents persona-confusion. |
| 0.3.4.03 | Multi-persona recruiter critique council | [x] | MEDIUM | (new) | **Plan 67 EXECUTED 2026-05-21 via PR #168.** `services/critique_council.py` — 3 reviewers (FAANG L5/L6 + startup founder + Fortune-500 HR). difflib `SequenceMatcher` ≥0.6 consensus detection per T4. Regen capped at 1 pass (engineer deviation 2 cap 1→0 mid-flight; defensible — preserves user control over refinement motion). |
| 0.3.4.04 | Tool-loop orchestrator (ats_parse_test + detector + skim + keyword + defensibility) | [x] | MEDIUM | (new) | **Plan 67 EXECUTED 2026-05-21 via PR #168** (delta MED-3 added injection-marker reject). `services/tool_loop.py` — 5 tools registered with Anthropic SDK tool-use; 3-iteration cap + budget-aware exit per OQ-3. NO `interleaved-thinking-2025-05-14` beta header for Sonnet 4.6 / Opus 4.7 (adaptive thinking auto-enables per T5). 9-marker injection rejection BEFORE `tracked_call` (no cost incurred on rejected inputs). |
| 0.3.4.05 | ATS parser ensemble (pyresparser + OpenResume + pyresume cross-check) | [x] | MEDIUM | (new) | **Plan 67 EXECUTED 2026-05-21 via PR #168** (delta wired into orchestrator path). `services/ats_parser_ensemble.py` — pdfplumber (existing) + pyresparser (optional `[premium-parsers]` dep) + OpenResume via Node subprocess (`scripts/openresume_parser.js`; `nodejs_20` added to devShell). Aggregate score = mean of available parsers. Wired into `_generate_bundle_premium` at Wave-3.5 with `assert_awaited_once()` regression guard. |
| 0.3.4.06 | Settings UI surface + PREMIUM tier toggle + cost projection display + audit-trail viewer | [x] | MEDIUM | (new) | **Plan 67 EXECUTED 2026-05-21 via PR #168** (delta HIGH-1 fixed silent-wipe bug via `originality_api_key_clear=True` sentinel). Settings · Generation tab full UI: tier toggle FREE/PREMIUM radio, per-app cost projection (history-based ≥10 bundles; ROADMAP fallback), TIER-2 evasion opt-in, Originality.ai API key (write-only password field), audit-trail viewer renders past `Application.generation_trace` entries. Closes 0.3.3.07 inline. |

**Deliverable (0.3.4):** PREMIUM-tier opt-in. Independently shippable; doesn't block any 0.3.x dependency. User-facing cost surface explicit; PREMIUM converts in jobs scoring > 0.85.

---

### 0.3.2 — UI polish + score analytics + 8-bug regression sweep
> **Goal:** Every UI surface that consumes `match_breakdown` or `score` honors the mockup contract; user sees "why" + "what's missing" + "what was generated." Plus close 8 owner-reported local-QA regressions (route+ctx rewire to service layer; Settings · LLM dual-selector cleanup; auth/401 first-run UX).
> **Status:** ✅ Complete (2026-05-21).
> **Shipped:** 2026-05-21 via PR #169 squash `da579f1` — 9 commits, 2038 tests passing.
> **Plan archive:** `docs/plans/archive/68-0.3.2-ui-remediation-master.md` (master) + `docs/plans/archive/{69,70,71,72,73,74}-*.md`.

| # | Task | Status | Priority | Legacy ID | Notes |
|---|---|---|---|---|---|
| 0.3.2.01 | Score card component + per-dim bar component (`score_card.html` + `match_breakdown.html`) | [x] | HIGH | 3.7 | **Plan 72 EXECUTED 2026-05-21 via PR #169 squash `da579f1`.** Variant B Linear bento 3-zone composite shipped at `src/ui/templates/components/score_card.html` (new partial, 91→92 catalog). Composes existing `score_circle.html` + `match_breakdown.html` + STRENGTHS/WHAT'S MISSING/PROVENANCE overlay panels. Wired into Discover swipe_card + Discover · review LEFT column. |
| 0.3.2.02 | Bullet selection preview UI (Discover · review & apply) — color-coded shipped vs dropped | [x] | MEDIUM | (new) | **Plan 72 EXECUTED 2026-05-21** (paired w/ 0.3.2.01). `tailored_bullet_row.html` extended with `rationale` arg (Variant A Inline ledger). Italic micro-copy below each bullet ("why kept · ..." cyan / "why dropped · ..." slate). `bullet_selection_log` field added to `Application.generation_trace`. |
| 0.3.2.03 | Score history time-series view (sparkline on Profile via HTMX fragment — NO new `/scores` route per Q73.4 lock) | [x] | LOW | 3.6 | **Plan 73 EXECUTED 2026-05-21** (2 commits). Alembic 0020 added `Profile.score_history` JSONB column. APScheduler `score_aggregate_daily` cron at 03:30 UTC writes per-role-family blob. Heuristic substring `role_family` classifier (9-tag + other). Variant A Hero strip sparkline embedded in Profile hero via inline SVG (no JS, no Chart.js). |
| 0.3.2.04 | Calibration + cost-cap fallback indicator banner ("LLM judge paused — cost cap reached") | [x] | MEDIUM | (new) | **Plan 74 EXECUTED 2026-05-21.** `_llm_cost_cap_widget.html` extended with `judge_skipped_count_today` + `judge_skipped_reasons_today` args. Severity-gated render: count 1-3 Variant A inline; count ≥4 Variant B prominent strip (SPENT/CAP/RESUMES grid + CTA). New helpers in `services/llm_tracker.py`. |

**Deliverable (0.3.2):** Every UI surface honors `match_breakdown` mockup contract; user sees explanations + cost-cap state.

---

### 0.3.3 — Deferred-from-0.3.x catch-all
> **Goal:** Catch-all for items deferred or deviated during 0.3.0/0.3.1/0.3.2 implementation. Mirrors the 0.2.7 pattern (which closed 2026-05-21). Items either ship as a future sub-release pickup or graduate to 0.8.0.
> **Status:** 🟢 Mostly closed (24 rows total; 19 closed via PRs #169 + #170; 5 remaining: 0.3.3.04+09 defer-triggered, 0.3.3.16 expansion-deferred, 0.3.3.08a + 0.3.3.24 new follow-ups from PR #170 reviewers).
> **Plan archive:** `docs/plans/archive/75-0.3.3-housekeeping-batch-2026-05-21.md` (PR #170 squash `a37b454`) + master plan 68 (PR #169 squash `da579f1`).

| # | Task | Status | Priority | Legacy ID | Notes |
|---|---|---|---|---|---|
| 0.3.3.24 | Burstiness regen audit-trail completeness + cost-cap test determinism | [x] | LOW | (hacker PR #170; closed in plan 85) | **Plan 85 § A EXECUTED 2026-05-21 via PR #204 squash `be07069`.** Log WARNING→ERROR + `bundle_generator` writes `burstiness_regen_failed=True` to trace dict. Cost-cap test refactored via stack-frame inspection. |
| 0.3.3.23 | Confirm endpoint rate-limit bucket — was `check_rescore_rate_limit`, should be `check_generate_bundle_rate_limit` (PR #170 hacker MED) | [x] | MEDIUM | (hacker PR #170 review 2026-05-21) | **CLOSED inline via PR #170 fold-in commit `d4e46a6` 2026-05-21.** `src/ui/routes/discover.py:729` `fragment_apply_confirm` dependency swapped from rescore-bucket (10/min) to generate-bundle-bucket (10/hr) — confirms consume bundle-generation budget, not rescore budget. Docstring updated. |
| 0.3.3.08a | Wire "Review & apply" button HTMX target to `/_fragments/apply/preview/<id>` (PR #170 architect MED-1 — UI entry point missing) | [x] | MEDIUM | (architect PR #170 review 2026-05-21) | **CLOSED via plan 77 PR #192 squash `926a7b7` 2026-05-21.** Verified post-merge: `discover_action_bar.html:23` now uses `action_url="/_fragments/apply/preview/by-job/" ~ job_id`. ROADMAP row was stale `[ ]`; flipped `[x]` 2026-05-21 during plan 85 batch audit. |
| 0.3.3.22 | Architect LOW-1 (PR #169) — auth.py 401 → HX-Redirect refinement | [x] | LOW | (architect PR #169 review 2026-05-21) | **CLOSED via PR #170 squash `a37b454` (plan 75 row 14).** Plan 71 § File-by-file edit #7 HX-Redirect refinement landed: `src/services/auth.py:556-609` branches on `is_htmx` + `is_api`; HTMX callers get `HX-Redirect /login`, API callers get JSON 401. 4 tests in `tests/test_auth_401_hx_redirect.py` cover the matrix. |
| 0.3.3.21 | Architect MED-3 (PR #169) — SCREENS.md § 4 Profile sparkline graduation | [x] | MEDIUM | (architect PR #169 review 2026-05-21) | **CLOSED via PR #170 squash `a37b454` (plan 75 row 16).** SCREENS.md § 4 Profile updated with sparkline section spec at lines 274-284. |
| 0.3.3.20 | Architect MED-2 (PR #169) — DATA_MODEL.md graduation for `Profile.score_history` JSONB column | [x] | MEDIUM | (architect PR #169 review 2026-05-21) | **CLOSED via PR #170 squash `a37b454` (plan 75 row 15).** DATA_MODEL.md § Profile updated with `score_history` JSONB sub-shape at lines 149-191. |
| 0.3.3.19 | Architect MED-1 (PR #169) — `score_aggregate_daily` cron-slot collision with `score_recompute_stale` (03:30 UTC) | [x] | MEDIUM | (architect PR #169 review 2026-05-21) | **CLOSED via PR #170 squash `a37b454` (plan 75 row 2).** `src/scheduler/jobs.py` `score.aggregate_daily` shifted from 03:30 → 03:35 UTC. Docstring synced via PR #170 fold-in commit `d4e46a6`. |
| 0.3.3.18 | Hacker L2 (PR #169) — `scoring_history._parse_scored_at` defensive parsing tightening | [x] | LOW | (hacker PR #169 review 2026-05-21) | **CLOSED via PR #170 squash `a37b454` (plan 75 row 5).** Defensive parsing tightened; regression test added. |
| 0.3.3.17 | Hacker L1 (PR #169) — JSONB `judge_skipped` predicate string-literal compare hardening | [x] | LOW | (hacker PR #169 review 2026-05-21) | **CLOSED via PR #170 squash `a37b454` (plan 75 row 4).** Predicate compares lowercase; dialect-branch preserved. |
| 0.3.3.16 | Invert `tests/conftest.py:_patch_services_to_sample_data` autouse to opt-in marker — **MOVED to 0.4.5.03** | [→] | LOW | (DEFERRED-TO-0.4.5 per owner directive 2026-05-21) | **MOVED to 0.4.5.03 2026-05-21 per owner directive ("everything else defer to 0.4.5").** Continues at `docs/plans/archive/75-...` plan-75-escape-hatch context; ~138 file marker annotation scope didn't fit 0.3.x cycle window. |
| 0.3.3.15 | Hacker M2 (PR #169) — screener-answer IDOR boundary (GET /_fragments/apply/screener/{application_id}/{question_id}) | [x] | HIGH | (hacker PR #169 review 2026-05-21) | **CLOSED via PR #170 squash `a37b454` (plan 75 row 1).** `application_service.get_screener_answer` + `record_screener_answer` now JOIN through `Application.user_id`; route adds `require_authed_session` dep. 5 dedicated tests in `tests/test_screener_idor.py` cover cross-user reads + writes + owner-passes + legacy bypass. **Multi-tenant readiness gate cleared.** |
| 0.3.3.14 | Auth flow + 401 remediation (first-run UX) | [x] | HIGH | (master plan 68 — owner bugs 5+7) | **Plan 71 EXECUTED 2026-05-21 via PR #169 squash `da579f1`.** Owner reports "no auth flow" + "401 on saving" closed. Delivered: RUNBOOK § 2.12 (First-run auth flow + 401 troubleshooting) + `GET /setup-help` public diagnostic route + `src/services/first_run.py` canonical state probe + main.py lifespan WARN. +23 tests (test_first_run/test_setup_help_route/test_first_run_walkthrough). |
| 0.3.3.13 | Settings · LLM dual-selector cleanup (delete "Active provider" radio cards; keep env-presence indicators) | [x] | HIGH | (master plan 68 — owner bug 6) | **Plan 70 EXECUTED 2026-05-21 via PR #169 squash `da579f1`.** Owner directive closed. Active-provider radio section deleted from `_settings_llm.html`. New helper `env_secrets.resolve_active_llm_provider(settings=None)` returns ANTHROPIC > OPENAI > OLLAMA precedence based on env presence. Env-resolved active-provider chip added to API-key section header. +7 tests. |
| 0.3.3.12 | Route + ctx-builder rewire to service layer (descoped from 0.2.7.17 / plan 60 commit 2) | [x] | HIGH | (master plan 68 — was 0.8.0.38) | **Plan 69 EXECUTED 2026-05-21 via PR #169 squash `da579f1`.** Owner regression bugs 1+2+3+4+8 closed (Discover/Profile/save-doesnt-persist/stub-data/overview-pipeline-live). 13 → 0 sample_data offenders. 8 routes + 5 ctx-builders migrated to service layer. New autouse fixture `_patch_services_to_sample_data` in conftest. 2 new service-layer helpers (job_service.get_scrape_run + application_service.get_screener_answer) close raw-SQL-in-route gaps. _KNOWN_LEGACY_OFFENDERS set deleted entirely. |
| 0.3.3.01 | Extend `test_no_cross_user_embedding_reads.py` lint to cover `ProfileEmbedding` | [x] | MEDIUM | (hacker PR #166 MED-1) | **CLOSED via PR #170 squash `a37b454` (plan 75 row 9).** Lint extended; 1 new fixture; ProfileEmbedding cross-user reads now guarded. |
| 0.3.3.02 | Rate limit on `POST /api/v1/jobs/{id}/rescore` (10/min, 60/hr) | [x] | MEDIUM | (hacker PR #166 MED-2) | **CLOSED via PR #170 squash `a37b454` (plan 75 rows 6+7 — paired with 0.3.3.06; new `src/services/rate_limit.py` module clone of auth.py:354-380 pattern; 12 sliding-window tests). |
| 0.3.3.03 | `json_jobstore._decode_datetime` should reject naive timestamps explicitly (not silent UTC rebase) | [x] | MEDIUM | (hacker PR #166 MED-3) | **CLOSED via PR #170 squash `a37b454` (plan 75 row 3).** Naive timestamps explicitly rejected; 2 regression tests added. |
| 0.3.3.04 | LLM cost-cap probe race condition — **MOVED to 0.4.5.01** | [→] | LOW | (DEFERRED-TO-0.4.5 per owner directive 2026-05-21) | **MOVED to 0.4.5.01 2026-05-21 per owner directive.** Defer-until-first-thrash-observation; doesn't fit 0.3.x cycle window. |
| 0.3.3.05 | Burstiness regen path — one-shot regenerate when std-dev < 6 in bundle output | [x] | MEDIUM | (engineer PR #167 round-2 deviation) | **CLOSED via PR #170 squash `a37b454` (plan 75 row 11).** Regen path ships with cost-cap probe BEFORE regen LLM call (records `burstiness_regen_skipped_cost_cap` on skip); 1-per-bundle cap matches critique-council T4 pattern. |
| 0.3.3.06 | Per-user rate limit on `POST /api/v1/applications/{id}/generate-bundle` (10/hr suggested) | [x] | MEDIUM | (hacker PR #167 LOW-2) | **CLOSED via PR #170 squash `a37b454` (plan 75 rows 6+7 — paired with 0.3.3.02; shared `src/services/rate_limit.py` module; per-user 10/hr bucket isolation tests). |
| 0.3.3.07 | Settings · Generation tab + Settings · Advanced tab UI | [x] | MEDIUM | (engineer PR #167 round-1 deviation) | **CLOSED inline by PR #168 / plan 67 EXECUTED 2026-05-21.** Settings · Generation tab + Audit-trail viewer + tier toggle + cost projection + Originality.ai API key field shipped as part of 0.3.4 PREMIUM Settings UI (T7 lock). Settings columns now operator-accessible via UI. |
| 0.3.3.08 | Discover · review & apply HTMX two-step + HX-Trigger headers | [x] | MEDIUM | (engineer PR #167 round-1 deviation) | **CLOSED via PR #170 squash `a37b454` (plan 75 row 11).** `/_fragments/apply/{preview,confirm,cancel}/<id>` routes ship + new `apply_preview_card.html` partial (COMPONENTS.md 92→93) + HX-Trigger handoff to existing generate-bundle route. **UI entry-point wiring deferred to 0.3.3.08a follow-up** (button still points at workspace fragment; architect MED-1). |
| 0.3.3.09 | Conditional second prompt-caching breakpoint — **MOVED to 0.4.5.02** | [→] | LOW | (DEFERRED-TO-0.4.5 per owner directive 2026-05-21) | **MOVED to 0.4.5.02 2026-05-21 per owner directive.** Defer-until-heavy-corpus-user-observed; doesn't fit 0.3.x cycle window. |
| 0.3.3.10 | Tool-loop injection-marker list extensibility + 200-byte log truncation parity | [x] | LOW | (hacker PR #168 NOTE) | **CLOSED via PR #170 squash `a37b454` (plan 75 row 12).** New `NAAVIK_TOOL_LOOP_MARKERS` env-var (opt-in, comma-separated additions to baseline 9-marker tuple); 200-byte log truncation aligned. Operational surface documented in README + .env.example. |
| 0.3.3.11 | Refactor cross-module `_sync_fallback` private API to `services/_council_common.py` | [x] | LOW | (hacker PR #168 LOW-4) | **CLOSED via PR #170 squash `a37b454` (plan 75 row 13).** New `src/services/_council_common.py` module extracts `_sync_fallback`; `critique_council.py:24` import updated. Pure refactor; behavior unchanged. |

**Deliverable (0.3.3):** Pickup queue for product-side cleanup deferred from 0.3.x. Items graduate to a future release version when scheduled.

---

### 0.4.0 — Application Tracking & Auto-Apply (legacy Phase 4)
> **Goal:** Sponsorship-safe application lifecycle with operator-controllable automation (semi-auto or auto-apply); operator-facing recovery + analytics + bulk operations on top of the Wave 5 / 0.2.x / 0.3.x substrate.
> **Status:** ✅ Complete (2026-05-21). All 7 plans (76-82) shipped. 4 rows MOVED to 0.4.5 per owner directive (0.4.0.05 Playwright form fill / 0.4.0.06 Sheets sync / 0.4.0.18 cover letter regen + 3 review-derived follow-ups 0.4.5.06-12).
> **Audit memo:** `docs/design/research/0.4.0-AUDIT.md` (architect dispatch 2026-05-21T07-54-17_362de279) — row-by-row assessment + 13 augmentation rows + sponsorship-gate decision + option matrices + build sequence.
> **Plans:** 76 (sponsorship gate) → 77 (semi-auto wiring + notification parity) → 78 (auto-apply hardening cluster) → 79 (retry-failed) → 80 (bulk actions) → 81 (detail + postmortem + timeline + analytics) → 82 (bookkeeping closer). 70-80% foundation reuse: Wave 5 application_service + plan 75 apply-preview routes + plan 52 postmortem + plan 73 score_history/uplot.
> **Implementation contract:** `docs/design/BACKEND.md` § K (auto-apply + manual paths), § K.5 (ATS adapters per board), § L.1 (Gmail/Outlook OAuth). `docs/design/DATA_MODEL.md` § A multi-axis state, § E state transitions. `docs/design/RESUME_GENERATION.md` (plan 66/67) for bundle generation. `AGENTS.md § Owner Profile` H1B sponsorship rule.

| # | Task | Status | Priority | Legacy ID | Notes |
|---|---|---|---|---|---|
| 0.4.0.01 | Application multi-axis state model | [x] | — | 4.1 | **CLOSE-AS-SHIPPED 2026-05-21 via 0.4.0 cycle close (plan 82 ROADMAP-only).** Substrate built via 0.1.0 Wave 5 / plan 10. Canonical: `docs/design/DATA_MODEL.md § C/E`. |
| 0.4.0.02 | Manual application logger | [x] | — | 4.2 | **CLOSE-AS-SHIPPED 2026-05-21 via 0.4.0 cycle close (plan 82 ROADMAP-only).** Shipped via 0.2.7.19 / plan 56. |
| 0.4.0.03 | Semi-auto flow: generate docs → notification → human approves → submit → update status | [x] | — | 4.3 | **Plan 77 EXECUTED 2026-05-21 via PR #192 squash `926a7b7`.** M1 wires Review & apply CTA to new `/_fragments/apply/preview/by-job/{job_id}` route (closes 0.3.3.08a UI wiring) — get_or_create_draft pattern + stricter IDOR via `_job_or_404`. M2 adds `_default_notify_fn(settings)` closure factory + extends `submit_draft(notify_fn=None)` for manual+auto parity. Reviewers: hacker APPROVE zero findings; architect APPROVE_WITH_NOTES 2 INFO (plan 77 event-name drift vs plan 75 canonical `bundle-generated`; orphan workspace fragment route still exists but unreachable from CTA). 7 new tests. |
| 0.4.0.04 | Auto-apply flow: high-score jobs → generate → submit automatically (user setting, default OFF) | [x] | — | 4.4 | **Plan 78 EXECUTED 2026-05-21 via PR #193 squash `0a48cbc`.** Score-threshold belt-and-suspenders gate + paired cluster 0.4.0.13/14/15/20. |
| 0.4.0.05 | Playwright form filling — **MOVED to 0.4.5.04** | [→] | — | 4.5 | **MOVED 2026-05-21 per owner directive ("defer everything else to 0.4.5"); originally 0.8.0.24-27 target now 0.4.5.04.** Production Workday/LinkedIn/Indeed/Generic adapters; multi-week effort. |
| 0.4.0.06 | Google Sheets sync — **MOVED to 0.4.5.05** | [→] | — | 4.6 | **MOVED 2026-05-21 per owner directive; originally 0.8.0 target now 0.4.5.05.** Moot post-n8n-closeout; re-engage if multi-tenant cloud emerges. |
| 0.4.0.07 | Application analytics dashboard | [x] | — | 4.7 | **Plan 81 EXECUTED 2026-05-21 via PR #206 squash `1569153`.** 4 KPIs (Response/Onsite/Offer Rate + Funnel · 90d) + 1 breakdown (by company; `kpis_by_role_family` + `kpis_by_tag` stubbed empty → 0.4.5.06 + 0.4.5.07 follow-ups). Funnel uses Tailwind CSS bars (deviation: no new uplot CDN dep). New `/tracking/analytics` route + page; route order verified (literal before dynamic). |
| 0.4.0.08 | Sponsorship-gate enforcement at submission — belt-and-suspenders defense on top of scorer's visa filter | [x] | HIGH | (audit memo 2026-05-21 § D) | **Plan 76 EXECUTED 2026-05-21 via PR #191 squash `68bfd1d`.** Foundation row shipped FIRST per audit build sequence. New `_enforce_sponsorship_gate` in `application_service.py:279-330` raises `ValidationError(code='visa_incompatible')` at `validate_submittable` when H1B operator submits to no-sponsorship Job. Reuses `scorer.visa.needs_visa_zero_out` single-source-of-truth predicate. 4 unit tests cover predicate matrix. Reviewers: hacker APPROVE_WITH_NOTES (2 follow-ups filed 0.4.0.21+22); architect APPROVE FULL spec match (self-approval pivot). |
| 0.4.0.21 | IDOR boundary on application mutation routes (`/api/v1/applications/{id}/{submit,discard,status,move}`) | [x] | MEDIUM | (hacker PR #191; closed in plan 85) | **Plan 85 § B EXECUTED 2026-05-21 via PR #204 squash `be07069`.** Service-layer `svc.get_application` + route-level `user_id != current_user.id` → 404 boundary applied to all 4 mutation routes. `grep -c` confirms 6 total guards (4 new + 2 pre-existing on postmortem + generate-bundle). 6 new tests in `tests/test_applications_idor.py` (4 cross-user + 1 missing-app + 1 positive). Multi-tenant-readiness gate cleared on Application mutation surface. |
| 0.4.0.22 | `process_auto_apply_queue` re-fires every 5min on visa-blocked DRAFT (cron tight-loop) | [x] | LOW | (hacker PR #191 — folded into plan 78) | **CLOSED via PR #193 squash `0a48cbc` plan 78 fold-in M7.** On `code=visa_incompatible`, flips Job.queue_state QUEUED_FOR_AUTO_APPLY → SAVED + emits `AppEvent(AUTO_APPLY_VISA_BLOCKED)`. |
| 0.4.0.09 | Bulk actions on /tracking list (multi-select toolbar — move stage, archive, export CSV) | [x] | MEDIUM | (audit memo 2026-05-21 § C) | **Plan 80 EXECUTED 2026-05-21 via PR #195 squash `187e9e8`.** 3 service fns (`bulk_update_status` + `bulk_archive` + `list_for_export`) + 3 routes (`POST /_fragments/tracking/bulk/{move-stage,archive}` + `GET /api/v1/applications/export.csv`). New `ClosedReason.USER_ARCHIVED` enum value via Alembic **0022** (NOT 0018 as plan said — slot 0018 was `generation_trace_and_settings`; 0021 used by plan 78). 50-ID cap raised as `ValidationError` in service → 422 in route. New `tracking_bulk_action_bar.html` partial (sticky top toolbar; data-bulk-active Tailwind variant for visibility flip — `hidden` attr was beaten by `.flex` utility specificity). New JS helpers in `base.js` (`trackingBulkSelectionChange` + `bulkExportCsv` + `showToast` HX-Trigger DOM listener — plan implicitly assumed listener existed). Plan claimed `_tracking_list.html` already had per-row checkboxes; commit 3 added them to `tracking_list_row.html`. Components catalog 94 → 95. 14 net-new tests; tests 2134 → 2148. Reviewers: hacker APPROVE_WITH_NOTES (1 MED CSV formula injection → filed 0.4.0.23 / Issue #196; other surfaces clean — IDOR boundary, CSRF, 50-cap, ENUM migration, SQL-injection sanity); architect APPROVE (self-approval pivot state=COMMENTED; 5/5 D.1+D.2+D.3+enum+tests PASS; 3 INFO acknowledging partial-comment doc nit + camelCase HX-Trigger naming + test-name nit). 3 deviations promoted to archived plan. New operational surface: alembic 0022 ENUM-value addition (auto-applies via `alembic upgrade head`). |
| 0.4.0.23 | CSV export formula-injection — quote leaders + tab/CR on operator-controllable fields | [x] | MEDIUM | (hacker PR #195; closed in plan 85) | **Plan 85 § C EXECUTED 2026-05-21 via PR #204 squash `be07069`.** New `_defang_csv_cell` helper prepends `'` to cells starting with `=/+/-/@/\t/\r`. Applied to all 10 export fields in `list_for_export` (defense-in-depth across operator + scraper-controlled values). 4 new tests (leaders / whitespace / safe values / e2e). OWASP A03:2021 CSV-injection mitigation. |
| 0.4.0.10 | Postmortem UI on Application detail (modal overlay) | [x] | MEDIUM | (audit memo 2026-05-21 § C+F) | **Plan 81 EXECUTED 2026-05-21 via PR #206.** `postmortem_modal.html` + `/_modal/postmortem/<application_id>/<ts>` reusing plan 52's IDOR + path-traversal gauntlet (regex + `Path.resolve().relative_to`). Uses `<pre>` instead of `markdown | safe` filter (deviation; markdown lib not installed; autoescape blocks XSS — POSITIVE security delta). |
| 0.4.0.11 | Retry-failed-application flow (one-click recovery from stuck DRAFT) | [x] | MEDIUM | (audit memo 2026-05-21 § C) | **Plan 79 EXECUTED 2026-05-21 via PR #194 squash `7fa4b6b`.** `application_service.retry_failed` (`+60 LOC`) + `POST /api/v1/applications/{id}/retry` (CSRF + auth gated; 204 + `HX-Trigger: applicationRetried`; 409 on non-DRAFT or no-failure; 404 on IDOR mismatch). Idempotent strip of `submission_artifacts["last_failure"]`; conditional Job re-queue iff `Settings.auto_apply_enabled`. New `AppEventKind.AUTO_APPLY_QUEUED` enum value (`StatusChangeTrigger.AUTO_APPLY_QUEUED` was a different enum); no alembic migration shipped (matches plan 78 ENUM-via-`create_all` precedent). UI: Retry button on `_application_detail.html` `last_failure` info_card (Lucide `refresh-cw` stroke 1.5; `hx-confirm` dialog); compact RETRY chip on `up_next_card.html` stuck-state. 11 net-new tests (7 service + 4 route) — tests 2123 → 2134. Reviewers: hacker APPROVE_WITH_NOTES (2 LOW + 2 INFO; both LOWs cross-route pre-existing patterns, not regressions — race-on-AppEvent paired naturally w/ 0.4.0.21 IDOR work; rate-limit deficit below LLM-spend threshold; no follow-up filed); architect APPROVE (self-approval pivot — state=COMMENTED; D.1/D.1-route/D.2/D.3 all PASS; 0 actionable findings; 2 INFO acknowledging pre-existing HX-Trigger naming split + already-documented enum addition). 1 deviation promoted to archived plan. **No new operational surface** (no env / CLI / on-disk path / port / schedule / migration). |
| 0.4.0.12 | Application timeline view (full AppEvent history toggle) | [x] | LOW | (audit memo 2026-05-21 § C) | **Plan 81 EXECUTED 2026-05-21 via PR #206.** "Show full history" toggle + new `_application_timeline_full.html` partial (engineer added as separate partial vs plan's inline; cleaner separation) + `/_fragments/tracking/timeline/<application_id>` route. 15-kind decor map covers every `AppEventKind`. |
| 0.4.0.13 | Submission rate-limiting per board / per day | [x] | MEDIUM | (audit memo 2026-05-21 § C) | **Plan 78 EXECUTED 2026-05-21 via PR #193 squash `0a48cbc`.** Alembic 0021 (numbered 0021 not 0017 — slots 0017-0020 used); per-board running counter SELECT; cap_class=per_board skip event. |
| 0.4.0.14 | Pause/resume auto-apply (global drain + per-job toggle) | [x] | LOW | (audit memo 2026-05-21 § C) | **Plan 78 EXECUTED 2026-05-21 via PR #193.** Per-job HTMX toggle on Discover; global drain button on Settings · Auto-apply; AppEvent(AUTO_APPLY_DRAINED). |
| 0.4.0.15 | Sponsorship-friendly visa chip on Discover | [x] | LOW | (audit memo 2026-05-21 § C) | **Plan 78 EXECUTED 2026-05-21 via PR #193.** New `visa_status_chip.html` partial; COMPONENTS.md 92→94. 3 states keyed off canonical VisaRestriction enum (US_CITIZEN_ONLY / GREEN_CARD_REQUIRED / SPONSORSHIP_AVAILABLE / NOT_MENTIONED). |
| 0.4.0.16 | Application detail page polish (editable notes + Discover-jump + score chip) | [x] | LOW | (audit memo 2026-05-21 § C) | **Plan 81 EXECUTED 2026-05-21 via PR #206.** Notes textarea + `PUT /api/v1/applications/{id}/notes` (form-encoded + JSON dual content-type per existing precedent; CSRF + IDOR + 2000-char max). Discover-jump for DRAFT. Score chip via `score_circle.html` (sm variant). |
| 0.4.0.17 | Submission notification parity (manual review submit → Discord/Telegram echo same as auto-apply) | [x] | MEDIUM | (audit memo 2026-05-21 § C) | **Plan 77 EXECUTED 2026-05-21 via PR #192 squash `926a7b7`.** Shipped paired with M1. `_default_notify_fn(settings)` signature (no session — closure factory only needs settings); auto-apply cron unchanged. Plan 78 can reuse this directly per architect review. |
| 0.4.0.18 | Cover letter regen + bullet selection override on /tracking edit — **MOVED to 0.4.5.08** | [→] | LOW | (audit memo 2026-05-21 § C) | **MOVED 2026-05-21 per owner directive ("defer everything else to 0.4.5"); originally 0.8.0 target now 0.4.5.08.** Polish; reuses plan 66 `/api/v1/applications/{id}/generate-bundle`. |
| 0.4.0.19 | Recruiter contact link surfacing on Application detail | [ ] | LOW | (audit memo 2026-05-21 § C) | **DEFER-TO-0.5.0 per audit memo recommendation.** Couples with `Contact` + `ContactApplicationLink` CRUD surfaces shipping in 0.5.0 / plan 14 outreach. Re-engage there. |
| 0.4.0.20 | Auto-apply dry-run mode | [x] | LOW | (audit memo 2026-05-21 § C) | **Plan 78 EXECUTED 2026-05-21 via PR #193.** `Settings.auto_apply_dry_run` boolean; short-circuit BEFORE submit_draft; AppEvent(AUTO_APPLY_DRY_RUN). |

**Deliverable (0.4.0):** Sponsorship-safe full Kanban + List tracking. Semi-auto (default) or auto-apply based on user preference; auto-apply with score-gate belt-and-suspenders + adapter-confidence threshold + per-board daily caps + pause/resume + dry-run. Failure recovery (postmortem UI + retry button). Bulk actions on /tracking. KPI dashboard (Response/Onsite/Offer rates · 90d + Funnel). New on-disk surfaces: alembic 0017 (auto_apply_per_board_daily_caps JSONB + auto_apply_dry_run bool on Settings) + alembic 0018 (ClosedReason.USER_ARCHIVED enum value) + 6 new partials (COMPONENTS.md 93 → 99). Foundation reuse 70-80%.

---

### 0.4.5 — Deferred-from-0.3.x/0.4.x catch-all (cycle parking lot)
> **Goal:** Park items deferred from the 0.3.x + 0.4.x cycles when the owner wants to defer-not-cancel — small-scope follow-ups, large-scope test refactors, low-priority polish that doesn't fit the current sprint but shouldn't drop off the roadmap.
> **Status:** 🟡 Queued (owner-locked cycle 2026-05-21: "everything else defer to 0.4.5").
> **Plan:** No single plan — each row gets its own plan when scheduled OR batches via housekeeping plans per § Batching small tasks (manager.md).
> **Mirror milestone:** `0.4.5`.

| # | Task | Status | Priority | Legacy ID | Notes |
|---|---|---|---|---|---|
| 0.4.5.01 | LLM cost-cap probe race condition (concurrent callers; ~$0.075 overshoot worst case) | [x] | LOW | (DEFERRED-FROM-0.3.3.04) | **SHIPPED 2026-05-22 via plan 86 PR #207 squash d909702.** `services/llm_tracker.acquire_cost_cap_slot` + `release_cost_cap_slot` with `SELECT FOR UPDATE` row-lock + placeholder-row INSERT pattern. sqlite degrades gracefully via `_dialect_supports_for_update` predicate. Follow-ups: 0.4.5.01a (empty-set advisory-lock upgrade), 0.4.5.16a/16b (placeholder propagation to widgets + bundle_generator delegation). |
| 0.4.5.02 | Conditional second prompt-caching breakpoint (when corpus > N tokens) | [x] | LOW | (DEFERRED-FROM-0.3.3.09) | **SHIPPED 2026-05-22 via plan 86 PR #207 squash d909702.** `llm/prompts/score_job.should_insert_second_cache_breakpoint` + `split_for_double_cache` helpers; threshold 60K tokens. Wiring through `LLMProvider.structured()` abstraction deferred to 0.4.5.02a (engineer D2 descope). |
| 0.4.5.03 | Invert `tests/conftest.py:_patch_services_to_sample_data` autouse to opt-in marker | [x] | LOW | (DEFERRED-FROM-0.3.3.16) | **SHIPPED 2026-05-22 via plan 87 PR #208 squash `5b2d351`.** Marker `uses_sample_data_shims` registered in `pyproject.toml`; `_SERVICE_DIRECT_TEST_MODULES` deleted; fixture body marker-gated (engineer D1: kept `autouse=True` with early-return — same semantics as plan W1's manual-opt-in, lower boilerplate). 162 test files annotated across 10 batched commits. Contract test (`test_conftest_opt_in_marker_works.py`) pins inversion direction. Negative test verified: removing marker from `test_pages.py` produces real failure. Tests 2216 → 2218 (+2). Reviewers: hacker APPROVE (0 findings — test-infra only); architect APPROVE_WITH_NOTES (3 LOW cosmetic; cycle-close GO). |
| 0.4.5.04 | Playwright form filling for supported boards (DEFERRED-FROM-0.4.0.05) — DECOMPOSED into 04a-d per-adapter sub-rows | [x] | — | (DEFERRED-FROM-0.4.0.05 per owner directive 2026-05-21; decomposed via plan 86 2026-05-21) | **DECOMPOSED 2026-05-21 via plan 86.** Multi-week scope split into 0.4.5.04a-d per-adapter rows below (each becomes its own plan when scheduled). Plan 63 scaffolds shipped via PR #164; per-adapter production implementations remain. |
| 0.4.5.04a | Workday production Playwright adapter — replaces plan 63 scaffold | [ ] | LOW | (split from 0.4.5.04 via plan 86 2026-05-21) | **Filed 2026-05-21 via plan 86 W5.3 decomposition.** Multi-week effort; own plan when scheduled. |
| 0.4.5.04b | LinkedIn Easy Apply production Playwright adapter | [ ] | LOW | (split from 0.4.5.04 via plan 86 2026-05-21) | **Filed 2026-05-21 via plan 86 W5.3 decomposition.** Multi-week effort; own plan when scheduled. Account-ban risk noted in plan 14 (Phase 5) — coordinate w/ 0.5.0.12 outreach scope. |
| 0.4.5.04c | Indeed apply production Playwright adapter | [ ] | LOW | (split from 0.4.5.04 via plan 86 2026-05-21) | **Filed 2026-05-21 via plan 86 W5.3 decomposition.** Multi-week effort; own plan when scheduled. Captcha-rate hazard documented in `docs/design/research/`. |
| 0.4.5.04d | Generic-form heuristic adapter (best-effort form-fill on unknown ATS) | [ ] | LOW | (split from 0.4.5.04 via plan 86 2026-05-21) | **Filed 2026-05-21 via plan 86 W5.3 decomposition.** Multi-week effort; own plan when scheduled. LLM-assisted form-field-detection candidate. |
| 0.4.5.05 | Google Sheets sync (DEFERRED-FROM-0.4.0.06) — CLOSED AS MOOT | [x] | — | (DEFERRED-FROM-0.4.0.06 per owner directive 2026-05-21; closed-as-moot via plan 86 2026-05-21) | **CLOSED-AS-MOOT 2026-05-21 via plan 86.** Moot post-n8n-closeout: owner uses Naavik as fresh-start; no historical spreadsheet data to import. Re-engage if multi-tenant cloud emerges. |
| 0.4.5.06 | `kpis_by_role_family` analytics breakdown (stubbed empty in plan 81) | [x] | LOW | (architect PR #206 review 2026-05-21) | **SHIPPED 2026-05-22 via plan 86 PR #207 squash d909702 + R3 round 2.** `services/application_analytics.kpis_by_role_family` real implementation via `scoring_history.classify_role_family` substrate. Wired into `/tracking/analytics` ctx + new `kpis_by_role_family.html` partial via 0.4.5.15 R3 round 2 fold-in. |
| 0.4.5.07 | `kpis_by_tag` analytics breakdown (stubbed empty in plan 81) | [x] | LOW | (architect PR #206 review 2026-05-21) | **SHIPPED 2026-05-22 via plan 86 PR #207 squash d909702 + R3 round 2.** `services/application_analytics.kpis_by_tag` real implementation via `Bullet.tags ∩ Job.tags` join (reads `GeneratedDocument.bullet_selection["selected_ids"]` per plan 66 substrate; engineer D1 deviation from plan-stated path). Wired into `/tracking/analytics` ctx + new `kpis_by_tag.html` partial via 0.4.5.15 R3 round 2 fold-in. |
| 0.4.5.08 | Cover letter regen + bullet selection override on /tracking edit (DEFERRED-FROM-0.4.0.18) | [x] | LOW | (DEFERRED-FROM-0.4.0.18 per owner directive 2026-05-21) | **SHIPPED 2026-05-22 via plan 86 PR #207 squash d909702 + R1+R2 round 2.** Regenerate button on detail slide-over → `regenerate_kind=cover_letter` branch on `/generate-bundle` (R1 round 2). Per-bullet override toggle → `PUT /api/v1/applications/{id}/bullet-override` cycles `null → always_include → never_include → null`. Wired into `document_generator._split_bullets_by_override` via `application_overrides` arg + `_application_bullet_overrides` extractor (R2 round 2). Per-app override wins over `Bullet.selection_override` model column. |
| 0.4.5.09 | `sample_data.py` extension to 30+ applications + STATUS_CHANGE AppEvent trails (analytics dashboard demo data) | [x] | LOW | (architect PR #206 review 2026-05-21) | **SHIPPED 2026-05-22 via plan 86 PR #207 squash d909702.** `src/db/sample_data.py` extended 16 → 30 applications + ~150 new AppEvents (STATUS_CHANGE + DOCS_GENERATED chains across 10 role-family buckets + multiple ClosedReason values). Analytics dashboard demo experience no longer triggers low-sample warning banner. |
| 0.4.5.10 | `PUT /api/v1/applications/{id}/notes` cap check should fire BEFORE JSON parse (DoS amplification) | [x] | LOW | (hacker PR #206 review 2026-05-21) | **SHIPPED 2026-05-22 via plan 86 PR #207 squash d909702.** Pydantic `NotesPayload(BaseModel)` with `notes: str = Field(max_length=2000)` — validator fires at body-parse layer. Form-encoded path routed through `model_validate` for consistent gating. |
| 0.4.5.11 | `_application_or_404` should filter `deleted_at IS NULL` (pre-existing pattern gap) | [x] | LOW | (hacker PR #206 review 2026-05-21) | **SHIPPED 2026-05-22 via plan 86 PR #207 squash d909702.** Added `a.deleted_at is not None` check in `src/ui/routes/tracking.py:_application_or_404` before user_id boundary. Soft-deleted Applications return 404 regardless of ownership; aligns with broader soft-delete discipline. |
| 0.4.5.12 | COMPONENTS.md reconciliation — partials catalog now 100 after plan 81 chain | [x] | LOW | (architect PR #206 review 2026-05-21) | **SHIPPED 2026-05-22 via plan 86 PR #207 squash d909702 + R3 round 2 (100→102).** Initial reconciliation 95 → 100 (5 plan-81-chain partials registered); R3 round 2 fold-in bumped 100 → 102 (2 new analytics partials `kpis_by_role_family.html` + `kpis_by_tag.html`). 3 mention sites updated. |
| 0.4.5.13 | `regenerate_kind=cover_letter` semantic — narrow regen path on `/api/v1/applications/{id}/generate-bundle` (DEFERRED-FROM-PR-#207-architect-R1-round-2) | [x] | HIGH | (architect PR #207 review round 2 2026-05-21) | **SHIPPED 2026-05-22 via plan 86 R1 round 2 fold-in (PR #207 squash d909702).** `api.applications.generate_bundle_route` accepts `regenerate_kind ∈ {cover_letter, resume, bundle}` (default bundle); cover_letter branch invokes new `bundle_generator.regenerate_cover_letter` helper (re-runs cover letter prompt only; skips resume regen + bullet selection). Tracked via `tracked_call`. |
| 0.4.5.14 | Bullet overrides wired into document generator (`PUT /api/v1/applications/{id}/bullet-override`) — DEFERRED-FROM-PR-#207-architect-R2-round-2 | [x] | HIGH | (architect PR #207 review round 2 2026-05-21) | **SHIPPED 2026-05-22 via plan 86 R2 round 2 fold-in (PR #207 squash d909702).** `document_generator._split_bullets_by_override` extended with `application_overrides` arg; threaded through `_ai_select_bullets` + `generate_resume`. Resolution order per bullet: per-app (`submission_artifacts.bullet_overrides`) wins > model col (`Bullet.selection_override`) > None default. |
| 0.4.5.15 | `kpis_by_role_family` + `kpis_by_tag` wired into `/tracking/analytics` route ctx + 2 new partials — DEFERRED-FROM-PR-#207-architect-R3-round-2 | [x] | HIGH | (architect PR #207 review round 2 2026-05-21) | **SHIPPED 2026-05-22 via plan 86 R3 round 2 fold-in (PR #207 squash d909702).** `get_tracking_analytics` ctx threads `by_role_family` + `by_tag` keys; new partials `kpis_by_role_family.html` + `kpis_by_tag.html` (catalog 100 → 102); `pages/tracking_analytics.html` includes both sections. QA verified via screenshot md5 `f0ce5c3f187f03288c45a07eb7722eda`. |
| 0.4.5.16 | `acquire_cost_cap_slot` placeholder-row INSERT pattern (concurrent-call overshoot) — DEFERRED-FROM-PR-#207-architect-R5-round-2 | [x] | MED | (architect PR #207 review round 2 2026-05-21) | **SHIPPED 2026-05-22 via plan 86 R5 round 2 fold-in (PR #207 squash d909702).** Signature `bool → int \| None`; placeholder ApiUsage row inserted with `error_kind="cost_cap_slot_placeholder"` holds slot until `release_cost_cap_slot` updates with real cost or DELETEs on short-circuit. `today_cost_usd` NULL-safe filter excludes placeholders from user-facing cost display. Follow-ups: 0.4.5.16a (placeholder propagation to recent_usage/usage_summary), 0.4.5.16b (refactor dg.is_cost_capped), 0.4.5.16c (orchestrator integration), 0.4.5.16d (orphan placeholder cleanup cron). |
| 0.4.5.16a | Placeholder-row filter propagation — `recent_usage` + `usage_summary` exclude `cost_cap_slot_placeholder` rows | [ ] | MED | (DEFERRED-FROM-PR-#207-hacker-finding-round-2 2026-05-22) | **Filed via plan 86 round 2 hacker MED.** `services/llm_tracker.recent_usage` + `usage_summary` do not exclude `cost_cap_slot_placeholder` rows like `today_cost_usd` does. Settings · LLM monthly card over-reports cost by $0.015 × N orphan placeholders. Fix: same NULL-safe `error_kind != 'cost_cap_slot_placeholder'` filter; also strip from `scheduler.aggregate_costs` log. |
| 0.4.5.16b | Refactor `dg.is_cost_capped` to delegate to `llm_tracker.today_cost_usd` (placeholder-aware) | [ ] | MED | (DEFERRED-FROM-PR-#207-hacker-finding-round-2 2026-05-22) | **Filed via plan 86 round 2 hacker MED.** `document_generator.is_cost_capped` reads `_today_spend` filtered on `succeeded=True` — placeholder rows are invisible to it. Concurrent flows (regen + LLM judge cron) can both proceed when second should have been blocked. Fix: delegate to `today_cost_usd` OR have `bundle_generator` stages use `acquire`/`release_cost_cap_slot` directly across the LLM call window. |
| 0.4.5.16c | Orchestrator placeholder integration — release-then-call-LLM gap at `services/scorer/orchestrator.py:329-372` | [ ] | LOW | (DEFERRED-FROM-PR-#207-architect-A1-round-2 2026-05-22) | **Filed via plan 86 round 2 architect A1.** `acquire_cost_cap_slot` returns slot id; orchestrator pattern is release → call LLM → record. Recommendation: hold slot DURING the LLM call, update the placeholder on completion. Lower priority — single-caller orchestrator path is rarely concurrent within itself. |
| 0.4.5.16d | Placeholder orphan cleanup cron (rare SIGKILL window during LLM await) | [ ] | LOW | (DEFERRED-FROM-PR-#207-architect-A3-round-2 2026-05-22) | **Filed via plan 86 round 2 architect A3.** If process is SIGKILL'd mid-LLM-call (rare — graceful shutdown handles SIGTERM), placeholder rows orphan in `api_usage`. Over time capacity throttles. Fix: cron job (existing APScheduler) sweeps `cost_cap_slot_placeholder` rows older than 5 minutes; either DELETE or mark `error_kind="cost_cap_slot_orphan"`. |
| 0.4.5.01a | Cost-cap empty-set race (deeper correctness — advisory-lock upgrade) | [ ] | MED | (DEFERRED-FROM-PR-#207-hacker-finding-round-2 2026-05-21) | **Filed via plan 86 round 2 deferred follow-up.** Hacker finding: even with the W4.1 row-lock + R5 round 2 placeholder-row pattern, an EMPTY-set today (no ApiUsage rows for the user yet) skips the lock acquisition entirely on the `FOR UPDATE` branch — concurrent callers don't serialize. Upgrade to `pg_advisory_xact_lock(hashtext('cost_cap_' || user_id::text))` for a deterministic predicate-lock. Own plan when scheduled. |
| 0.4.5.08c | Bullet-override endpoint missing cross-user `bullet_id` ownership join | [ ] | LOW | (DEFERRED-FROM-PR-#207-hacker-finding-round-2 2026-05-21) | **Filed via plan 86 round 2 deferred follow-up.** Hacker LOW: `PUT /api/v1/applications/{id}/bullet-override` accepts `bullet_id` from form body without verifying the bullet belongs to the requester's profile. Defense-in-depth: the override only affects THAT application's per-app dict (not a foreign bullet's model column), but a malicious user could pollute their `bullet_overrides` artifact with foreign bullet ids (no real-world impact — the resume generator filters by profile_id). Add an ownership check anyway. |
| 0.4.5.02a | Wire double-cache breakpoint helpers into `LLMProvider.structured()` abstraction | [ ] | LOW | (DEFERRED-FROM-PR-#207-engineer-D2-deviation 2026-05-21) | **Filed via plan 86 round 1 deviation 2.** Plan 86 W4.2 shipped only the heuristic helpers (`should_insert_second_cache_breakpoint` + `split_for_double_cache`); wiring through the `LLMProvider` abstraction requires changing the abstract `structured()` signature + all 3 impls (Anthropic + OpenAI + Ollama). Out of W4.2's ~10 LOC budget. |
| 0.4.5.17 | Plan 86 deviation log completion — append D3 + D4 pre-archive | [ ] | LOW | (DEFERRED-FROM-PR-#207-architect-R7-finding 2026-05-21) | **Filed via plan 86 round 2 architect R7.** Plan 86 archive must include all engineer deviations from BOTH round 1 + round 2; manager owns the archive promotion. Pre-archive verification per `naavik-deviations-check` skill. |
| 0.4.5.18 | Graduate `submission_artifacts` schema to `docs/design/DATA_MODEL.md` | [ ] | INFO | (DEFERRED-FROM-PR-#207-architect-R8+R9-finding 2026-05-21) | **Filed via plan 86 round 2 architect R8+R9.** `Application.submission_artifacts` JSONB has accumulated 4+ sub-keys (`last_failure`, `bullet_overrides`, `resume_payload` references). Document the canonical sub-shape in `DATA_MODEL.md § Application` so future readers don't have to grep across plans + engineer commits. |

**Deliverable (0.4.5):** N/A — cycle parking lot; items execute on their own merits when scheduled.

---

### 0.5.0 — Email Monitoring & Outreach (legacy Phase 5)
> **Goal:** Monitor emails, classify responses, manage interview prep, and track recruiter/employee outreach.
> **Status:** ⚪ Future.
> **Plans:** `docs/plans/13-phase-5-email.md` (Gmail/Outlook OAuth + classifier) → then `docs/plans/14-phase-5-outreach.md` (LinkedIn DM + Calendar + Discord/Telegram inbound). Both authored after plan 12 ships.
> **Implementation contract:** `docs/design/BACKEND.md` § L.1 (Gmail/Outlook), § L.2 (LinkedIn browser, account-ban risk), § L.3–L.5 (Discord/Telegram/Calendar), § H.1 (`email_classifier`, `outreach_generator`, `contact_tracker`).
> **Estimated effort:** Plan 13 1–2 weeks; plan 14 2–3 weeks (LinkedIn is the most fragile dep).

| # | Task | Status | Priority | Legacy ID | Notes |
|---|---|---|---|---|---|
| 0.5.0.01 | Gmail API / IMAP email monitoring | [ ] | — | 5.1 | Connect to user's email inbox |
| 0.5.0.02 | AI email classification: INTERVIEW_REQUEST, REJECTION, OFFER, ASSESSMENT, FOLLOW_UP | [ ] | — | 5.2 | |
| 0.5.0.03 | Auto-update job status from email classification | [ ] | — | 5.3 | |
| 0.5.0.04 | Priority notifications (HIGH for interviews/offers) | [ ] | — | 5.4 | |
| 0.5.0.05 | Email thread tracking per application | [ ] | — | 5.5 | |
| 0.5.0.06 | AI draft response generation | [ ] | — | 5.6 | |
| 0.5.0.07 | Interview scheduling integration (Calendly/webhook) — surfaces on Tracking application detail and Overview priority actions | [ ] | — | 5.7 | |
| 0.5.0.08 | Interview prep: role-specific questions from job desc + profile gaps | [ ] | — | 5.8 | |
| 0.5.0.09 | LinkedIn connection tracker: store recruiter/employee contacts per company | [ ] | — | 5.9 | |
| 0.5.0.10 | Outreach template system: personalized messages for recruiters + employees | [ ] | — | 5.10 | Uses profile + job context |
| 0.5.0.11 | AI-generated outreach messages: referral requests, follow-ups, check-ins | [ ] | — | 5.11 | Tone-appropriate, not spammy |
| 0.5.0.12 | LinkedIn automation: send connection requests + messages via API | [ ] | — | 5.12 | Rate-limited, anti-detection. **Re-open `docs/design/research/LINKEDIN_SCRAPING.md` option matrix when authoring plan 14** — § 5 stack-rank flags stickerdaniel/linkedin-mcp-server (1.9k stars, Apache-2.0, Patchright-based) as the front-runner for the outreach surface because authenticated session is unavoidable here, unlike 0.2.0.07 where the guest API wins. |
| 0.5.0.12.1 | LinkedIn MCP adoption — outreach + warm-intro (parented under 0.5.0.12) | [ ] | LOW | (0.3.0 research memo 2026-05-21) | **Filed 2026-05-21 via 0.3.0 research memo § B.5.** stickerdaniel/linkedin-mcp-server (Apache-2.0, Patchright, 17 tools, May 2026 active, 1.9k stars). 4 high-value tools serve Phase 5 outreach: `send_message`, `connect_with_person`, `search_people`, `get_company_employees`. Defer-trigger from 0.3.0 because LinkedIn does NOT expose match-% via any MCP path; Naavik computes its own score regardless. Re-evaluate as a LinkedIn-discovery fallback if 0.2.0.07 guest-API path closes (`LINKEDIN_SCRAPING.md § 8` revisit checklist). |
| 0.5.0.13 | Outreach history tracking: sent messages, responses, acceptance rates | [ ] | — | 5.13 | |
| 0.5.0.14 | Warm intro finder: suggest mutual connections for warm outreach | [ ] | — | 5.14 | LinkedIn API |
| 0.5.0.15 | Interview process accelerator: auto-send thank-you notes, follow-up reminders | [ ] | — | 5.15 | |

**Deliverable (0.5.0):** Email inbox monitored → job statuses auto-updated. Recruiter/employee contacts tracked → AI-assisted outreach → referral requests sent at optimal timing.

---

### 0.6.0 — Optimization & Polish (legacy Phase 6)
> **Goal:** Performance, analytics, and advanced features.
> **Status:** ⚪ Future.
> **Plan:** `docs/plans/15-phase-6-polish.md` — splits cleanly into 15a (observability — Prometheus + Sentry + OTel), 15b (light mode), 15c (LaTeX template + ML scoring calibration). Author after plan 14 ships.
> **Implementation contract:** `docs/design/BACKEND.md` § N (observability — Prometheus, Sentry, OTel), `docs/design/DATA_MODEL.md` § H (`JobEmbedding` pgvector for semantic match), `DESIGN.md` (light mode tokens — Phase 6).
> **Estimated effort:** 3–4 weeks total (split across 15a/b/c).

| # | Task | Status | Priority | Legacy ID | Notes |
|---|---|---|---|---|---|
| 0.6.0.01 | Resume A/B testing (track which variants get responses) | [ ] | — | 6.1 | |
| 0.6.0.02 | Semantic job matching with pgvector embeddings | [ ] | — | 6.2 | |
| 0.6.0.03 | Weekly summary reports | [ ] | — | 6.3 | |
| 0.6.0.04 | Performance: caching, batch AI calls, parallel scraping | [ ] | — | 6.4 | |
| 0.6.0.05 | ML scoring calibration from application outcomes | [ ] | — | 6.5 | |
| 0.6.0.06 | LaTeX template support alongside Typst (for users who prefer LaTeX) | [ ] | — | 6.6 | NEU template compat, latexmk/tectonic compilation |
| 0.6.0.07 | Additional Typst/LaTeX resume templates (modern, academic, creative) | [ ] | — | 6.7 | Template marketplace |

**Deliverable (0.6.0):** Observability stack live; light mode shipped; LaTeX template parity with Typst; ML-calibrated scoring.

---

### 0.7.0 — Agent-system follow-ups (legacy Phase 2.5)
> **Goal:** Quality-of-life improvements + nice-to-have agent-system features deferred until Phase 2 (job scrapers) clears. None blocks the product; all surfaced during Phase A bring-up.
> **Status:** ⚪ Future (deferred per user lock 2026-05-17; ships post-0.2.0 cycle clears).
> **Plan:** No single plan — each row gets its own plan when scheduled (post-Phase-2 user pickup).
> **Mirror milestone:** `0.7.0` (recreated by migration step 4; supersedes `Phase 2.5`).
> **Mirror epic:** `[Epic] 0.7.0`.
>
> User-locked scope decision 2026-05-17: A.22 carries HIGH priority within Phase 2.5 but still defers — Tier 1 / Tier 2 work clears first per pre-Phase-2 scope lock.

| # | Task | Status | Priority | Legacy ID | Notes |
|---|---|---|---|---|---|
| 0.7.0.01 | Cap retention on `traces/` (auto-delete runs > N days) | [ ] | — | A.9 | Today: manual delete. Add a `traces/.cleanup.sh` cron-style helper once accumulation gets noisy (>50 runs). |
| 0.7.0.02 | Visual run dashboard (web UI for `traces/runs.log`) | [ ] | — | A.10 | Nice-to-have. Current state: `claude /runs` + `./traces/watch.sh` cover the inspection use cases. |
| 0.7.0.03 | Architect-as-PR-reviewer (replaces devops in review role) | [ ] | — | A.18 | **Filed 2026-05-17 — deferred until after Phase 2.** User directive: *"moving forward i want the architect to review the PR and not the devops."* Rationale: architect's option-matrix lens fits PR reviews. Open: does devops stay for build-gate re-verify (ruff/pytest clean-shell) while architect does code-correctness + plan-fidelity, OR does devops fully exit PR-review duty? |
| 0.7.0.04 | Security review bar — Claude-Mythos-style depth | [ ] | — | A.19 | **Filed 2026-05-17 — deferred until after Phase 2.** User directive: *"there will be security review similar to the one that claude mythos provides."* Target: deeper STRIDE per-feature + supply-chain checks (dependency CVE scan, lockfile drift) + secret-rotation hygiene + auth-flow modeling + LLM prompt-injection vectors for scrapers. |
| 0.7.0.05 | PR review verdicts stored in repo (not only GitHub) | [ ] | — | A.20 | **Filed 2026-05-17 — deferred until after Phase 2.** User directive: *"the approved with notes is good but it should be stored in the repo not left on github."* Target: also persist to repo as `traces/<run-id>/pr-reviews/<pr-num>.md` AND/OR `docs/reviews/<pr-num>-<slug>.md`. Survives GitHub data loss; greppable history; works offline. Format: machine-readable per A.16 (now 0.1.0.44). |
| 0.7.0.06 | Progress indicator — flow visualizer like Jenkins pipeline | [ ] | — | A.21 | **Filed 2026-05-17 — deferred until after Phase 2.** User directive: *"somewhere in the PR or in claude code when im building it shows what step we are at."* Three candidate surfaces (architect picks): (a) PR body section auto-updated, (b) chat preamble line, (c) `.claude/current-step.json` file. ~1-2 engineer days. |
| 0.7.0.07 | Confusion-gate clause — ask user on ambiguity even in auto mode; technical decisions OK, business decisions always user | [ ] | HIGH | A.22 | **Filed 2026-05-17 — deferred until after Phase 2 (HIGH within 0.7.0 but cycle defer).** User directive: *"add a clause to always ask questions if the requirement is confusing or there are multiple paths forward."* Scope: extend `docs/PLAYBOOK.md` with explicit confusion-gate clause + decision-class taxonomy (technical vs business vs critical). |
| 0.7.0.08 | State-of-the-art security review tools (replace standard Claude Code hacker tooling) | [ ] | — | A.23 | **Filed 2026-05-17 — deferred until after Phase 2.** User directive: *"instead of using the standard claude code tools for security review."* Target: integrate SoTA SAST/DAST/SCA tools — Semgrep, CodeQL, Snyk/Trivy, gitleaks, pip-audit, OWASP ZAP, plus LLM-specific (prompt-injection detectors, jailbreak scanners). |
| 0.7.0.09 | State-of-the-art architect tools | [ ] | — | A.24 | **Filed 2026-05-17 — deferred until after Phase 2.** Target: add architecture-visualization + ADR tooling — Structurizr (C4 model), Mermaid (sequence + state diagrams, gh-native), PlantUML, diagrams.net, OpenAPI/AsyncAPI generators, dependency graph tools, ADR-tools. |
| 0.7.0.10 | State-of-the-art designer tools — wire huashu-design + similar | [ ] | — | A.25 | **Filed 2026-05-17 — deferred until after Phase 2.** User directive: *"for designer we are not using huashu design tooling. we need to use that."* Target: surface `huashu-design` as PRIMARY designer skill. |
| 0.7.0.11 | Trace analytics dashboard — simple metrics script | [ ] | — | A.26 | **Filed 2026-05-17 — deferred until after Phase 2.** Simple stdout script (`scripts/trace-analytics.py` or `scripts/agent-metrics.sh`) reads `traces/<run-id>/MANIFEST.json` × N + `traces/runs.log` and prints aggregate metrics. NOT a web UI (that's 0.7.0.02). Reads-only the trace logs. ~1 engineer day. |
| 0.7.0.19 | Tighten `JobCreate.source_url` type to `pydantic.HttpUrl` | [ ] | — | A.36 (hacker PR #95 INFO) | **Filed 2026-05-19** — hacker INFO on PR #95. URL validation at boundary catches malformed scraper output. Trivial fix; defer to 0.2.0.06+ when scrapers populate field. |
| 0.7.0.23c | Plan 41 D.7 — `tests/test_workflow_invariants/` commit-time lint suite (parallel-reviewer + deviations + fold-in) | [ ] | MEDIUM | (architect PR #127 HIGH) | **Filed 2026-05-19 via PR #127 architect HIGH partial-impl finding (#130).** Plan 41 D.7 not implemented in PR #127 (omnibus shipped PR-A only). 6 lint tests deferred per architect verdict that "engineer-manual-qa-gate would have caught mass-replace artifacts" — these tests automate that. Wire into CI gate. |
| 0.7.0.23b | Plan 41 D.5 — `MODEL_PICK` trace event in `manager.log` for dispatch-time model decisions | [ ] | LOW | (architect PR #127 HIGH) | **Filed 2026-05-19 via PR #127 architect HIGH partial-impl finding (#129).** Plan 41 D.5 not implemented. Per-dispatch trace event: `[ts] MODEL_PICK agent=<x> model=<opus-4-7\|opus-4-7[1m]> estimated_tokens=<n>` for retrospective analysis of model-selection accuracy. ~10 LOC + 1 test. |
| 0.7.0.23a | Plan 41 D.3 — twin agent variants `architect-1m.md` + `engineer-1m.md` for token-based model selection | [x] | MEDIUM | (architect PR #127 HIGH) | **Filed 2026-05-19 via PR #127 architect HIGH partial-impl finding (#128).** PR #127 shipped manager.md § Token-based model selection prose + flipped architect/engineer base from `[1m]` to non-`[1m]`, but did NOT ship the twin files. Task tool's `model` enum only accepts `sonnet\|opus\|haiku` per plan 41 OQ.1 research — twins are the workaround for fine-grained 1m selection. Ship `architect-1m.md` + `engineer-1m.md` as frontmatter-only copies with `model: claude-opus-4-7[1m]`; manager dispatches to twin variant when scope ≥60K. **CLOSED 2026-05-19 as RESOLVED-BY-DESIGN per user directive (no twin files; manager picks at dispatch via Task  enum override).** Frontmatter stays  default;  override drops  for small dispatches;  for tiny ones. Encoded in . |
| 0.7.0.30 | Generalize agent infra into self-serve Claude plugin (rivals `oh-my-opencode` and similar) | [ ] | — | (user directive 2026-05-20) | **Filed 2026-05-20 via user directive.** Extract `.claude/agents/` + `.claude/skills/` + `.claude/commands/` + `.claude/hooks/` + `.claude/naavik_ops/` (the orchestration system) into a generalized, project-agnostic Claude plugin. Self-serve installation. SQLite base for task management + decisions + lessons + knowledge stores (composes with 0.7.0.29). Should rival `oh-my-opencode` and similar polished plugin ecosystems — best-of-breed UX, easy install, well-documented, cross-project orchestration (same plugin works for any codebase). Sequenced AFTER 0.7.0.29 (which establishes the SQLite-backed agent-ops core). State-of-the-art is moving fast — choose tech at plan time. **ROADMAP row only for now — no plan yet; user said "we can change it later".** Large effort; needs design doc + multi-wave plan + careful extraction of project-specific vs project-agnostic surfaces. |
| 0.7.0.29 | Convert ROADMAP markdown parser to proper agent-ops application (SQLite-backed task management + structured API) | [ ] | — | (user directive 2026-05-20) | **Filed 2026-05-20 via user directive.** Currently `.claude/naavik_ops/lib/roadmap.py` parses ROADMAP.md markdown tables into JSONL on each invocation. Convert to first-class agent-ops application backed by SQLite: task table (id, release, position, priority, status, dependencies, history) + decisions + discussions + knowledge stores all in one DB. Markdown ROADMAP becomes a rendered view OR moves to read-only summary; SQLite is source of truth. Closes the 0.7.0.27 race condition class (issue-map.json:statuses sync gap) by making the DB authoritative. **ROADMAP row only for now — no plan yet; user said "we can change it later".** Large effort; prereq for 0.7.0.30 plugin extraction. Needs design doc + migration runbook (export ROADMAP → SQLite + verify round-trip) + multi-wave plan. State-of-the-art for task-management UX should be inventoried at plan time. |
| 0.7.0.34 | Process fix D (master plan 68 § Why this slipped) — release-cut MILESTONE_GATE requires fresh-install Playwright smoke devops dispatch | [ ] | HIGH | (master plan 68 / owner regression sweep 2026-05-21) | **Filed 2026-05-21 via PLAN_GATE Q4 lock on master plan 68.** Every release-version cut (0.3.X → 0.4.0, etc.) requires a "fresh-install smoke" devops dispatch BEFORE MILESTONE_GATE closes. The dispatch boots clean `rm -rf .naavik && nix run .#dev`, performs Playwright login → edit → save → reload sequence, captures screenshots, attaches to release manifest. Without smoke evidence, MILESTONE_GATE doesn't close. Plan TBD; touches `docs/AGENT_OPS.md § 7` + `docs/PLAYBOOK.md § E` + `naavik-ops release cut` subcommand. Plan author should think Settings UI surface OR `.env.example` slot if any operator-facing knob — NOT a new CLI subcommand. |
| 0.7.0.35 | `engineer-manual-qa-gate` skill must teardown orchestrator before hand-back (close-stray-process invariant) | [ ] | MEDIUM | (owner observation 2026-05-21 run `2026-05-21T15-33-09_fccfbeb5`) | **Filed 2026-05-21.** Engineer dispatch for plan 79 (0.4.0.11) ran `nix run .#dev` for Playwright manual QA capture, then handed back PR URL WITHOUT killing the orchestrator. Owner ran `nix run .#dev` 4hr later → port-5433 conflict → 5 db-restart thrashes → process-compose `Project completed exit 1`. Manager memory note ("Always shut down long-running processes I start") was violated by both engineer + manager (manager didn't catch in PR-review-gate). Fix: extend `.claude/skills/engineer-manual-qa-gate/SKILL.md` to mandate teardown via `pkill -TERM -f "process-compose --no-server"; pkill -TERM -f "fastapi dev src/main.py"` (or equivalent) as final hand-back step. Architect-as-reviewer should verify the `QA_GATE` log line includes `teardown=pass`. ~10 LOC skill + 3 LOC verification regex in `engineer-deviations.log` schema. |
| 0.7.0.36 | Delete auto-seed dev user; force first-time signup (`~/.naavik/dev-credentials` + `NAAVIK_DEV_PASSWORD` + lifespan echo all gone) | [x] | MEDIUM | (owner directive 2026-05-21) | **Plan 83 EXECUTED 2026-05-21 via PR #198 squash `3255caf`.** `src/db/seed.py` (-317 LOC) + `tests/test_seed.py` (-295 LOC) DELETED. `flake.nix` orchestrator collapses `db-init → db → migrate → seed → app` to `db-init → db → migrate → app`. `~/.naavik/dev-credentials` artifact + `NAAVIK_DEV_PASSWORD` env var + lifespan credential echo + WARN block all removed. `src/services/first_run.py` collapses to `FirstRunState(user_count: int)` + `is_first_run = (user_count == 0)`. `/setup-help` payload trimmed to one signal + signup CTA recovery recipe. New regression-lint `tests/test_no_db_seed_imports.py`. `.env.example` slot removed. README + DEPLOYMENT + RUNBOOK § 2.12 + POST_PHASE_1 + CLAUDE.md updated signup-first. Tests 2148 → 2141 (-7 net). Reviewers: hacker APPROVE_WITH_NOTES (1 MED alembic race, severable, filed 0.7.0.37 / Issue #199; all 16 default attack surfaces clear; net security posture IMPROVES — removes plaintext-credential-on-disk surface); architect APPROVE_WITH_NOTES (4/4 D.1+D.2+D.3 PASS; D.4 PIVOT to TestClient + Playwright file:// because of alembic race; filed Issue #199). 1 deviation promoted to archived plan (manual QA pivot). **HARD GAP:** the owner-stated user-visible flow (`rm -rf .naavik/` + `nix run .#dev` + signup) is BLOCKED on the alembic race fix (0.7.0.37) — plan 83 ships the CODE change but the end-to-end orchestrator UX cannot be verified until 0.7.0.37 lands. |
| 0.7.0.37 | Alembic 0001+0002 duplicate-column race blocks fresh-install boot — `SQLModel.metadata.create_all(checkfirst=False)` in 0001_initial creates `Settings.allow_multiple_users`, then 0002_settings_multi_users `op.add_column` the same column → `DuplicateColumn` | [x] | HIGH | (architect PR #198 review 2026-05-21 → Issue #199) | **Plan 84 EXECUTED 2026-05-21 via PR #200 squash `cb60020`.** Option A locked + executed via deviation: synthetic-MetaData helper clones LIVE tables + applies a documented delta map (`_TABLES_CREATED_LATER` × 7 tables; `_COLUMNS_ADDED_LATER` × 5 tables × 32 columns; `_RESTORED_COLUMNS` × 5 vault Settings cols; `_type_overrides_for` × 3 columns for source/visa_restrictions/closed_reason). Same logical outcome as explicit `op.create_table` × 20 but ~360 LOC vs ~600. `0001_initial.py` 48 → 390 LOC. Chain-replay test (`tests/test_alembic_0001_chain_replay.py`, +204 LOC, 3 tests: 1 unconditional AST guard + 2 PASS under `NAAVIK_LIVE_DB=1`) verifies `alembic upgrade head` from empty DB ends in same schema as `metadata.create_all(LIVE)`. Bonus fold-in: pre-existing VARCHAR(32) alembic_version overflow fixed via `env.py` `DefaultImpl.version_table_impl` monkey-patch to VARCHAR(64) (4 revision IDs >32 chars). Tests 2141 → 2142. **Manual QA passed: `rm -rf .naavik/` + `nix run .#dev` → fresh-install boot clean → /login signup CTA → signup → onboarding round-trip with 4 screenshots.** Owner-visible signup flow now works end-to-end. Reviewers: hacker APPROVE_WITH_NOTES (1 LOW + 3 INFO; no security regression; delta map verified exhaustive against 0002-0022); architect APPROVE_WITH_NOTES (D.1+D.2+D.3 all PASS; synthetic-helper deviation ACCEPT; VARCHAR(64) fold-in ACCEPT; 1 MED + 1 LOW filed as 0.7.0.38). 2 deviations promoted to archived plan. |
| 0.7.0.38 | Rename 4 over-32-char alembic revision IDs (0011/0015/0016/0018) so existing operators with VARCHAR(32) can upgrade | [ ] | MEDIUM | (architect PR #200 review 2026-05-21 → Issue #201) | **Filed 2026-05-21 via PR #200 architect review.** Plan 84's env.py VARCHAR(64) fold-in only protects FRESH installs (where alembic CREATES the `alembic_version` table). Existing operators with partially-migrated DBs have the original VARCHAR(32) column; their next `alembic upgrade` will choke when one of the 4 long IDs (`0011_*`, `0015_*`, `0016_*`, `0018_*`) doesn't fit. Fix: shorten the 4 revision IDs to ≤32 chars (`alembic.config` `truncate_slug_length=32` was the canonical mechanism but was bypassed at author time). Path: rename the 4 versions + update `down_revision` chains + update `alembic_version` data via auto-applied migration. ~30 LOC + 1 test (verify all revision IDs ≤32 chars). |
| 0.7.0.39 | Fresh-install signup UX bug cluster (3 fixes: HX-Redirect URL + browser-direct 401 redirect + HTMX 4xx swap) | [x] | HIGH | (manager-direct triage 2026-05-21 → Issue #202) | **EXECUTED 2026-05-21 via PR #205 squash `8428005`.** 3-bug cluster surfaced when user actually tried fresh `nix run .#dev` + signup post plan 83 + 84. Bug 1: `HX-Redirect: /auth/login` (dead URL — plan 75 regression) → `/login`. Bug 2: browser-direct unauth nav got bare 401 → new 307 + `Location: /login` via `_raise_unauthenticated` helper. Bug 3: HTMX 2.x doesn't swap 4xx by default → `hx-target-error="#login-card"` on form so error card renders. Tests 2142 → 2143 (+1 net; full 4-case unauth matrix in `test_auth_401_hx_redirect.py` + 5 sibling test files updated). 4 distinct md5 manual QA screenshots verified (end-to-end signup → onboarding). Reviewers: hacker APPROVE_WITH_NOTES (0 actionable; 4 INFO); architect APPROVE_WITH_NOTES (1 LOW doc-staleness + 3 INFO). 2 engineer deviations promoted (worktree isolation; chr(64)+chr(46) email-fixture pattern). |
| 0.7.0.41 | Engineer agent dispatches with possibly-concurrent file area SHOULD use `Agent(isolation="worktree")` parameter | [ ] | MEDIUM | (architect PR #205 review 2026-05-21; engineer-39 + engineer-85 parallel-dispatch hazard) | **Filed 2026-05-21 via PR #205 architect review.** Engineer-85 reported HEAD reset to engineer-39 branch twice mid-run during parallel dispatch. Engineer-39 worked around by manually checking out a worktree at `/tmp/naavik-0.7.0.39`. Manager should default to `isolation="worktree"` on parallel multi-engineer dispatches; or better, the parallel-dispatch pattern itself should be conventionalized in manager.md § Dynamic resource allocation. ~10 LOC manager.md edit + add to dispatch-pattern playbook. |
| 0.7.0.42 | Document `chr(64) + chr(46)` Playwright email-fixture pattern (evades runtime string-rewrite of literal `[email protected]`) | [ ] | LOW | (engineer PR #205 deviation; architect INFO) | **Filed 2026-05-21 via PR #205 architect review INFO.** Engineer-39 needed `chr(64) + chr(46)` concat to evade an upstream agent-test-harness layer rewriting hardcoded `[email protected]` strings. Surface this in `docs/AGENT_OPS.md` so future Playwright authors aren't surprised. ~10 LOC doc edit. |
| 0.7.0.43 | Orchestrator `db-init` strips inherited POSIX ACLs from `.naavik/db/` before PG starts (PG14+ rejects 0770 even when caused by inherited-ACL mask) | [x] | MEDIUM | (owner bug report 2026-05-22) | **SHIPPED 2026-05-22 via PR #209 squash `4596972`.** Owner-reported bug — PG `FATAL: data directory has invalid permissions` due to inherited `default:user:hermes:rwx` POSIX ACL on `~/personal/dev/naavik/` parent (pair-sharing with local hermes user). Subdirs inherit default ACL at creation → `drwxrwx---+` (mode 0770 via ACL mask) → PG14+ rejects. **Round 1 strategy (`mkdir -p .naavik/db` pre-create + child strip) BROKEN per architect HIGH F1**: services-flake gates initdb on `[[ ! -d $PGDATA ]]`; pre-creation skipped initdb on first install → `pg_ctl: not a database cluster directory`. **Round 2 fold-in fix**: strip the PARENT default ACL only (`mkdir -p .naavik` + `setfacl -k .naavik`); wrap child cleanup in `if [ -d ./.naavik/db ]` so first-boot lets services-flake's initdb create the dir cleanly under the de-ACL'd parent; second+ boot strips access + default ACLs + chmods 700 on existing dir. Empirically verified in /tmp sandbox: parent strip → fresh child mkdir produces `drwxr-xr-x` clean (no `+`). Also added `docs/RUNBOOK.md § 2.13` entry (symptom + auto-fix + manual recovery recipe) + dropped `2>/dev/null` stderr redirect for diagnostic visibility (architect F2+F3). Reviewers: hacker round-1 APPROVE_WITH_NOTES (1 INFO; closed in round-2 F3) + round-2 APPROVE (0 findings); architect round-1 REQUEST_CHANGES (HIGH+MED+LOW+INFO) + round-2 APPROVE_WITH_NOTES (2 LOW non-blocking). |
| 0.7.0.44 | APScheduler `[db]` ERROR-log spam on every orchestrator boot — duplicate-key INSERT noise from `NaavikJsonJobStore.add_job` | [~] | MEDIUM | (owner bug report 2026-05-22) | **IN FLIGHT — owner-reported.** Every orchestrator boot emitted ~16 noisy `[db] ERROR: duplicate key value violates unique constraint "apscheduler_jobs_pkey"` lines (one per registered job: `scraping.linkedin`, `scraping.workday`, `scraping.greenhouse`, etc.). Root cause: `NaavikJsonJobStore.add_job` used a bare `INSERT` + caught `IntegrityError` → raised `ConflictingIdError`. APScheduler's `Scheduler.add_job(..., replace_existing=True)` correctly caught it + fell back to `update_job` → behavior was correct, but Postgres logged the failed INSERT as a server-side ERROR line BEFORE the Python catch fired. Fix: switch to dialect-aware `INSERT ... ON CONFLICT (id) DO NOTHING` (Postgres + sqlite both support since SQLite 3.24); detect no-op via `result.rowcount == 0` and raise `ConflictingIdError` so APScheduler's update_job fallback still fires unchanged. Legacy IntegrityError-catch path preserved for non-PG/sqlite dialects as defense-in-depth. 2 new tests pin the contract (`test_add_job_raises_conflicting_id_on_duplicate` + `test_add_job_uses_dialect_aware_on_conflict_for_postgres_and_sqlite` + `test_add_job_then_update_job_via_store_api_round_trip`). |
| 0.7.0.45 | Login UX dead-end: `/login?mode=signup` on single-user instance showed "already has an account" banner with NO signin form below — operator stuck | [~] | MEDIUM | (owner bug report 2026-05-22) | **IN FLIGHT — owner-reported.** When `_compute_signup_disabled=True` (user_count >= 1 AND `Settings.allow_multiple_users=False`) AND operator visited `/login?mode=signup`, template rendered ONLY the amber banner with no form. Operator had to navigate back to `/login` manually to actually sign in. Fix: rewrite `is_signup` flag at top of `login.html` template — `_requested_signup and not signup_disabled` collapses to False when gated; the heading, form-action, button label, password complexity hint, and SSO info card all then render in signin mode. Banner still appears explaining why signup is gated, but the signin form renders below so operator can immediately sign in. "First time? Create account" CTA hidden when `signup_disabled` (would just bounce back to same banner). 1 existing test extended + 1 new test (`test_signup_disabled_renders_welcome_back_header`) pin heading/CTA/SSO-card coherence. |
| 0.7.0.33 | Process fix C (master plan 68 § Why this slipped) — `engineer-manual-qa-gate` requires QA_GATE log + screenshot evidence per surface | [ ] | HIGH | (master plan 68 / owner regression sweep 2026-05-21) | **Filed 2026-05-21 via PLAN_GATE Q4 lock on master plan 68.** `engineer-manual-qa-gate` skill exists but enforcement is honor-system. Every plan execution must append `[ts] QA_GATE surface=<...> outcome=pass evidence=<screenshot-path>` to `traces/<run-id>/engineer-deviations.log` for each user-facing surface affected. Manager / architect verifies the line at PR_REVIEW_GATE; absence blocks merge. Plan TBD; touches `.claude/skills/engineer-manual-qa-gate/SKILL.md` + `docs/AGENT_OPS.md § 7.2` + `naavik-ops plan archive` (could validate QA_GATE log line presence). Owner regression sweep on 2026-05-21 traced 5 of 8 bugs to this gate not running. |
| 0.7.0.32 | Process fix B (master plan 68 § Why this slipped) — `architect-plan-quality-bar` skill adds 12th check: "follow-up filed in current release" | [ ] | HIGH | (master plan 68 / owner regression sweep 2026-05-21) | **Filed 2026-05-21 via PLAN_GATE Q4 lock on master plan 68.** Extend `architect-plan-quality-bar` skill from 11 items to 12: "did the plan deliver every Build sequence item, OR did each descoped item get a follow-up plan filed in the CURRENT release version?" Architect REQUEST_CHANGES on any plan whose archive moves >30% scope to a later release without a same-release follow-up. Plan TBD; touches `.claude/skills/architect-plan-quality-bar/SKILL.md` + arguably the architect agent prompt body. Pair with 0.7.0.31 (the `naavik-ops plan archive` enforcement). |
| 0.7.0.31 | Process fix A (master plan 68 § Why this slipped) — `naavik-ops plan archive` refuses >30% descope without same-release follow-up | [ ] | HIGH | (master plan 68 / owner regression sweep 2026-05-21) | **Filed 2026-05-21 via PLAN_GATE Q4 lock on master plan 68.** Today `naavik-ops plan archive` validates `## Deviations from plan` section non-empty but doesn't quantify descope severity. Extend with descope-fraction analyzer: parse Build sequence items from plan body, count how many engineer-deviations.log entries mark `kind=descoped`, compute fraction. If ≥30% AND no follow-up row exists in the SAME release version (e.g. `0.2.X` plan → `0.2.X` follow-up; not `0.8.0`) → exit 2 with operator hint. Plan 60 → 0.2.7.17a → 0.8.0.38 path is exactly what should have BLOCKED but didn't. Plan TBD; touches `.claude/naavik_ops/plan.py` + tests + AGENTS § Workflow step 7. |
| 0.7.0.28 | `prepare-commit-msg` hook regex doesn't match 4-level+alpha task-ids (e.g. `0.2.0.07a`) — Closes-N append silently skips | [ ] | LOW | (dogfood discovery 2026-05-20) | **Filed 2026-05-20 via /build run pre-engineer dispatch.** `.claude/hooks/git/prepare-commit-msg:BRANCH_REGEX` currently `^(feat\|fix\|chore\|docs\|refactor)/([A-Z]+\.[0-9]+[a-z]?\|PC\.[0-9]+\|DEF-[0-9]+\|A\.[0-9]+\|[0-9]+\.[0-9]+\.[0-9]+(\.[0-9]{2})?)-[a-z0-9-]+$`. The 4-level alternative is `[0-9]+\.[0-9]+\.[0-9]+(\.[0-9]{2})?` — only `.dd` (2 digits), no alpha suffix. Branches like `feat/0.2.0.07a-url-slug-validate` fall through. Workaround: engineer manually adds `Closes #N` to commit body. Fix: widen 4-level alt to `[0-9]+\.[0-9]+\.[0-9]+(\.[0-9]{2}[a-z]?)?` to allow optional trailing alpha. ~1 LOC + 2 hook tests (positive + negative). |
| 0.7.0.27 | `naavik-ops task next-unblocked` returns done tasks — `issue-map.json:statuses` never populated by `gh set-status` | [x] | MEDIUM | (dogfood discovery 2026-05-20) | **EXECUTED 2026-05-20 — Option (b) shipped per user directive: ROADMAP-as-source-of-truth.** `naavik-ops task list / next-unblocked` now read `[ ]/[~]/[x]` status + priority directly from ROADMAP via the existing `lib.roadmap.parse_release_section(..., roadmap_text=...)` parser. Issue-map cache consulted ONLY for display-only Issue # cross-ref. New `--include-done` flag on `task list` (hidden by default). New tests: `test_list_hides_done_by_default`, `test_list_include_done`, `test_skips_done_picks_next_open`, `test_all_done_returns_no_unblocked`, `test_blocked_by_unshipped_dep_skips`. 258 `tests/test_naavik_ops/` pass. **GitHub Project board sync is deferred** per user "forget about it for now" directive — re-engaging post-0.7.0.29 SQLite migration. |
| 0.7.0.26 | Remove session-handoff file convention — `/build` + `naavik-cold-start` + ROADMAP + memory = sufficient state | [x] | LOW | (user critique 2026-05-19) | **CLOSED 2026-05-20 — manager-direct.** Deleted `docs/prompts/00-session-continue.md` (created earlier same session as bloat-shaped redundancy with canonical surfaces). User critique: "why do i need a handoff why cant i just do /build isnt that what we should do? this should already be in memory atleast the stuff that matters right?" Confirmed: ROADMAP § Index/Phase status/Active conventions + memory decisions store + git log + `naavik-ops task next-unblocked` cover everything the handoff was duplicating. Codifies principle: new sessions cold-start via `naavik-cold-start` skill + run `/build`; no separate handoff file needed. Surfaced 0.7.0.27 bug as side-effect (next-unblocked staleness). |
| 0.7.0.25 | Doc-sizing matrix (small/medium/large impact → matched doc output) + small-task batching (5-10 items per housekeeping plan) | [x] | MEDIUM | (user directive 2026-05-19) | **CLOSED 2026-05-19 — manager-direct edit per § Dynamic resource allocation.** Added `manager.md § Doc-sizing matrix` (3 tiers: ≤30 LOC mechanical = no plan; 30-200 LOC = short plan; >200 LOC = full plan) + `§ Batching small tasks` (cap 5-10 items per `docs/plans/<NN>-housekeeping-batch-<YYYY-MM-DD>.md` plan; no option matrices, no PLAN_GATE pause for batched mechanical work). Operationalizes user directive: "we should have 3 levels small issues causing small documents. medium issues and large issues ... for small tasks like this we need to batch these into some sort single plan which covers multiple such things. usually 5 to max 10 that will make it way more effecient." |
| 0.7.0.24 | Requirement-slot feedback cadence — manager prefixes every user-directive response with a one-line `Slotted: <task-id> — <title> — <plan> — <PR>. Status: ...` ack | [~] | MEDIUM | (user directive 2026-05-19 — audit request) | **Filed 2026-05-19 via user audit "where did we log all the requirements i gave can you give me a rundown again and where they get slotted. and which ones did we already do? we need to make this a cadence you need to show me the version you added each one to when i give you requirements".** Codified in `manager.md` § Requirement-slot feedback (new section before § Dynamic resource allocation). Cadence rule: every new directive (not status query, not gate response) gets the slot ack as FIRST line of manager response. Includes the auto-log routing table (code/decision/discussion/run-event). Folded into PR #127 (omnibus). |
| 0.7.0.23 | Manager evolution + model selection + bookkeeping fold-in (plan 41 PR-A activated via PR #127 — D.3/D.5/D.7 deferred to 0.7.0.23a/b/c) | [~] | MEDIUM | (user directive) | **Plan 41 PR-A activated 2026-05-19 via PR #127 omnibus. PR-A shipped:** (a) manager staff-engineer coding lane (manager.md § Dynamic resource allocation); (c) timing-based bookkeeping fold-in (manager.md + PLAYBOOK.md § I revised); (b-partial) base model flip on architect/engineer to non-`[1m]` + § Token-based model selection prose. **D.3 twin agents → 0.7.0.23a (#128) deferred.** **D.5 MODEL_PICK trace → 0.7.0.23b (#129) deferred.** **D.7 lint suite → 0.7.0.23c (#130) deferred.** **D.8 two-PR sequencing → folded into followups.** Original plan body at Bundles 5 interrelated process changes: (a) manager staff-engineer coding lane (manager.md:9 + Anti-patterns revision); (b) twin agent variants (`architect-1m.md` + `engineer-1m.md`) for token-budget-based model selection (60K threshold; <60K → `opus-4-7`; ≥60K → `opus-4-7[1m]`); (c) timing-based bookkeeping fold-in (fold when PR open + related; exceptions: gitignored, security, personal); (d) `tests/test_workflow_invariants/` 6-lint suite shifting detection from PR-review-time → commit-time; (e) two-PR sequencing. Empirical evidence from run `2026-05-19T15-42-42_833f4a`: 14 direct-push bookkeeping commits since PR #91 (11 foldable); 22 architect dispatches range 38-260K (18% under 50K, 27% under 60K); parallel-reviewer invariant violated 2x; 5/8 plans archived w/o deviations section. |
| 0.7.0.22 | ROADMAP Backlog section + parser/task/sync recognition | [~] | MEDIUM | (plan 40) | **Plan 40 IN FLIGHT 2026-05-19 — DRAFT at `docs/plans/40-roadmap-backlog-section.md`.** Adds `## Backlog (unprioritized)` section to ROADMAP for future-eligible-but-unprioritized work. Teaches `.claude/naavik_ops/{lib/roadmap.py, task.py, gh.py}` to recognize as synthetic release-version `backlog`. First migrant: `0.2.0.14` n8n migration deferred (not closed-as-moot — historical-data import still possible). Engineer dispatch pending. |
| 0.7.0.21d | Partial-write rollback in `naavik_ops/plan.py` — `git mv` failure leaves plan mutated at source; capture pre-write text + restore on failure | [ ] | LOW | (hacker PR #120) | **Filed 2026-05-19 via PR #120 hacker review (#124).** Defense-in-depth: any `git mv` error (dirty tree, file outside repo) leaves plan file mutated at original path with section + Status: EXECUTED. Operational confusion, not exploitable. Fix: `try/except` around the file-write + git-mv pipeline; restore on failure. ~10 LOC + 2 tests. |
| 0.7.0.21c | `validate-deviations` should accept paragraph-style "No material deviations" as PASS, not just bullets | [x] | LOW | (dogfood discovery) | **Filed 2026-05-19 via dogfood scan.** Plan 32 retrofit uses paragraph form ("No material deviations.") — `validate-deviations` currently BLOCKs it. Either: tighten regex to detect non-empty content + sentinel phrase, OR convert plan 32 retrofit to bullet form. ~5 LOC + 1 test. **CLOSED 2026-05-19 via PR #127 squash 473d9b6 — anchored regex  multiline; 7 regression tests in test_plan.py::TestNoMaterialDeviationsSentinelAnchored.** |
| 0.7.0.21b | Bullet shape — `entry_to_bullet` should insert periods between `what`/`why`/`impact`/`surface` per plan 39 § C.7 | [ ] | LOW | (architect PR #120) | **Filed 2026-05-19 via PR #120 architect review (#122).** Cosmetic — current output reads `what: X why: Y impact: Z surface: S` without periods. ~1 LOC + 1 test. |
| 0.7.0.21a | Path-traversal hardening in `naavik_ops/plan.py:_resolve_plan_path` — constrain to `docs/plans/` via `relative_to` + reject if `ARCHIVE_DIR in parents` | [ ] | MEDIUM | (hacker PR #120) | **Filed 2026-05-19 via PR #120 hacker review (#121).** `_resolve_plan_path` accepts any `.md` path on disk — operator-only attack surface but allows mutation of arbitrary repo `.md` with Status: EXECUTED + forged Deviations. Fix: scope-check against `PLANS_DIR`; reject if path already in `ARCHIVE_DIR`. ~5 LOC + 2 tests. |
| 0.7.0.21 | `naavik-ops plan archive` subcommand — hard-stop deviation promotion at archive | [x] | HIGH | A.39 | **Plan 39 EXECUTED 2026-05-19 via PR #120 squash `b4346f4` (closes #119).** New `.claude/naavik_ops/plan.py` provides `plan archive <plan-path>` (canonical/only archive path) + `plan validate-deviations` (read-only PASS/BLOCK). Three UX paths: happy / empty-section-empty-log refusal exit 2 / `--no-material-deviations "<rationale>"` explicit / `--force` short-circuit. Replaces manual `git mv` per AGENTS step 7-8 + PLAYBOOK § I + CLAUDE.md + AGENT_OPS § 2.7a + manager.md step 11 + engineer.md hand-back. 22 net-new tests. **Dogfood**: plan 39 archived via the new command (5 bullets lifted from `engineer-deviations.log`). Reviewers: hacker APPROVE_WITH_NOTES (1 MEDIUM `0.7.0.21a` + 1 LOW `0.7.0.21d`); architect APPROVE (1 LOW `0.7.0.21b`). Plus 1 dogfood-discovery follow-up `0.7.0.21c`. 5 engineer deviations promoted via the new command's own pipeline. |
| 0.7.0.20 | Codify parallel reviewer invariant — manager.md hard-stop pre-flight + PLAYBOOK.md alignment | [x] | — | A.38 | **EXECUTED 2026-05-19 via PR #99 squash `08d2e3a`.** Added § Parallel reviewer invariant to `.claude/agents/manager.md` (top-level non-negotiable section between § Identity invariant and § GitHub state) with explicit pre-flight check: before submitting any reviewer-dispatch message, verify it contains TWO `Agent` tool calls (hacker + architect), not one. Strengthened `docs/PLAYBOOK.md` § D step 3 + § F step 9 + § H step 7 with identical single-message-two-Agent-calls language. Self-approval pivot symmetrized to existing `.claude/memory/knowledge/hacker-self-approval.md` (dispatch always, GitHub state pivots to COMMENTED). Captured after run `2026-05-19T15-42-42_833f4a` violated the invariant twice in one run despite existing language in 4 places. Architect REQUEST_CHANGES round-1 (HIGH scope leak from wrong branch base) resolved by rebase on main + force-push; both reviewers APPROVE round-2. |
| 0.7.0.18 | `JobUpdate` schema should `exclude` the `score` field (computed by scoring pipeline) | [ ] | — | A.35 (hacker PR #95 INFO) | **Filed 2026-05-19** — hacker INFO on PR #95. Prevents accidental score overwrite via API. Trivial fix; defer to 0.3.0 scoring sub-task. |
| 0.7.0.17 | `JobScrapeRun.errors[]` ARRAY needs schema guard against secret leakage | [ ] | — | A.34 (hacker PR #95 LOW) | **Filed 2026-05-19** — hacker LOW on PR #95. Forward-looking: Phase 2 scrapers may capture exception messages with tokens/cookies/URL params. Add `_redact_url` helper + cap entries at 200 chars. Fix BEFORE 0.2.0.07 (site scrapers wire exception capture). |
| 0.7.0.16 | Tighten visa_restrictions backfill: replace `LIKE '%sponsorship%'` with exact-match SQL CASE | [ ] | — | A.33 (hacker PR #95 LOW) | **Filed 2026-05-19** — hacker LOW on PR #95. Migration backfill over-broad; misclassifies hypothetical no_sponsorship strings as candidate-friendly. Trivial SQL CASE rewrite; defer to 0.2.5 cleanup cycle. |
| 0.7.0.15 | Add `user_id` boundary check to `archive_job` / `restore_job` (IDOR pattern propagation) | [x] | MEDIUM | A.32 (hacker PR #95 MEDIUM) | **CLOSED via fold-in to PR #112 (`0.2.0.11`) squash `47d78ec`.** `archive_job(session, *, user_id, job_id)` + `restore_job(...)` now require user_id; raise `PermissionError` on cross-user mutation. UI wiring in 0.2.0.11 routes uses scoped helpers + `_job_or_404` conflates `None/wrong_user/soft_deleted → 404` (no enumeration leak). 2 new IDOR tests in `tests/test_job_service.py`. |
| 0.7.0.14 | Fix 2 pre-existing pytest failures on main (`test_lazy_visit_shows_cta_no_draft` + `test_login_signup_mode_renders_signup_form`) | [ ] | — | A.31a (engineer deviation #4 from plan 27) | **Filed 2026-05-19** — discovered during plan 27 PR #95 dispatch. Both fail on a fresh `main` worktree, unrelated to job-models work. PR #95 deselects them for green-bar; this row tracks the proper fix. Likely flaky fixtures / state assumption. |
| 0.7.0.13 | Fix `task move` position-stability + add `gh clear-priority` + codify principle in 5 docs + restore 12 corrupted GH titles | [x] | MEDIUM | A.31 | **Plan 28 EXECUTED 2026-05-19 via PR #94 squash `70ee0d3` (closes #93).** Bug surfaced in run `2026-05-19T05-40-56_194aa5` when `task move 0.2.0.02 0.2.1.05` auto-renumbered ~10 sibling tasks. Shipped: (A) `cmd_move` gap-only on source + reject-on-collision on dest; (B) new `gh clear-priority` subcommand via `clearProjectV2ItemFieldValue` GraphQL mutation; (C) 5-doc codification of patch-position-stability principle; (D) 12 GH titles restored (`#62→[0.2.0.03]`, `#70→[0.2.0.04]`, `#15→[0.2.0.05]`, `#10→[0.2.0.06]`-`#19→[0.2.0.14]`; `#21` correctly at `[0.2.1.05]`). 4 commits W0-W3 + audit anchor; 217 tests passing (+8 new). Hacker APPROVE (2 INFO non-blocking); architect APPROVE FULL spec match (1 INFO naming). Memory knowledge entry at `.claude/memory/knowledge/patch-version-position-stability.md`. 3 deviations promoted to archived plan. |
| 0.7.0.12 | `develop` branch workflow — move PR target away from `main` | [ ] | — | A.27 | **Filed 2026-05-17 — deferred (user-flagged future move).** Action when scheduled: (1) create `develop` branch from `main`; (2) shift PR targets from `main` to `develop`; (3) keep `main` as release/stable + periodic `develop → main` promotion PR; (4) update PLAYBOOK + manager + repo settings. Open: BOOKKEEPING commits routing TBD. **Couples with A.29 release-tag implications** — release branches per MINOR? Release-branch promotion ceremony? A.27 owns the decision. |

**Deliverable (0.7.0):** Pickup queue for quality-of-life work after the scrapers ship. New rows added via `.claude/naavik-ops gh create-issue 0.7.0.NN "..." --milestone "0.7.0"`; rows graduate to active `0.2.X` or `0.3.0`+ release when scheduled.

---

### 0.8.0 — Future enhancements (low priority)
> **Goal:** Long-tail product capabilities not blocking near-term roadmap. Items here are deferred because they require significant design effort + integration work + are NOT critical to the self-hosted single-user MVP. They graduate to an active release only when explicitly scheduled.
> **Status:** ⚪ Future (low priority).
> **Plan:** Per-row plans authored when picked up; some may need a design doc graduation first.

| # | Task | Status | Priority | Legacy ID | Notes |
|---|---|---|---|---|---|
| 0.8.0.01 | OIDC for self-hosted (Authentik / Keycloak / Okta) | [ ] | — | DEF-06 | **Moved 2026-05-20 from 0.2.7.08 → 0.8.0.01 per user directive.** Significant Phase 2+ feature requiring full integration design + per-provider testing. Needs design doc covering: discovery flow, claims mapping, group sync, JWT vs session-cookie session model, multi-tenant key isolation. From SCREENS.md § Phase mapping > Deferred. |
| 0.8.0.02 | Light mode | [ ] | — | DEF-18 | **Moved 2026-05-20 from 0.2.7.09 → 0.8.0.02 per user directive.** Phase 6 explicit marker. Needs design doc covering: light-mode token palette (vs DESIGN.md dark palette), per-tab override, system-preference detection, surface inventory across 11 screens. From DESIGN.md. |
| 0.8.0.03 | DATA_MODEL.md graduation for `ProfileAnswer` + `JobEmbedding` | [ ] | MEDIUM | (architect PR #162 HIGH) | **Filed 2026-05-20 via PR #162 architect review.** Plan 61 § H listed 8 specific lifts into `docs/design/DATA_MODEL.md` (§ B entity inventory bump 20→22; § C add 2 SQLModel defs; § G index rows; § H/J Phase-deferred prose update; § L Settings rows; § N locked decisions). None landed in PR #162 per user "defer→0.8.0" directive. Doc-only surgery; ~150 LOC. |
| 0.8.0.04 | Add `Depends(require_csrf)` to `POST /api/v1/profile-answers/{id}/accept` | [ ] | MEDIUM | (hacker PR #162 MED-1) | **Filed 2026-05-20 via PR #162 hacker review.** Only state-changing API route in codebase missing CSRF dep. Template at `_profile_answer_diff.html:46` already sends `X-CSRF-Token`; server ignores it. SameSite=Strict mitigates but same-site XSS + browser extensions bypass. Same pattern as 0.2.7.25/26. ~3 LOC + 2 tests. |
| 0.8.0.05 | Tighten `test_no_cross_user_embedding_reads.py` regex to require `<Table>.user_id ==` exactly | [ ] | LOW | (hacker PR #162 LOW-2) | **Filed 2026-05-20 via PR #162 hacker review.** Current regex `\.user_id ==` matches any `.user_id ==` in an 8-line window; widen window to 16 lines + require table-qualified prefix. Not exploitable today (all 7 shipped reads correctly scope). ~10 LOC lint + 1 fixture test. |
| 0.8.0.06 | Wire `Settings.semantic_match_sync_on_upsert` into `job_service.upsert_job` | [ ] | LOW | (engineer dev #4 PR #162) | **Filed 2026-05-20 via PR #162 engineer deviation.** Settings column shipped but engineer descoped the actual wiring — toggle currently no-ops. Plan 61 D3 promised optional sync trigger gated by this toggle. ~15 LOC + 2 tests. Requires `embedding_service.embed_one_job` exposed. |
| 0.8.0.07 | Collapse `profile_answer_service.fingerprint` + `document_generator.question_fingerprint` duplicates | [ ] | LOW | (engineer dev #6 PR #162) | **Filed 2026-05-20 via PR #162 engineer deviation + architect dev-6 disposition.** Two fingerprint impls coexist; profile_answer_service is canonical per plan 61 D6, but document_generator predates plan 61. Migrate `document_generator` callsites to `profile_answer_service.fingerprint`. ~20 LOC + 3 tests. |
| 0.8.0.08 | HNSW perf test behind `NAAVIK_LIVE_DB=1` gate | [ ] | LOW | (engineer dev #8 PR #162) | **Filed 2026-05-20 via PR #162 engineer deviation.** Plan 61 § E promised HNSW build-time perf test on 10K-row fixture; deferred for cost. Gate behind NAAVIK_LIVE_DB to keep default `pytest -x` fast. ~50 LOC test. |
| 0.8.0.09 | `maintenance_work_mem >= 256MB` HNSW recommendation in `docs/RUNBOOK.md` | [ ] | LOW | (hacker PR #162 follow-up) | **Filed 2026-05-20 via PR #162 hacker review.** Hacker noted operational gotcha — HNSW index build / vacuum requires bumped `maintenance_work_mem` on self-host postgres for sub-100K-row datasets. Doc-only; ~5 LOC RUNBOOK update + cross-ref. |
| 0.8.0.10 | Per-source trust-score weighting on Phase 6 similar-jobs surface (poison-vector mitigation) | [ ] | LOW | (hacker PR #162 INFO-3) | **Filed 2026-05-20 via PR #162 hacker review.** Adversarial scraper-controlled `Job.description` could craft embeddings that always score high cosine similarity once a similar-jobs UI ships. Mitigation: weight cosine score by `JobSource` trust-tier (n8n_legacy / company_direct = trusted; indeed / linkedin = neutral; rsshub = lower). Compose with 0.3.0 scoring plan. |
| 0.8.0.11 | `verify_jwt_async` `tid` claim validation (defense-in-depth) | [ ] | HIGH | (hacker PR #163 HIGH-1) | **Filed 2026-05-21 via PR #163 hacker review.** `src/services/auth.py:282-330` extracts `sub`/`jti`/`exp` but never validates the `tid` claim against the `tenant_id` kwarg. Defense-in-depth gap. ~10 LOC + 2 tests. Composes with 0.8.0.12. |
| 0.8.0.12 | Thread `tenant_id` through `get_current_user` + `require_authed_session` (multi-tenant resolver) | [ ] | HIGH | (hacker PR #163 HIGH-2) | **Filed 2026-05-21 via PR #163 hacker review.** `src/services/auth.py:486,565` call `verify_jwt_async(session, token)` without `tenant_id=...`; default kicks in to `tenant_id=1`. Acceptable today (single-tenant) but blocks multi-tenant rollout. Wire tenant resolver: extract from JWT `tid` claim OR session; fall back to `_SELF_HOST_TENANT_ID` for self-host. ~30-40 LOC + 4 tests. |
| 0.8.0.13 | Surface env-legacy DB-pinned snapshot in Settings UI + README | [ ] | MEDIUM | (hacker PR #163 MED-2) | **Filed 2026-05-21 via PR #163 hacker review.** Alembic 0014 captures `SECRET_KEY` env at migration time → persists in DB forever as `kid="env-legacy"` HS256 row. Operator mental model ("rotate via .env") becomes silently wrong. Settings UI should display the env-legacy row's status + "rotated to RS256 — env SECRET_KEY no longer read at runtime" indicator. README update for the same. ~30 LOC + doc + 2 tests. |
| 0.8.0.14 | JWT cadence-driven rotation cron (90-day automatic rotation) | [ ] | MEDIUM | (architect PR #163 MED) | **Filed 2026-05-21 via PR #163 architect review.** Plan 62 D3 promised hybrid 90-day cron + on-demand UI; engineer shipped manual UI + grace-expiry cron but NOT cadence-driven cron. Add `admin.rotate_signing_keys_cadence` APScheduler job (daily 03:15 UTC; rotate if `created_at + jwt_rotation_days < now`). ~50 LOC + 3 tests. |
| 0.8.0.15 | `docs/design/JWT_ROTATION.md` design-doc graduation | [ ] | MEDIUM | (architect PR #163 HIGH) | **Filed 2026-05-21 via PR #163 architect review.** Plan 62 § H specified `docs/design/JWT_ROTATION.md` heading layout for graduation. Engineer deferred per user "defer→0.8.0" directive. Doc-only surgery; ~200 LOC lift from plan § A + § B + § C + § D + § F into the new canonical file. Same pattern as 0.8.0.03 (DATA_MODEL.md graduation for plan 61). |
| 0.8.0.16 | README + CLAUDE.md surface propagation for `/settings/security` + cron + Settings columns | [ ] | LOW | (architect PR #163 INFO) | **Filed 2026-05-21 via PR #163 architect review.** New operational surfaces shipped via plan 62: `/settings/security` route, `admin.expire_retiring_signing_keys` cron, `jwt_rotation_days` + `jwt_rotation_grace_days` Settings columns, `tenant_signing_key` table, alembic 0014 + 0015. Per CLAUDE.md § Deviations workflow these should propagate to README/CLAUDE.md. Doc-only; ~30 LOC. |
| 0.8.0.17 | ATS adapter D.5 second-gate enforcement (compare `SubmissionResult.confidence` vs `auto_apply_adapter_confidence_threshold` in `application_service`) | [ ] | MEDIUM | (architect PR #164 MED) | **Filed 2026-05-21 via PR #164 architect review.** Plan 63 D.5 promised dual auto-approve gate; PR #164 ships the infra (Settings column + alembic 0016 + SubmissionResult.confidence field) but `application_service.submit()` never compares. HTTP adapters emit confidence=1.0 so always passes; gate enforcement opens at first Generic-adapter submission. ~20 LOC + 4 tests. |
| 0.8.0.18 | README + CLAUDE.md + POST_PHASE_1 surface propagation for ATS adapter scaffolding | [ ] | LOW | (architect PR #164 LOW) | **Filed 2026-05-21 via PR #164 architect review.** New operational surfaces shipped via plan 63: 4 env vars (WORKDAY_LOGIN_TOKEN / LINKEDIN_SESSION_COOKIE / INDEED_SESSION_COOKIE / ATS_GENERIC_LLM_CONFIDENCE_THRESHOLD), postmortem screenshot path, alembic 0016, Settings column, `ATSBrowserPool` Playwright lifespan. Doc-only; ~40 LOC. |
| 0.8.0.19 | `ATS_GENERIC_LLM_CONFIDENCE_THRESHOLD` env var `ge=0.0/le=1.0` validator | [ ] | LOW | (hacker PR #164 LOW-1) | **Filed 2026-05-21 via PR #164 hacker review.** `src/config.py:55` accepts OOB values silently → disables Generic safety gate when 0.8.0.NN production-adapter ships. Add `Field(ge=0.0, le=1.0)` + 1 fail-fast startup test. ~3 LOC + 1 test. |
| 0.8.0.20 | Alembic CHECK constraint on `Settings.auto_apply_adapter_confidence_threshold` (defense-in-depth for 0.8.0.19) | [ ] | LOW | (hacker PR #164 LOW-2) | **Filed 2026-05-21 via PR #164 hacker review.** `migrations/versions/0016_*.py` adds the column without CHECK (0 ≤ value ≤ 1). Pair w/ 0.8.0.19 env-var validation. ~5 LOC migration + 1 round-trip test. |
| 0.8.0.21 | Per-adapter screenshot-serving endpoint IDOR + path-traversal gauntlet enforcement | [ ] | MEDIUM | (hacker PR #164 LOW-3) | **Filed 2026-05-21 via PR #164 hacker review.** Plan 63 § C.5 documents the requirement; PR #164 scaffold doesn't add the route. Each per-adapter PR (Workday/LinkedIn/Indeed/Generic) MUST reuse the `_POSTMORTEM_TS_RE` + `Path.resolve().relative_to(data_root)` gauntlet from 0.2.3.02. Track as cross-cutting concern for 0.4.0.24 + 0.8.0.25-27. |
| 0.8.0.22 | ATS postmortem screenshot redaction policy (PII / sensitive form fields) | [ ] | LOW | (hacker PR #164 LOW-4) | **Filed 2026-05-21 via PR #164 hacker review.** PNG written verbatim today; adversarial form (SSN/DOB/address fields) could be screenshotted with PII visible. 0.4.0.24 (Workday adapter) MUST carry per-page allowlist gate + redaction layer (e.g. blur sensitive selectors before screenshot). Design + implementation cost ~80 LOC. |
| 0.8.0.23 | `FormFillStep.action` Pydantic `Literal["fill","click","select"]` hardening (mirror plan 30 `JobExtraction.tags`) | [ ] | LOW | (hacker PR #164 LOW-5) | **Filed 2026-05-21 via PR #164 hacker review.** `src/llm/prompts/ats_generic_form_fill.py:24-41` — current `action: str` is length-bounded but not enum-constrained. Mirror plan 30's tag hardening pattern. ~5 LOC + 1 test. Land BEFORE 0.8.0.27 (production Generic). |
| 0.8.0.24 | Workday ATS adapter — production implementation (graduates to 0.4.0.NN when scheduled) | [ ] | LOW | (plan 63 D.11) | **Filed 2026-05-21 via plan 63 D.11 lock.** Plan 63 ships the stub; production Workday submission flow (5-7 page form, state checkpointing per D.9, OAuth-style session w/ `WORKDAY_LOGIN_TOKEN`, account-ban anti-detection per 0.2.0.13 substrate). Composes with 0.2.7.11 LinkedIn proxy work. Multi-week effort. |
| 0.8.0.25 | LinkedIn ATS adapter — production implementation (Easy Apply preferred + external-link fallback per D.4) | [ ] | LOW | (plan 63 D.11) | **Filed 2026-05-21 via plan 63 D.11 lock.** LinkedIn account-ban risk asymmetric per BACKEND.md § L.2. Requires opt-in default OFF + low cadence + warning UI per plan 63 § F. Composes with 0.2.7.11 LinkedIn proxy work + 0.8.0.10 trust-score weighting. |
| 0.8.0.26 | Indeed ATS adapter — production implementation | [ ] | LOW | (plan 63 D.11) | **Filed 2026-05-21 via plan 63 D.11 lock.** Indeed has anti-bot fingerprinting; needs `use_undetected_adapter=True` from plan 38 + IP rotation. |
| 0.8.0.27 | Generic ATS adapter — production form-fill via LLM (graduate from stub) | [ ] | LOW | (plan 63 D.11) | **Filed 2026-05-21 via plan 63 D.11 lock.** Current PR #164 ships the LLM prompt schema + browser-pool wrapper; production Generic adapter needs: form-detect heuristics, multi-page state, confidence calibration, fail-fast triggers. Depends on 0.8.0.19 + 0.8.0.20 + 0.8.0.22 + 0.8.0.23. |
| 0.8.0.28 | `docs/design/ATS_ADAPTERS.md` design-doc graduation | [ ] | MEDIUM | (plan 63 § H pending) | **Filed 2026-05-21 via plan 63 pattern.** Plan 63 § H listed expected graduation into new design doc; deferred per pattern from 0.8.0.03 + 0.8.0.15. Doc-only surgery; ~300 LOC lift from plan § A + § B + § C + § D + § F into new canonical file. |
| 0.8.0.29 | `ProxyURLConfig.__str__` + `SecretStr` migration (defense-in-depth for f-string + format-string + model_dump leak vectors) | [ ] | LOW | (hacker PR #165 LOW-1 partial) | **Filed 2026-05-21 via PR #165 hacker re-review.** Engineer's `__repr__` override scrubs `%r` path but `str(cfg)`, `f"{cfg}"`, `%s`, `model_dump()`, `model_dump_json()` ALL still leak full credentialed URL. Footgun; no active caller today. Fix: `__str__ = __repr__` + migrate `url` field to `pydantic.SecretStr`. ~15 LOC + 5 tests. |
| 0.8.0.30 | Multi-source generic proxy infra (per-user OR per-source fan-out beyond LinkedIn) | [ ] | LOW | (architect PR #165 follow-up) | **Filed 2026-05-21 via PR #165 architect review.** Plan 64 D.7 locked LinkedIn-only this PR; multi-source generalization (Workday/Indeed/Greenhouse/Generic ATS adapter) needs design work for per-source proxy resolution + per-`Settings.scraper_rate_limits` JSONB extension pattern. Composes with 0.8.0.24-27 ATS adapter production rows. Multi-week design + impl. |
| 0.8.0.31 | `RoundRobinProxyStrategy` proxy rotation pool (beyond sticky-per-session) | [ ] | LOW | (architect PR #165 follow-up) | **Filed 2026-05-21 via PR #165 architect review.** Plan 64 D.1 locked sticky-per-Crawl4AIClient-instance. For high-volume scrape runs, rotation pool with N proxies + round-robin + sticky-session-window-N-mins is the next layer. Adds operator-facing `LINKEDIN_PROXY_URLS` (comma-separated) env var OR DB-stored list. ~80 LOC + 8 tests. |
| 0.8.0.32 | `socks5h://` vs `socks5://` DNS-routing default + operator escape hatch | [ ] | LOW | (architect PR #165 follow-up) | **Filed 2026-05-21 via PR #165 architect review.** SOCKS5 protocol has two variants: `socks5://` resolves DNS LOCALLY (operator IP leaks via DNS); `socks5h://` resolves DNS through proxy (no leak). Naavik should default to `socks5h://` for security; current parser accepts both without enforcement. ~5 LOC validator + 2 tests. |
| 0.8.0.33 | Playwright IndexedDB round-trip test for offline_queue.js | [ ] | LOW | (was 0.2.7.13a) | **Moved 2026-05-21 from 0.2.7.13a per user defer-0.2.x directive.** PR #158 ships TestClient assertions; behavioral DOM testing (offline → enqueue → online → drain → drop event) deferred until Playwright DOM-driver exists. ~50 LOC test. |
| 0.8.0.34 | Re-read `meta[name=csrf-token]` at offline-queue replay time | [ ] | LOW | (was 0.2.7.13b) | **Moved 2026-05-21 from 0.2.7.13b per user defer-0.2.x directive.** Queued autosaves surviving logout+login replay with stale CSRF → silent drop after 5 retries. Fix: at replay time, re-read `meta[name=csrf-token]` from DOM. ~5 LOC JS + 1 test. |
| 0.8.0.35 | Catch-all `/settings/{tab}` `user_id=1` IDOR — extend `_effective_user_id` to catchall | [ ] | MEDIUM | (was 0.2.7.24) | **Moved 2026-05-21 from 0.2.7.24 per user defer-0.2.x directive.** `src/ui/routes/settings.py:531` catchall defaults `user_id=1`. Latent IDOR — explicit routes match FIRST so not actively exploitable. Same pattern as 0.2.7.23. ~5 LOC + 2 tests. |
| 0.8.0.36 | CSRF gap on `put_auto_apply` + `put_llm` + `put_notifications` Settings PUT routes | [ ] | MEDIUM | (was 0.2.7.25) | **Moved 2026-05-21 from 0.2.7.25 per user defer-0.2.x directive.** Pre-existing on main. 3 settings PUT routes lack `require_csrf` dep — cross-origin form-submit could toggle config on logged-in user. ~3 LOC × 3 routes + 3 tests. |
| 0.8.0.37 | IDOR `user_id=1` hardcode on `put_auto_apply` + `put_llm` + `put_notifications` | [ ] | MEDIUM | (was 0.2.7.26) | **Moved 2026-05-21 from 0.2.7.26 per user defer-0.2.x directive.** Pre-existing on main. Same pattern as 0.2.7.02 + 0.2.7.23 + 0.8.0.35. Thread `_effective_user_id(_user)` through 3 routes. ~3 LOC × 3 routes + 6 tests. |
| 0.8.0.38 | Route + ctx rewire — migrate 13 files off `_KNOWN_LEGACY_OFFENDERS` allowlist to service-layer | [x] | MEDIUM | (was 0.2.7.17a) | **PROMOTED 2026-05-21 → 0.3.3.12 via master plan 68 — owner-reported regressions (Discover/Profile/save/stub/pipeline-live) traced directly to this descope.** Originally moved here from 0.2.7.17a per user defer-0.2.x directive on 2026-05-21; promoted back to active cycle same day after local QA revealed 5 visible regressions. Tracking continues at 0.3.3.12. |
| 0.8.0.40 | Score calibration via Platt scaling | [ ] | LOW | (0.3.0 research memo 2026-05-21) | **Filed 2026-05-21 via 0.3.0 research memo § F.3.** Fit a logistic curve over historical `(score, accepted_yes/no)` tuples → surface calibrated probability alongside raw score so "0.86" means the same thing across role families. Defer until 50+ user-accepted-or-rejected signal pairs exist. Needed only if users complain "scores feel inconsistent." ~150 LOC + cron + 4 tests. |
| 0.8.0.41 | Cross-encoder rerank layer (Cohere rerank v3.5 or `cross-encoder/ms-marco-MiniLM-L-6-v2` self-host) | [ ] | LOW | (0.3.0 research memo 2026-05-21) | **Filed 2026-05-21 via 0.3.0 research memo § F.3.** Optional accuracy bump between layers 2 (semantic) and 3 (LLM-judge) in the hybrid scorer if benchmarks show needed. Cost: $0.02 per top-100 rerank (Cohere). Self-host alternative uses sentence-transformers cross-encoder. ~250 LOC + Pydantic provider extension + 6 tests. |
| 0.8.0.42 | Per-source trust-score weighting on scoring path (poison-vector mitigation, scoring-side) | [ ] | LOW | (0.3.0 research memo 2026-05-21) | **Filed 2026-05-21 via 0.3.0 research memo § F.3.** Extends `0.8.0.10` (Phase 6 similar-jobs surface) to the scoring path: weight tag/semantic scores by `JobSource` trust tier (n8n_legacy / company_direct = trusted; rsshub = neutral; linkedin/indeed = unverified). Adversarial scraper-controlled `Job.description` could craft embeddings that always score high. Lock the seam in 0.3.0.06 orchestrator. ~80 LOC + tests. |
| 0.8.0.43 | Score-trend insights surface ("Your score for ML roles trended up 12% this month — consider platform-engineering at 0.75 baseline") | [ ] | LOW | (0.3.0 research memo 2026-05-21) | **Filed 2026-05-21 via 0.3.0 research memo § F.3.** Phase 6 polish surface. Time-series analysis on `ScoreSnapshot` (or aggregated view) → narrative insight rendered on Profile/Scores screens. Requires 0.3.2.03 (score history time-series) shipped first. ~200 LOC + LLM-generated insight prompt + tests. |

**Deliverable (0.8.0):** Long-tail enhancements parking lot. Items graduate to an active release version when scheduled.

---


## Backlog (unprioritized)

Tasks deferred from active cycles but not deleted. No priority within Backlog; pick by inspection or promote to a release section via `.claude/naavik-ops task move <id> <release>.NN` when ready. Project v2 board mirrors these to `Status=Backlog`.

| # | Task | Status | Priority | Legacy ID | Notes |
|---|---|---|---|---|---|
| 0.2.0.14 | Migrate existing n8n DataTable + Google Sheets data to PostgreSQL — historical-data import | [x] | — | 2.10 | **CLOSED-AS-MOOT 2026-05-20 per user directive at release-readiness audit** — user starting fresh in Naavik; no meaningful n8n history worth importing. Every n8n-functional-equivalent already shipped via 0.2.0 (scrapers / scheduler / notifications / dedup / Application table); legacy importer skeleton (`services/legacy_import.py` per BACKEND.md § J.5) NOT implemented and won't ship. n8n decommission still tracked via POST_PHASE_1.md cross-cutting concern #4 — disable n8n Main Workflow after 0.2.0.17 observation window confirms scraper stability. GH Issue #19 left OPEN pending explicit user authorization to close (sandbox classifier requires it). |

**Deliverable (Backlog):** none — this section is a parking lot. Items promoted to a release-version section trigger their own deliverable. Items closed-as-moot (`[x]`) document the reason inline.

---

## Agent System (mirror conventions)

> **Companion docs:** `docs/AGENT_OPS.md` (single operational guide), `AGENTS.md` § Agent System (workflow integration), `.claude/agents/` (full agent prompts), `.claude/commands/` (slash commands).
>
> **Reference guides loaded by agents on cold start:**
> - `ROADMAP.md` — one-page roadmap digest (faster than this 800-line doc)
> - `docs/ARCHITECTURE.md` — layer responsibilities + cross-cutting concerns + pattern catalog
> - `DESIGN.md` (root) — visual contract (tokens, type, icons, voice; frozen) + `docs/design/WORKFLOW.md` — UI sub-process (skill routing, per-screen checklist, accessibility, common patterns, anti-patterns)
> - `docs/DEPLOYMENT.md` — 4 deployment paths + config + ops checklist
> - `docs/RUNBOOK.md` — devops runbook (known failure modes + diagnostic recipes + recovery)
> - `docs/plans/POST_PHASE_1.md` — testing playbook + monitoring + "when something goes wrong"
> - `docs/design/PHASE_NUMBERING.md` — 4-level semver schema + release ceremony + Conventional Commits (post-A.29)

This ROADMAP is mirrored onto a GitHub Project v2 board for queryability and assignability. **ROADMAP is authoritative** (per AGENTS.md § Single-doc-tracking); the Project is a one-way operational mirror. The mapping is mechanical (post-A.29):

| ROADMAP element | GitHub Project equivalent |
|---|---|
| `### <X.Y.Z> — <Name>` release header | Milestone `<X.Y.Z>` (description = `**Goal:**` line) |
| Task row in a release table | Issue titled `[<X.Y.Z.NN>] <description>`, body links back to ROADMAP |
| `[ ]` / `[~]` / `[x]` status | Project Status field: `Backlog` (deferred) / `Todo` / `In Progress` / `Done` |
| Priority column (`HIGH` / `MEDIUM` / `LOW` / unset; TASK-level only) | Project Priority single-select (4-level IDs only; never on 3-level patches/releases) |
| Release header | Project Milestone single-select (`0.1.0`–`0.6.0`, `0.1.1`, `0.2.1`–`0.2.6`, `0.7.0`) |
| Notes column | Issue body |
| Plan reference (`docs/plans/NN-name.md`) | Label `plan:<NN>` on the Issue |

**Sync flow:** always ROADMAP → Project. Manager updates ROADMAP first (mark `[~]` on start, `[x]` on done), then runs `.claude/naavik-ops gh set-status` to push to Project. `/sync-roadmap --apply` is the bulk reconcile. If the Project drifts from ROADMAP, the Project is wrong — never edit ROADMAP to match a stale board.

**Bootstrap:** `.claude/naavik-ops gh bootstrap [--apply]` parses ROADMAP's task tables and creates Milestones + Issues + Project items for the active releases (defaults to `0.2.0` + `0.2.1`–`0.2.6` + `0.7.0` open releases; closed `0.1.0` is skipped). See `docs/AGENT_OPS.md` § 2 for the full setup walkthrough.

**What to do as a plan / task author:**
1. Write the task row in ROADMAP first (status `[ ]`, with optional Priority on 4-level IDs).
2. Run `/plan <task-id>` — architect drafts the plan, opens the GH Issue (via `.claude/naavik-ops gh create-issue`), adds to Project, links back to ROADMAP.
3. Run `/build <release>` (or `/build "next"`) when ready to implement.

**What to do as an implementer:**
1. Mark the ROADMAP row `[~]` when starting.
2. Reference the Issue in commits: `Closes #<N>` in the last commit triggers GH's auto-close on merge.
3. Mark the ROADMAP row `[x]` + add a one-line deliverable note when done.
4. `/build` handles steps 1 + 3 automatically; manual implementers do them by hand.
