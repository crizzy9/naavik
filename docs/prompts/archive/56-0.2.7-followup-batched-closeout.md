# Kickoff · 56 · 0.2.7 follow-up batched closeout

## Goal

Close 9 small/medium 0.2.7 follow-ups (3 MEDIUM security/info-disclosure + 6 LOW mechanical) in one PR on `chore/0.2.7-followup-batched-closeout`.

## Required reading (read in order)

1. `docs/plans/56-0.2.7-followup-batched-closeout.md` — full plan (this PR's spec)
2. `AGENTS.md` § Workflow steps 2 + 5 + 7 + § Key Conventions § CLI + § GitHub state — single writer rule
3. `docs/PLAYBOOK.md` § H — CONTRACT_CHANGE (this is a `chore/` branch)
4. `CLAUDE.md` § Deviations workflow + § GitHub state — single writer rule
5. `docs/AGENT_OPS.md § 2.7a` (dispatcher) + § 2.8 (commit-msg hook + UPPERCASE branch task-id rule — though this branch has no task-id matching the hook regex, so the hook silently no-ops)
6. `docs/design/SOURCES_UI.md` — full (item 3 + item 4 + item 8 touch this contract)
7. `docs/design/SCREENS.md` § 11 only (lines 570-633) — items 4 + 8
8. `src/ui/routes/jobs.py:42-54` — canonical `_effective_user_id` pattern (item 1 mirrors this)
9. `src/ui/routes/discover.py:100-130` — canonical `Depends(require_csrf)` pattern (item 6 mirrors this)
10. `src/services/ats_postmortem.py` — full (item 5 edits this)
11. `tests/test_ats_postmortem.py` (or equivalent) — existing test surface for postmortem; item 5 extends

## Deliverables

Nine commits on `chore/0.2.7-followup-batched-closeout`:

| Item | File(s) | LOC | Severity |
|---|---|---|---|
| 1 — Sources IDOR (`0.2.7.02`) | `src/ui/routes/settings.py`, `tests/test_settings_sources_route.py` | ~5 src + ~30 test | MEDIUM |
| 2 — Loud raise on None session (`0.2.7.03`) | `src/ui/routes/settings.py`, `tests/test_settings_sources_route.py` | ~3 src + ~10 test | LOW |
| 3 — Workday popover + README/.env.example (`0.2.7.04`) | `src/ui/routes/settings.py`, `src/ui/templates/components/_source_row.html`, `README.md`, `.env.example`, test file | ~15 src + ~10 docs + ~15 test | LOW |
| 4 — SCREENS.md § 11 Sources rewrite (`0.2.7.05`) | `docs/design/SCREENS.md` | ~30 docs | LOW |
| 5 — Postmortem `_redact` value patterns (`0.2.7.18`) | `src/services/ats_postmortem.py`, `tests/test_ats_postmortem.py` | ~30 src + ~50 test | MEDIUM |
| 6 — Twin CSRF gaps (`0.2.7.19`) | `src/ui/routes/discover.py`, `src/ui/routes/tracking.py`, test file | ~10 src + ~40 test | MEDIUM |
| 7 — `/settings/llm-provider` auth dep (`0.2.7.20`) | `src/ui/routes/settings.py`, test file | ~3 src + ~15 test | LOW |
| 8 — SOURCES_UI.md + SCREENS.md doc gaps (`0.2.7.21`) | `docs/design/SOURCES_UI.md`, `docs/design/SCREENS.md` | ~10 docs | LOW |
| 9 — ruff format misses (`0.2.7.22`) | `tests/test_naavik_ops/test_plan.py`, `tests/test_naavik_ops/test_roadmap.py` | ~5 whitespace | LOW |

**Total:** ~110 LOC src + ~50 LOC docs + ~155 LOC tests + ~5 LOC whitespace = ~320 LOC delta.

## Quality bar

After EACH commit (not just at the end):

- `uv run ruff check .` → exit 0
- `uv run ruff format --check .` → exit 0
- `uv run pytest -x` → green (default env, NAAVIK_LIVE_DB unset is fine — items 1/2/3/6/7 use TestClient + fake-session path; items 5 + 9 don't touch DB)

After the final commit:

- `uv run pytest -x` full run green
- `gh pr create` with `.github/pull_request_template.md`
- Manual QA: `engineer-manual-qa-gate` skill — exercise the Sources panel + `/discover` + `/tracking/manual` POSTs in a browser to verify item 6's CSRF gate doesn't accidentally 403 a legitimate HTMX form

## Forbidden patterns

- No new `naavik` CLI subcommand or `src/cli/` file edits (sunset per `AGENTS.md § Key Conventions § CLI`)
- No `src/services/vault.py` edit or AES-GCM / PBKDF2 / `secrets.enc` / `key.bin` code (vault deprecated in plan 26 / `0.2.0.01`)
- No new env vars introduced (this PR fixes the misleading `WORKDAY_COMPANIES` slot via doc/comment edits only — no boot-time env→DB seed wiring)
- No `git commit --amend` (per `AGENTS.md § Workflow`)
- No raw `gh api graphql` or `gh issue create` from this branch (manager files Issues separately via `naavik-ops gh`)
- No `git add -A` or `git add .` (use explicit paths per `docs/PLAYBOOK.md § I`)
- No raw SQL in route handlers (item 1 stays in `services.settings_service` + `services.job_service`)
- No bare `await client.messages.create()` / `client.chat.completions.create()` — item 5 doesn't touch the LLM call itself (only the prompt input redaction), but if engineer finds a bare LLM call elsewhere, the `engineer-llm-tracker-wrap` skill applies

## Hand-back format

When all 9 commits land on the branch + PR is opened, report:

1. **PR URL** + branch name
2. **Per-item summary** (one line each: "Item N — <short description> — <files touched> — <test count added>")
3. **Deviations log** — every divergence from this plan's spec, one line each:
   - **What** changed vs the plan (one-line)
   - **Why** (root cause / constraint)
   - **Impact** on follow-up rows (does this need a new ROADMAP row?)
   - **Surface** any new env var / CLI / on-disk path / port / schedule introduced (none expected — this PR's contract is "no new operational surface")
   - Append each line to `traces/<run-id>/engineer-deviations.log` per the `engineer-deviation-log` skill format
4. **Quality gate evidence** — last `uv run pytest -x` line count + last `uv run ruff check .` exit code
5. **Manual QA evidence** — one sentence per surface exercised (Sources panel, /discover + Job-by-URL modal, /tracking + manual application form)
6. **Open follow-ups** — any item that revealed a deeper bug requiring a new ROADMAP row (e.g. item 5 over-redaction false positives spotted in dev → new row `0.2.7.NN`)

The deviations summary feeds directly into the plan's `## Deviations from plan` section at archive time (per `AGENTS.md § Workflow step 7` + `docs/PLAYBOOK.md § I`).
