# Prompt — Plan 46 · 0.2.x batched closeout (2026-05-20)

## Goal

Ship four independent small follow-ups (0.2.0.07b + 0.2.0.08a + 0.2.0.11c + 0.2.0.12a) in one PR on branch `feat/0.2.0-batched-closeout-2026-05-20`. Each item closes a filed GH Issue.

## Required reading (in order)

1. `docs/plans/46-0.2.x-batched-closeout-2026-05-20.md` — this plan (file-by-file scope)
2. `AGENTS.md` § Workflow steps 7 (deviations) + § Key Conventions § CLI (sunset guard)
3. `CLAUDE.md` § Deviations workflow + § Claude Code Specific Notes
4. `docs/PLAYBOOK.md` § F PRODUCT_WORK + § I BOOKKEEPING (fold-in rule)
5. `tests/test_no_direct_http_imports.py` — current state (item 1 target)
6. `src/llm/prompts/extract_job.py:23` — current `JobExtraction.tags: list[str]` (item 2 target)
7. `src/ui/routes/jobs.py:115` — current `model_dump(mode="json")` (item 3 target)
8. `src/services/notifications.py:211` + `:242` + `:258` — `_telegram_text_for_event` + `send_telegram` + parse_mode (item 4 target)
9. Reference: `src/services/notifications.py:428–430` + git log entry `c4d4154` for the PR #116 delta — the exact mitigation pattern item 4 mirrors
10. `docs/design/JOB_MODEL.md` § Pydantic projections — confirm `JobRead` import path

## Deliverables

- `tests/test_no_direct_http_imports.py` — extended FORBIDDEN_MODULES + prefix-match helper + AST walk for `Import` + `ImportFrom`; `urllib.parse` explicit whitelist; ~8 parametrized positive cases + 1 negative case (~25 LOC)
- `src/llm/prompts/extract_job.py` — `tags` constrained to `list[Literal[...]]` 9-tag vocabulary; `from typing import Literal` import (~5 LOC)
- `src/ui/routes/jobs.py` — `GET /jobs/{id}` JSON projection swapped to `JobRead.model_validate(job).model_dump(mode="json")` + import + docstring tweak (~5 LOC)
- `src/services/notifications.py` — `_telegram_text_for_event` `*x*` bolding stripped; `send_telegram` body drops `parse_mode` key (~2 LOC)
- New / extended tests in existing files: 1 test per item (4 new tests total, 8 parametrized cases for item 1 lint)
- Final commit body: explicit four-trailer block:
  ```
  Closes #104
  Closes #107
  Closes #115
  Closes #117
  ```
- PR opened via `gh pr create` against `main` using `.github/pull_request_template.md`. PR body summarizes the 4 items + links each ROADMAP row + each closed issue.

## Quality bar

- `uv run ruff check .` — clean
- `uv run ruff format --check .` — clean
- `uv run pytest -x` — full suite green
- Each item independently green when its commit is applied in isolation (engineer's own check; not a CI gate). Stage by item: `git add <item-files> && uv run pytest -x` before staging the next.
- `Skill: engineer-manual-qa-gate` — for item 3, hit `GET /jobs/<id>` locally OR via TestClient + assert response JSON does NOT contain `"raw_meta"` key + DOES contain `"id"`, `"company"`, `"position"`. Item 4 — exercise `send_telegram` with a mock httpx + assert request body shape (no `parse_mode` + literal `*` in `text` preserved). Item 1 + item 2 — pytest is the surface.

## Forbidden patterns

- No new `naavik` CLI subcommand (sunset per AGENTS.md § Key Conventions § CLI / ROADMAP 0.2.1.05)
- No extension of `src/services/vault.py` (deleted; permanent sunset per ROADMAP 0.2.0.01)
- No new env var, no new on-disk artifact path, no new CLI command, no new schedule, no new port
- No new file outside the four targets named above (test fixtures inside existing test files OK)
- No raw SQL in route handlers (item 3 stays in projection layer; no DB-shape changes)
- No `console.log`, no new component partials, no inline styles, no React/Vue (none of the four items touches the UI surface; this rule still applies on principle)
- No `parse_mode=` re-introduction in any Telegram body construction (item 4's invariant going forward)
- No `tags: list[str]` re-loosening (item 2's invariant going forward)
- No direct push to main (this is category F PRODUCT_WORK per `docs/PLAYBOOK.md`)
- No `git commit --amend` to fix pre-commit-hook failures — create a NEW commit (AGENTS.md § Workflow hard rule 3)

## Hand-back format

Report:

1. **PR URL** — `gh pr create` output
2. **Files touched** — full list (should be ~6: 4 source/test files + the existing notification test file extended)
3. **Test count delta** — `uv run pytest --collect-only | wc -l` before / after (~12 new test cases collected total: 8 parametrized for item 1 + 1 each for items 2/3/4)
4. **Commit history on branch** — `git log --oneline main..HEAD`
5. **Final commit body verbatim** — including all four `Closes #N` trailers
6. **Manual QA evidence for item 3 + item 4** — JSON snippet + httpx request body capture (or pytest assertions that exercise those surfaces)
7. **Deviations summary** — even if "no material deviations," state it explicitly per AGENTS.md § Workflow step 7. For each deviation:
   - **What** changed (one-line)
   - **Why** (constraint that forced it)
   - **Impact** on the 4 items (does this need a follow-up ROADMAP row?)
   - **Operational surface** introduced — should be zero for all four items; flag explicitly if any item introduced a new env / CLI / on-disk path

Append each deviation as it happens to `traces/<run-id>/engineer-deviations.log` per the canonical format:
```
[ISO-ts] DEVIATION plan=docs/plans/46-0.2.x-batched-closeout-2026-05-20.md what=<one-line> why=<one-line> impact=<one-line>
```

Manager will promote these to the plan's `## Deviations from plan` section at archive time via `.claude/naavik-ops plan archive`.
