# Prompt — Plan 51 · 0.2.2 UI batched closeout

## Goal

Ship three independent small UI follow-ups (0.2.2.02 + 0.2.2.03 + 0.2.2.04) in one PR on branch `feat/0.2.2-ui-batched-closeout`. Each item closes a filed GH Issue.

## Required reading (in order)

1. `docs/plans/51-0.2.2-ui-batched-closeout.md` — this plan (file-by-file scope)
2. `AGENTS.md` § Workflow step 7 (deviations) + § Key Conventions § Frontend (HTMX) + § CLI (sunset guard)
3. `CLAUDE.md` § Deviations workflow + § Claude Code Specific Notes
4. `docs/PLAYBOOK.md` § F PRODUCT_WORK + § I BOOKKEEPING (fold-in rule)
5. `src/ui/templates/base.html:52–54` — current self-hosted Lucide script line (item 1 target)
6. `src/ui/static/base.js:13–22` (idempotent guard) + `:203–227` (sidebar toggle + sync) — item 2 context
7. `src/ui/static/lucide.min.js` — grep the first 100 bytes for the version banner; pin the jsDelivr URL to that exact tag (item 1)
8. `src/ui/templates/pages/discover.html:5–8` — current wrapper class string + comment (item 3 target)
9. `tests/test_base_js.py` + `tests/test_mobile_sidebar.py` + `tests/test_scroll_spy.py` — TestClient-only pattern (no Playwright on NixOS, per paper-cut #3); your new tests follow the same shape
10. Reference: `docs/plans/archive/09a-stage-3-bugfix.md` — original Lucide / sidebar root-cause analysis from plan 09a; confirms "No CSP headers sent by FastAPI" (item 1 CSP risk is zero today)

## Deliverables

- `src/ui/templates/base.html` — replace single self-hosted `<script src="/static/lucide.min.js">` with jsDelivr primary + `onerror` fallback to `/static/lucide.min.js`; refresh adjacent comment (~5 LOC)
- `src/ui/static/base.js` — add `syncSidebarAria()` wiring on `DOMContentLoaded` + `htmx:afterSwap` after the existing click handler block (~8 LOC)
- `src/ui/templates/pages/discover.html` — extend wrapper class string with `2xl:max-w-[1400px] 2xl:mx-auto`; refresh adjacent comment (~3 LOC)
- `tests/test_lucide_cdn_loading.py` — NEW file, 3 TestClient assertions (jsDelivr URL present, fallback path present, static mount still serves)
- `tests/test_mobile_sidebar.py` — extend with `test_base_js_syncs_aria_on_swap` (1 new assertion: base.js contains `htmx:afterSwap` + `syncSidebarAria` in proximity)
- `tests/test_discover_max_w_cap.py` — NEW file, 2 TestClient assertions (`2xl:max-w-[1400px]` + `2xl:mx-auto` in body)
- Final commit body: explicit three-trailer block (Issue numbers filed by manager pre-PR):
  ```
  Closes #<lucide-cdn-issue>
  Closes #<sidebar-aria-issue>
  Closes #<discover-cap-issue>
  ```
- PR opened via `gh pr create` against `main` using `.github/pull_request_template.md`. PR body summarizes the 3 items + links each ROADMAP row + each closed issue.

## Quality bar

- `uv run ruff check .` — clean
- `uv run ruff format --check .` — clean
- `uv run pytest -x` — full suite green
- Each item independently green when its commit is applied in isolation (engineer's own check; not a CI gate). Stage by item: `git add <item-files> && uv run pytest -x` before staging the next.
- `Skill: engineer-manual-qa-gate` — for item 1, hit `GET /` in a fresh browser tab, confirm icons render AND DevTools Network shows the jsDelivr URL with 200 (NOT the fallback). If you can simulate CDN failure (block `cdn.jsdelivr.net` in DevTools' "Block request URL") confirm the fallback kicks in and icons still render. For item 2, navigate `/` → `/discover` → back → `/profile`; open hamburger → close → navigate → re-open. Toggle responsive throughout. For item 3, resize window to 2560+px wide on `/discover`; confirm card content clamps to 1400px wide centered. At <1536px, full-width persists. Items 1 + 3 also have ruff/pytest as the testable surface; item 2's behavior is the only one fully manual.

## Forbidden patterns

- No new `naavik` CLI subcommand (sunset per AGENTS.md § Key Conventions § CLI / ROADMAP 0.2.1.05)
- No extension of `src/services/vault.py` (deleted; permanent sunset per ROADMAP 0.2.0.01)
- No new env var, no new on-disk artifact path, no new CLI command, no new schedule, no new port
- No new file outside the three target areas named above (test fixtures inside existing test files OK; new test files `test_lucide_cdn_loading.py` + `test_discover_max_w_cap.py` are explicitly listed deliverables)
- No raw SQL in route handlers (none of the three items touches the DB)
- No `console.log`, no new component partials (the three items modify existing surfaces only), no inline styles, no React/Vue
- No `<script>` inline body beyond the existing `tailwind.config = {...}` block in base.html and the existing per-page keyboard registration scripts — the item 1 inline `onerror` attribute is the sole new inline JS and is justified by being dependency-free fallback wiring
- No new Lucide version pin without matching `/static/lucide.min.js` to the same version; the two MUST match
- No removal of `/static/lucide.min.js` — it stays as fallback
- No direct push to main (this is category F PRODUCT_WORK per `docs/PLAYBOOK.md`)
- No `git commit --amend` to fix pre-commit-hook failures — create a NEW commit (AGENTS.md § Workflow hard rule 3)

## Hand-back format

Report:

1. **PR URL** — `gh pr create` output
2. **Files touched** — full list (should be 6: `base.html` + `base.js` + `discover.html` + 2 new test files + extended `test_mobile_sidebar.py`)
3. **Test count delta** — `uv run pytest --collect-only | wc -l` before / after (~6 new test cases collected: 3 in `test_lucide_cdn_loading.py` + 2 in `test_discover_max_w_cap.py` + 1 in `test_mobile_sidebar.py`)
4. **Commit history on branch** — `git log --oneline main..HEAD`
5. **Final commit body verbatim** — including all three `Closes #N` trailers
6. **Manual QA evidence** — for item 1, screenshot or text description of DevTools Network panel showing the jsDelivr 200 OR a TestClient assertion that exercises the URL chain. For item 2, narrative of the navigation sequence and toggle responsiveness. For item 3, screenshot at 2560px width or width-resize report.
7. **Lucide version pin** — exact version banner found in `/static/lucide.min.js` (e.g. `Lucide v0.469.0`) + the jsDelivr URL pinned to that tag. These two MUST match in the final diff.
8. **Deviations summary** — even if "no material deviations," state it explicitly per AGENTS.md § Workflow step 7. For each deviation:
   - **What** changed (one-line)
   - **Why** (constraint that forced it)
   - **Impact** on the 3 items (does this need a follow-up ROADMAP row?)
   - **Operational surface** introduced — should be zero for all three items; flag explicitly if any item introduced a new env / CLI / on-disk path

Append each deviation as it happens to `traces/<run-id>/engineer-deviations.log` per the canonical format:
```
[ISO-ts] DEVIATION plan=docs/plans/51-0.2.2-ui-batched-closeout.md what=<one-line> why=<one-line> impact=<one-line>
```

Manager will promote these to the plan's `## Deviations from plan` section at archive time via `.claude/naavik-ops plan archive`.
