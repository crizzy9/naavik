# Prompt · 44 · 0.2.0.11b — CSRF on `/api/v1/discover` save/skip/auto-submit

## Goal

Add `Depends(require_csrf)` to the three swipe-mutation endpoints in `src/ui/routes/discover.py` and ship matching tests, closing PR #112 hacker MEDIUM finding (Issue #114).

## Required reading (in this order)

1. `Skill: naavik-cold-start` — non-negotiable first action.
2. `docs/plans/44-0.2.0.11b-discover-csrf.md` — this plan (read in full).
3. `AGENTS.md` § Workflow steps 2 + 5 + 7 (plan contract + prompt contract + deviations).
4. `AGENTS.md` § Key Conventions § CLI (sunset rules — verify no CLI/vault touch).
5. `docs/PLAYBOOK.md` § H — CONTRACT_CHANGE flow (PR required; this is `src/**` + `tests/**`).
6. `src/api/auth.py:271-280` (`require_csrf` definition) + `:283-291` (canonical exemplar usage on `post_change_password`).
7. `src/services/auth.py:154-163` (`issue_csrf_token` + `validate_csrf` — double-submit semantics).
8. `src/ui/routes/discover.py:95-131` (the three endpoints to gate).
9. `src/ui/templates/base.html:28` (verify `hx-headers='{"X-CSRF-Token": "..."}'` global wiring — no template edits needed).
10. `tests/test_auth.py:342-426` (canonical CSRF-test pattern to mirror).
11. `tests/test_stub_endpoints.py:206-229` (3 swipe tests to patch).
12. `tests/test_auth_gate.py:140-204` (auth-gate sweep — 2 callsites to patch).
13. `docs/design/JOB_UI.md § E.4` — forward-points to this fix; verify no doc edit needed (the design doc already anticipates this row landing).

## Deliverables

| File | Action | One-line description |
|------|--------|----------------------|
| `src/ui/routes/discover.py` | modify | Import `require_csrf` from `api.auth`; add `_csrf: None = Depends(require_csrf)` to `post_skip` / `post_save` / `post_auto_submit`. |
| `tests/test_stub_endpoints.py` | modify | Thread matching CSRF cookie + header into the 4 swipe POST callsites (lines 207, 213, 218, 226). |
| `tests/test_auth_gate.py` | modify | Thread matching CSRF cookie + header into the 2 discover POST callsites (lines 145-148, 200-203). |
| `tests/test_discover_csrf.py` | new | 6 new tests — 3 reject (no/mismatched CSRF → 403) + 3 accept (matching CSRF → not 403) — one pair per endpoint, mirroring `test_auth.py:342-426` shape. |

No alembic migration. No template edits. No new operational surface.

## Quality bar

- `uv run ruff check .` — clean.
- `uv run ruff format --check .` — clean.
- `uv run pytest -x` — full green (no skips beyond existing baseline).
- Optional Playwright smoke if you spin up a live orchestrator: capture one screenshot of `/jobs/<id>` after a Save click succeeds (200, swipe queue advances).
- Manual QA via `Skill: engineer-manual-qa-gate`:
  - Start `nix run .#dev`, log in as the seeded operator.
  - Click Save / Skip on `/jobs/<id>` — verify 200 + UI updates.
  - `curl -X POST http://localhost:5173/api/v1/discover/1/save` (no cookies, no CSRF) — verify 403 with `"CSRF"` in the response body.
  - `curl -X POST http://localhost:5173/api/v1/discover/1/save --cookie 'naavik_session=<valid-jwt>'` (no CSRF) — verify 403.
  - Capture evidence in `traces/<run-id>/qa/` per `Skill: engineer-manual-qa-gate`.

## Forbidden patterns

- Do NOT extend `src/cli/` (CLI sunset per 0.2.0.02).
- Do NOT touch `src/services/vault.py` (deleted per 0.2.0.01).
- Do NOT invent a new CSRF helper, middleware, or token-rotation scheme — reuse `require_csrf` exactly.
- Do NOT edit `_job_detail_body.html`, `_discover_review_workspace.html`, or any HTMX template — `base.html:28` already wires the header globally.
- Do NOT add an alembic migration — no schema change.
- Do NOT add a new env var, on-disk path, port, schedule, or operational surface.
- Do NOT introduce new `console.log`, `print()` debugging, or non-Lucide icons (none expected for a pure dep-injection fix).
- Do NOT skip the `Skill: engineer-llm-tracker-wrap` check — no LLM calls expected here, but if you find one, wrap it.
- Do NOT bypass the parallel-reviewer invariant at PR_REVIEW_GATE (manager dispatches hacker + architect in parallel; no engineer self-approval).
- Do NOT `git commit --amend` to fix pre-commit failures — create a new commit.

## Branch + commit

- Branch: `feat/0.2.0.11b-discover-csrf` (UPPERCASE-task-id safe per `docs/AGENT_OPS.md § 2.8`).
- Commit prefix: `feat(security):` or `fix(security):` per Conventional Commits.
- **Known workaround:** the 0.7.0.28 hook regex doesn't recognize `0.2.0.11b` (no UPPERCASE letter), so the `prepare-commit-msg` hook won't auto-append `Closes #114`. Manually add `Closes #114` to the commit body (HEREDOC pattern). Devops or hacker may flag this in review — point them at the 0.7.0.28 ROADMAP note.

## Hand-back format

Write your hand-back as a structured note covering:

1. **Files touched** — exact list, with line ranges per file.
2. **Quality gates** — paste the PASS line from each (`ruff check`, `ruff format --check`, `pytest -x` summary).
3. **Manual QA evidence** — paths to screenshots / curl output captured under `traces/<run-id>/qa/`.
4. **Branch / PR** — branch name, PR URL, squash sha if already merged.
5. **Deviations summary** — every place implementation diverged from plan 44 § D.1–D.3 + § Build sequence. Each bullet: **what** / **why** / **impact** / **surface** (always `none` for this plan unless something unexpected ships). Log each deviation as it happens via `Skill: engineer-deviation-log` to `traces/<run-id>/engineer-deviations.log`; the summary is the textual hand-back of those entries.
6. **Follow-ups** — any LOW/INFO findings not in scope; defer or note for hacker review.
7. **BUILT line** — append to `traces/<run-id>/engineer.log` as the last line: `[ts] BUILT files_added=1 files_modified=3 files_deleted=0 summary='0.2.0.11b CSRF gate on 3 discover swipe endpoints — 6 new tests + 6 patched'`.

Manager will promote the deviations summary into `docs/plans/44-0.2.0.11b-discover-csrf.md § Deviations from plan` via `.claude/naavik-ops plan archive` at archive time.
