# Plan 57 — 0.2.7 small batched closeout

## Goal

Ship four 0.2.7 follow-ups in one PR on `feat/0.2.7-small-batched`: `JobRead` projection in discover.py (0.2.7.01), onboarding offline retry buffer (0.2.7.13), portfolio API `?version=v1` query param (0.2.7.15), sibling IDOR helpers in settings.py (0.2.7.23).

## Required reading (in order)

1. `docs/plans/57-0.2.7-small-batched.md` — this plan
2. `AGENTS.md` § Workflow steps 2 + 4 + 5 + 7
3. `AGENTS.md` § Key Conventions § CLI (CLI + vault sunset — do NOT extend either)
4. `docs/PLAYBOOK.md` § F (PRODUCT_WORK) — PR required, branch prefix `feat/`
5. `CLAUDE.md` § Tech Stack + § Key Conventions
6. `docs/plans/archive/56-0.2.7-followup-batched-closeout.md` — exemplar for the batched-PR shape + per-item commit convention
7. `src/models/job.py:248-294` — `JobRead` schema (already excludes `raw_meta`)
8. `src/ui/routes/discover.py:242-299` — five callsites in scope
9. `src/ui/routes/settings.py:224-245` (the three IDOR-vulnerable view fns) + `:312-320` (`_effective_user_id` helper from plan 56)
10. `src/api/portfolio.py:51-103` — `get_cv` handler
11. `src/ui/templates/pages/_onboarding_step_review.html` — current autosave wiring (`hx-post` at line 48)
12. `src/ui/static/base.js:14-23` — idempotency-guard pattern (mirror for `offline_queue.js`)
13. `docs/design/INTERACTIONS.md § H.3` — offline-retry-buffer functional contract (the deferred surface this row closes)
14. `tests/test_discover_csrf.py` — CSRF token fixture pattern for the `post_job_by_url` test
15. `tests/test_settings_sources_route.py` (plan 56 item 1 exemplar) — `_effective_user_id` threading + cross-user filter test shape

## Deliverables

- `src/ui/routes/discover.py` — five callsites converted to `JobRead.model_validate(j).model_dump(mode="json")`; `from models import JobRead` added.
- `tests/test_discover_jobread_projection.py` — five tests (one per callsite) asserting `raw_meta not in response`.
- `src/ui/static/offline_queue.js` — new IndexedDB-backed offline queue (~80 LOC, IIFE w/ idempotency guard, path-gated to `/onboarding`).
- `src/ui/templates/components/auth_shell.html` (or wherever onboarding shell loads scripts) — gated `<script>` tag for `offline_queue.js`.
- `tests/test_onboarding_offline_queue.py` — three Playwright tests OR explicit Playwright-deferral comment + 1 unit test (engineer chooses based on existing Playwright wiring).
- `src/api/portfolio.py` — `version: Annotated[Literal["v1"], Query()] = "v1"` added to `get_cv`.
- `tests/test_portfolio_cv_versioning.py` — two tests (default version + unknown rejected).
- `src/ui/routes/settings.py` — three helper signatures updated; `get_settings_submissions` + `get_settings_llm_provider` thread `user_id=_effective_user_id(_user)`.
- `tests/test_settings_idor_helpers.py` — six tests (2 per helper: threading + cross-user filter).
- Four commits on `feat/0.2.7-small-batched`, one per item, each with `Refs ROADMAP 0.2.7.NN` trailer.
- PR via `gh pr create` using `.github/pull_request_template.md`.

## Quality bar

- `uv run ruff check .` — clean.
- `uv run ruff format --check .` — clean.
- `uv run pytest -x` — all tests pass.
- Optional manual QA for item 2: `nix run .#dev`, navigate to `/onboarding`, devtools Network tab "Offline" toggle, edit a field, assert no console error + IndexedDB inspector shows the queued row, toggle back online, assert the row clears.
- Optional manual QA for item 3: `curl 'http://127.0.0.1:8000/api/portfolio/cv'` (default v1) + `curl 'http://127.0.0.1:8000/api/portfolio/cv?version=v99'` (rejection).
- Engineer-deviations-log: append one line per deviation per `engineer-deviation-log` skill.
- Per-PR self-check: invoke `Skill: engineer-manual-qa-gate` before "ready for review".

## Forbidden patterns

- NO new `naavik` CLI subcommand (sunset per `AGENTS.md § Key Conventions § CLI`).
- NO new vault scope / `src/services/vault.py` extension (sunset per `AGENTS.md § Key Conventions § CLI`, plan 26 deleted the vault).
- NO new env var (the plan does NOT introduce any; if engineer discovers one is needed, log as deviation + halt for architect re-review).
- NO new on-disk path (IndexedDB is browser-side; no server-side `~/.naavik/...` additions).
- NO React/Vue/inline `<script>` blobs — offline_queue.js is a flat IIFE per project convention (`base.js`).
- NO non-Lucide icons (item 2 doesn't add icons; if engineer needs one for the drop-toast, use Lucide stroke 1.5).
- NO `console.log` in shipped JS (use `console.warn` for genuine errors only, matching `base.js:43` pattern).
- NO `--amend` to fix pre-commit-hook failures (create a NEW commit per `AGENTS.md § Hard rules`).
- NO raw SQL in route handlers (`AGENTS.md § Database`).
- NO `gh issue create` / `gh api graphql` for state writes — use `.claude/naavik-ops gh` per single-writer rule.

## Hand-back format

- **File list** — every file added or modified, grouped by item.
- **Test counts** — number of new tests per item.
- **Screenshot paths** (if any Playwright captures were saved) — `traces/<run-id>/screenshots/...`.
- **Manual QA outcome** — pass/fail per item that needed it (item 2 IndexedDB round-trip, item 3 curl rejection).
- **Deviations summary** — list every divergence from the plan's `## Proposal` section. One bullet per: what / why / impact / surface. This will be promoted into the plan's `## Deviations from plan` section by manager at archive time per `AGENTS.md § Workflow step 7`. Mandatory.
- **Open follow-ups** — anything that surfaced during impl that warrants a new ROADMAP row (e.g. if Playwright wiring forced a deferral for item 2 tests, file the deferral row).
- **Branch + PR URL** — `feat/0.2.7-small-batched` + `gh pr create` output.
