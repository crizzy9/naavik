---
Status: APPROVED
Type: execution
Authored: 2026-05-24
Last updated: 2026-05-24
Depends on: docs/plans/archive/10c-first-time-setup.md, docs/plans/archive/10b-phase-1-finalization.md, PR #210 (0.7.0.45), PR #211 (0.7.0.47 — closed-as-superseded)
---

# Plan 89 / `0.7.0.48` — Remove signup-disabled / single-user MVP gate entirely

## Goal

Remove the entire signup-disabled / single-user MVP gating concept across template, route, API, model, sample data, tests, and docs. **Multi-user signup is the default**: first user becomes admin (existing behavior preserved); subsequent users sign up as regular users (`is_admin=False`) without any gate. No 403 on second signup. No amber banner. No `_compute_signup_disabled` helper. No `allow_multiple_users` enforcement.

## Why this scope (owner directive 2026-05-24)

> "Lets remove the whole signup disabled logic please. its not right we will not be restricting users to a single user instance. i think this is causing more churn than needed. we need to properly go through all the code to remove this properly."

Three patches around `signup_disabled` (plan 10c, 0.7.0.45, 0.7.0.47) confirm the gate's design assumption doesn't fit the deployment model. Each patch creates a new edge case for the next one to chase. The clean cut is to remove the gate entirely.

**The 0.7.0.47 patch attempt** (PR #211, closed) was a UI workaround that left the underlying gate intact — banner still rendered, 403 message still mentioned "single-user MVP," operators with stale users still saw confusing UX. Owner verified this was insufficient.

## Design decision

| Option | Chosen | Rationale |
|---|---|---|
| **A** — Remove gating entirely; first user is admin, subsequent users are regular | ✅ | Owner directive; matches standard self-hosted patterns (Gitea, Nextcloud, Vaultwarden); eliminates 3+ patches of churn |
| B — Keep gate but flip `allow_multiple_users` default to True | ✗ | Toggle remains a footgun; tests still churn around it; operators with `False` legacy rows still hit the gate |
| C — Add new `Settings.allow_signups` admin toggle (default True) for future close-down | ✗ | Just renames the problem; can be filed as future `0.7.0.50` follow-up when an operator actually needs it |

Selected: **Option A**. The deferred follow-ups (column drop migration + future admin-toggle-to-close-signups) are filed as `0.7.0.49` + `0.7.0.50` respectively.

## Build sequence

### Commit 1 — code removal (single commit; tests below ride along)

#### Backend code

1. **`src/api/auth.py:post_signup`** (lines ~155-230):
   - DELETE the entire `if existing_count > 0: ... allow_multi_scalar ... if not allow_multi: return 403 ...` block (~25 LOC).
   - KEEP the `count_row → existing_count` probe (still feeds `is_first_user = existing_count == 0` for admin flag).
   - UPDATE docstring: remove "gated by Settings.allow_multiple_users" framing; replace with "First user becomes admin (`is_admin=True`); subsequent users sign up as regular users."
   - The `Email already registered → 400` branch (line ~226) STAYS — that's a different concern (duplicate-email handling, not a gate).
   - Imports: keep `Settings` (still used in skeleton creation at line ~244 `session.add(Settings(user_id=user.id))`).

2. **`src/ui/routes/auth.py`** (lines 41-69 + 86-95):
   - DELETE `_compute_signup_disabled` helper entirely (lines 41-69).
   - In `get_login` (lines 72-97): drop the `signup_disabled = await _compute_signup_disabled(session)` call AND the `"signup_disabled": signup_disabled` context key.
   - Imports: drop `from sqlalchemy import func` + `from sqlmodel import select` IF no other ref in file; drop `Settings` from `from models import Settings, User` IF unused. (Engineer verify via grep before deleting imports.)
   - Update docstring on `get_login`: drop the "Plan 10c (10c.2b, 2026-05-11)" reference to `signup_disabled`; mention plan 0.7.0.48 supersession.

#### Frontend template

3. **`src/ui/templates/pages/login.html`** (current state on `main` is the 0.7.0.45 baseline):
   - DELETE line 4: `{% set signup_disabled = signup_disabled | default(false) %}`
   - DELETE the Jinja comment block at lines 5-10 (0.7.0.45 explanation).
   - REWRITE line 10 from `{% set is_signup = _requested_signup and not signup_disabled %}` → `{% set is_signup = _requested_signup %}`. (Keeping `_requested_signup` as the canonical name in case future code wants the explicit intent; alternative: collapse to `{% set is_signup = (mode == "signup") %}` and drop `_requested_signup` too — engineer's call based on which reads cleaner.)
   - DELETE entire banner block lines 33-51 (the Jinja comment + the `{% if _requested_signup and signup_disabled %}<div data-signup-disabled-banner>...{% endif %}`).
   - REWRITE alt-CTA block lines 92-107: drop the `{% elif not signup_disabled %}` branch — collapse to plain `{% if is_signup %} ... {% else %} ... {% endif %}` with the existing two CTAs (Sign in / Create account). Drop the 0.7.0.45 comment about hiding the link.

#### Models + sample data

4. **`src/models/settings.py:200`**:
   - Flip `allow_multiple_users: bool = Field(default=False)` → `Field(default=True)`.
   - Replace docstring (lines 196-199) with: `# Plan 0.7.0.48 (2026-05-24): deprecated — field retained for schema compatibility but no longer read by code. Multi-user signup is the default; first user becomes admin. Drop via alembic migration in 0.7.0.49 follow-up.`

5. **`src/db/sample_data_models.py:510-512`**:
   - Flip `allow_multiple_users: bool = False` → `True`.
   - Update comment to mirror the SQLModel deprecation note.

#### Doc comments (cleanup, not behavior)

6. **`src/db/session.py:6`**: "single-user MVP doesn't need a pool; revisit if connection-overhead" → "self-hosted deployments don't need a pool; revisit if connection-overhead matters at multi-user scale" (one-line wording fix).

7. **`src/ui/routes/design.py:32`**: "Cheap: read settings for user_id=1 (single-user MVP)." → "Cheap: read settings for user_id=1 (dev/seed convention; multi-user wiring is post-0.7.0.48 follow-up)."

8. **`src/api/settings.py:314`**: "cross-tenant drain in any future allow_multiple_users path." — keep the cross-tenant warning, drop the stale `allow_multiple_users` framing.

### Commit 1 (continued) — test cleanup

9. **`tests/test_first_run_walkthrough.py`**:
   - DELETE 3 tests (currently on `main` they don't all exist — they were authored in the closed PR #211 — engineer verify against current branch state):
     - On `main` baseline, the file has: `test_setup_help_reachable_without_auth`, `test_login_page_renders_signup_form_when_no_user`, `test_login_page_renders_signin_form_by_default`, `test_signup_disabled_banner_when_user_exists`, `test_signup_disabled_renders_welcome_back_header`, `test_setup_help_link_path_matches_route`, `test_login_form_has_hx_target_error_for_4xx_swap`.
     - DELETE: `test_signup_disabled_banner_when_user_exists` + `test_signup_disabled_renders_welcome_back_header` (both pin the gone-gone gate).
     - KEEP unchanged: the other 5 (setup-help reachable, signup-no-user, signin-default, route-name, hx-target-error).
   - Update module docstring (lines 1-13) to drop the bullet "3. `/login` renders the amber signup-disabled banner when a User exists." Replace with: "3. The signup form posts to `/api/v1/auth/signup` and is always renderable on `/login?mode=signup` (no gating per plan 0.7.0.48)."

10. **`tests/test_pages.py`**:
    - DELETE `test_login_signup_mode_renders_banner_on_seeded_db` (lines ~300-326). The test pins the banner; banner is gone.
    - KEEP `test_login_signup_mode_renders_form_on_fresh_db` (~lines 278-298) but drop the misleading "on_fresh_db" qualifier — rename to `test_login_signup_mode_renders_form` and drop the monkeypatch of `_compute_signup_disabled` (helper no longer exists). The test now just asserts `?mode=signup` renders the signup form unconditionally.
    - DELETE the section comment at line 247: "Plan 10c (10c.2) — signup CTA promotion + signup_disabled gate" → replace with "Plan 10c (10c.2a) + 0.7.0.48 — signup CTA promotion."

11. **`tests/test_auth.py`** — ADD new test `test_signup_succeeds_when_user_exists` to pin the new behavior:
    ```python
    @pytest.mark.asyncio
    async def test_signup_succeeds_when_user_exists():
        """Plan 0.7.0.48 (2026-05-24, supersedes 10b/10c/0.7.0.45/0.7.0.47):
        no signup gate. Second signup succeeds with is_admin=False; first
        user (admin) remains unchanged.
        """
        # Use existing test fixtures for app + session; signup user1 (admin)
        # then signup user2 (regular). Both get 204; user2.is_admin == False.
        ...
    ```
    Engineer should look at existing `test_auth.py` patterns for app/session fixtures + craft a test that reuses them.

12. **`tests/test_auth_gate.py:328`** — docstring reference to `_compute_signup_disabled` needs to drop or be rephrased. Engineer read the test to see if the shape-reference is load-bearing for the test logic; if it's just docstring trivia, replace with a generic reference to "User count probe pattern".

13. **`tests/test_settings_observability_dashboards.py:183, 505`** — both references touch `allow_multiple_users` in a settings shape fixture. With the default flipped to True, the fixture needs to match. Engineer flip the value at line 183. Line 505 is a docstring "Phase-1 single-user MVP" — update to "dev fixture; multi-user-ready post-0.7.0.48."

14. **`tests/test_sample_data.py:267`** — comment "N.10: every row is `user_id=1` (single-user MVP)" — update to "N.10: every row is `user_id=1` (dev sample data convention; production multi-user uses real user IDs)".

### Commit 1 (continued) — docs

15. **`docs/ARCHITECTURE.md:183`** — bullet "**Multi-user gate:** new signups blocked when a user exists AND `Settings.allow_multiple_users=False` (plan 10b)." → replace with "**Open signup:** first signup becomes admin (`is_admin=True`); subsequent signups are regular users (`is_admin=False`). No gate (plan 0.7.0.48). Operators who need to lock down signups gate externally via firewall / reverse-proxy / OIDC, or wait on a future `Settings.allow_signups` admin toggle (`0.7.0.50` follow-up)."

16. **`ROADMAP.md`** — engineer flips on commit:
    - 0.7.0.47 row Status: leave as `[ ]` (PR #211 closed; row becomes obsolete). Update Notes column to: `**CLOSED-AS-SUPERSEDED 2026-05-24 by `0.7.0.48` per owner directive — full removal of the gate replaces the UI patch attempt.**` and Status checkbox to `[x]` with the explicit superseded annotation.
    - Add `0.7.0.48` row with Status `[~]` HIGH priority; plan reference.
    - Add `0.7.0.49` row LOW: "Alembic migration to drop `Settings.allow_multiple_users` column (vestigial post-0.7.0.48; safe to drop once we're sure no migration-replay path needs it)."
    - Add `0.7.0.50` row LOW: "Optional `Settings.allow_signups: bool` admin toggle (default True) for operators who later want to close down signups after seeding their team — future feature, no current need."
    - Bump "Last updated" line.

17. **`docs/RUNBOOK.md`** — search-and-replace pass: grep for "single user" / "single-user" / "allow_multiple_users". Likely 1-2 stale references in operational guidance. Update to multi-user-default framing.

### Files NOT changed (intentional)

- **`Settings.allow_multiple_users` column in PG** — no alembic migration this PR. Field becomes vestigial; engineer flips default for new-row creation but existing operators' DBs keep their `False` value (now harmless since code never reads it). Drop migration filed as `0.7.0.49`.
- **`docs/plans/archive/10b-*.md`, `archive/10c-*.md`** — archived plans are historical; don't edit. Their references to `signup_disabled` are factually accurate at archive-time.
- **`src/naavik.egg-info/PKG-INFO`** — gitignored auto-generated; not touching.
- **`tests/test_alembic_*`** — schema-snapshot tests stay; the column is still there.
- **`setup_help.html`** — diagnostic-only page; `state.user_count` rendering remains accurate. No banner-link cleanup needed (banner is gone).

## Files changed (estimate)

| File | LOC delta | Why |
|---|---|---|
| `src/api/auth.py` | -25 / +5 | Delete gate block; update docstring |
| `src/ui/routes/auth.py` | -33 / +3 | Delete helper; clean up context + imports |
| `src/ui/templates/pages/login.html` | -28 / +5 | Delete banner block + signup_disabled set + 0.7.0.45 comments |
| `src/models/settings.py` | -2 / +5 | Flip default + deprecation comment |
| `src/db/sample_data_models.py` | -2 / +2 | Flip default + comment |
| `src/db/session.py` | -1 / +1 | Wording fix |
| `src/ui/routes/design.py` | -1 / +1 | Wording fix |
| `src/api/settings.py` | -1 / +1 | Wording fix |
| `tests/test_first_run_walkthrough.py` | -45 / +3 | Delete 2 tests + docstring tweak |
| `tests/test_pages.py` | -30 / +5 | Delete banner test + simplify form test |
| `tests/test_auth.py` | +20 | New `test_signup_succeeds_when_user_exists` |
| `tests/test_auth_gate.py` | -2 / +2 | Docstring tweak |
| `tests/test_settings_observability_dashboards.py` | -2 / +2 | Fixture flip + docstring |
| `tests/test_sample_data.py` | -1 / +1 | Comment |
| `docs/ARCHITECTURE.md` | -1 / +3 | Multi-user gate bullet → open signup |
| `docs/RUNBOOK.md` | -2 / +3 | Wording cleanup |
| `ROADMAP.md` | +3 rows | 0.7.0.47 SUPERSEDED + 0.7.0.48 [~] + 0.7.0.49/50 follow-ups |
| `docs/plans/89-...md` | new (this plan) | n/a |

**Total: ~16 files; ~170 LOC delta net.**

## Manual QA gate (engineer-manual-qa-gate)

1. `rm -rf .naavik/db` + `nix run .#dev` (fresh DB path).
2. `curl /login` → "Welcome back" + "First time? Create account" link (the link works unconditionally now; no gating).
3. `curl /login?mode=signup` → "Create your account" + signup form + **NO amber banner** + `hx-post="/api/v1/auth/signup"`.
4. POST signup as `[email protected]` with valid 12-char password → 204 + `HX-Redirect: /onboarding`. Verify in PG: `SELECT id, email, is_admin FROM "user"` shows user1 with `is_admin=true`.
5. Logout (or wipe cookies). Visit `/login?mode=signup` AGAIN — still signup form, no banner.
6. POST signup as `[email protected]` with valid 12-char password → **204** (was 403 pre-fix) + `HX-Redirect: /onboarding`. Verify in PG: user2 row present with `is_admin=false`.
7. Login as user2 → 204 + `HX-Redirect: /` → main UI loads under user2's session.
8. Quick sanity check: `SELECT user_id, allow_multiple_users FROM settings` shows BOTH user1 and user2 rows; both with `allow_multiple_users=True` (new default for newly-created Settings rows). Existing pre-existing rows may still have `False` — that's expected (vestigial; harmless).

Log to `traces/<run-id>/engineer-deviations.log` per `engineer-manual-qa-gate` skill:
```
[ts] QA_GATE surface=/login outcome=pass evidence=<md5-or-screenshot-path>
[ts] QA_GATE surface=/login?mode=signup outcome=pass evidence=<md5-or-screenshot-path>
[ts] QA_GATE surface=POST-signup-user1 outcome=pass evidence='204 + HX-Redirect /onboarding; user1.is_admin=true'
[ts] QA_GATE surface=POST-signup-user2 outcome=pass evidence='204 + HX-Redirect /onboarding; user2.is_admin=false (was 403 pre-fix)'
```

**The user2-signup-succeeds test is the headline assertion** — engineer must confirm in PG that user2 was created (not just that the POST returned 204; somebody could 204 the wrong path).

## Risk + mitigation

| Risk | Severity | Mitigation |
|---|---|---|
| Open signup creates abuse surface on public deployments | MEDIUM | Operators expected to gate via firewall/OIDC/reverse-proxy externally (standard self-hosted pattern). `0.7.0.50` follow-up filed for future admin-toggle if operators ask for it. README + ARCHITECTURE doc updates make this explicit. |
| Existing operators' DBs have `allow_multiple_users=False` | LOW | Code no longer reads the column; the False value is harmless. Column drop deferred to `0.7.0.49`. |
| `Settings.allow_multiple_users` column drop in `0.7.0.49` could break schema-replay tests | LOW | That's a `0.7.0.49`-scoped concern — out of this PR's blast radius. |
| Tests that monkeypatch `_compute_signup_disabled` will ImportError | LOW | Engineer's deletion sweep MUST catch every `monkeypatch.setattr(auth_routes, "_compute_signup_disabled", ...)` — grep is the gate. Plan lists every known location (test_first_run_walkthrough.py + test_pages.py). |
| Engineer misses a `signup_disabled` reference in template/route/test | LOW | Plan's grep results in § Build sequence list every file; full `grep -rn signup_disabled src/ tests/` post-edit confirms zero hits. |
| First-time signup hardcodes admin via `is_first_user = existing_count == 0` — race condition on concurrent first signups | LOW | Pre-existing behavior from plan 10b; not in this PR's scope. Two concurrent first signups would race + the `Email already registered` 400 catches one of them. Documented in plan 10b's deviations. |
| Test count goes DOWN (deleting tests, not adding equivalent net) | NONE | Expected; the gate is being removed. New `test_signup_succeeds_when_user_exists` provides positive coverage. |

## Approval checklist

- [x] Owner directive honored — complete removal, not patch
- [x] No new CLI subcommand / vault scope / new env var
- [x] No alembic migration in this PR (column drop deferred to `0.7.0.49`)
- [x] Settings model field stays for schema compat; default flipped
- [x] First-user-becomes-admin behavior preserved (only the SECOND-and-on gate goes)
- [x] All `signup_disabled` references swept via grep
- [x] Tests cover positive path (`test_signup_succeeds_when_user_exists`) + delete stale negative tests
- [x] Per § Doc-sizing matrix Medium-Large — full plan + engineer dispatch (manager-direct exceeds boundary)
- [x] PR_REVIEW_GATE — hacker + architect dispatched in parallel per § Parallel reviewer invariant
- [x] PR #211 already closed (owner rejected the UI-patch approach)
