---
Status: DRAFT
Type: execution
Authored: 2026-05-20
Last updated: 2026-05-20
Depends on: plan 56 (archived; 0.2.7 batched-closeout exemplar)
GitHub: -
---

# 57 · 0.2.7 small batched closeout

## Goal

Close four small/medium 0.2.7 rows in one PR: `0.2.7.01` discover.py `JobRead` projection (5 callsites), `0.2.7.13` onboarding offline retry buffer (IndexedDB queue), `0.2.7.15` portfolio API `?version=v1` query param, and `0.2.7.23` sibling IDOR helpers in `settings.py` (3 view functions). All four are mechanical, share the same quality bar, and individually too small for their own PRs.

## Why

Plan 56 (PR #156) shipped nine 0.2.7 follow-ups in one PR and demonstrated batching works for this shape of work. These four rows are the next-cleanest unblocked items in the 0.2.7 queue. `0.2.7.06` (paired UI editors, 150-200 LOC) gets its own plan 58; `0.2.7.12` (auto-apply dispatch) + `0.2.7.17` (NAAVIK_PERSISTENCE removal) need mild brainstorming and get plans 59 + 60. The remaining 8 rows (`0.2.7.07/10/11/14/16` + Phase 2+ / Phase 4+ / Phase 6+ scope) stay deferred until scheduled.

Per `manager.md § Batching small tasks` + plan 56 exemplar: bundle mechanical fixes that share a common quality bar. Three of the four items are defense-in-depth (`raw_meta` projection ahead of fake-session retirement, IDOR helpers ahead of multi-user, API versioning ahead of cv.astro v2 schema). One is feature work (offline retry buffer per INTERACTIONS.md § H.3 deferred surface). None touches CLI / vault / new env vars / new on-disk paths.

## Proposal

Four items ship on `feat/0.2.7-small-batched` from `main`. Each item is committed independently for review legibility; final commit body carries `Refs ROADMAP 0.2.7.NN` per item (no GH Issue for `0.2.7.01/13/15`; `0.2.7.23` issue if filed). Branch prefix `feat/` per `docs/PLAYBOOK.md § F` — primary item is defense-in-depth feature work plus a new offline-retry feature; mixed src + tests + minor JS but no docs/process changes.

### Item 1 — discover.py `JobRead` projection (0.2.7.01, LOW)

**Rationale.** Filed via PR #146 hacker review (NOTE-1). Five callsites in `src/ui/routes/discover.py` return `Job.model_dump(mode="json")` directly — the sample-data `Job` model carries `raw_meta` (verified `src/db/sample_data_models.py:241`), so when discover graduates from sample-data to live `job_service.list_jobs` (multi-PR fake-session retirement refactor), these endpoints will leak scraper-controlled JSONB. Pattern propagation from plan 46 / `0.2.0.11c` (`src/ui/routes/jobs.py:get_job_json`) + plan 56 / `0.2.7.18` (postmortem redact). `JobRead` schema at `src/models/job.py:248-294` already excludes `raw_meta`.

**Files.**

- `src/ui/routes/discover.py` — five callsites to convert. Pattern: replace `[j.model_dump(mode="json") for j in items]` with `[JobRead.model_validate(j).model_dump(mode="json") for j in items]`; replace single-job `job.model_dump(mode="json")` with `JobRead.model_validate(job).model_dump(mode="json")`. Add `from models import JobRead` at top (line ~16 import block).
  - `get_jobs` at `src/ui/routes/discover.py:242-256` — list projection on the `items` comprehension at line 256.
  - `post_job_by_url` at `:259-278` — single-object projection on return at line 278.
  - `post_rescore` at `:281-289` — single-object projection on return at line 289.
  - `discover_saved` at `:292-294` — list projection at line 294.
  - `discover_skipped` at `:297-299` — list projection at line 299.

- `tests/test_discover_jobread_projection.py` — new file. Five tests (one per callsite), each:
  1. Hit the endpoint via TestClient.
  2. Assert HTTP 200.
  3. Assert `"raw_meta" not in response.json()` (or, for list endpoints, `"raw_meta" not in item` for every item).
  4. Assert presence of at least one expected `JobRead` field (`id`, `external_id`) to verify the projection didn't collapse to empty.

  Pattern mirrors `tests/test_api_v1_jobs_get_json_uses_jobread_projection` (plan 46). `post_job_by_url` test needs a valid CSRF token + JSON body (`{"url": "https://example.com/job/123"}`); existing CSRF fixture pattern in `tests/test_discover_csrf.py` is the template.

**LOC.** ~10 LOC src + ~80 LOC tests. **Risk.** None — `JobRead.model_validate` over the sample-data `Job` works today (sample-data Job is structurally a superset). If a `JobRead` field is missing on a sample-data Job, the validator surfaces it loudly in the test (preferable to silent drift).

### Item 2 — Onboarding offline retry buffer (0.2.7.13, LOW)

**Rationale.** Filed via INTERACTIONS.md § H.3 ("Phase 1.x optional; not blocking MVP") — queue retries in a small client-side buffer on `htmx:sendError` and replay on `online` event. Today the onboarding form (step 3 review, `src/ui/templates/pages/_onboarding_step_review.html`) posts to `/api/v1/profile/from-extraction` via HTMX (`hx-post` at line 48); per-field autosave at `src/ui/templates/pages/profile_edit.html` uses the same HTMX wiring. Offline = `htmx:sendError` fires + the swap silently no-ops. User loses unsaved edits if they navigate away.

Buffer in IndexedDB (not localStorage — autosave payloads can include large bullets/answers; 5MB localStorage cap is close to the realistic upper bound, IndexedDB has effectively unbounded quota subject to browser policy). Retry on `online` event AND on next page-load-after-reconnect (covers tab-closed-before-reconnect). Max 5 retries per request with exponential backoff (1s / 2s / 4s / 8s / 16s, then drop). Surfaces a toast on final drop using the existing OOB toast pattern.

**Scope.** Onboarding screen (`/onboarding/*`) only for this row. Profile editor + Settings tabs share the same HTMX autosave pattern but are out-of-scope; once the IndexedDB queue module exists, applying it to profile-edit is a follow-up (file as `0.2.7.13a` if needed). Plan 56 / `0.2.7.21` already noted "could fold with 0.2.7.05" precedent for cosmetic deferral.

**Files.**

- `src/ui/static/offline_queue.js` — new file. ~80 LOC. Exports an IIFE that:
  1. On load: opens IndexedDB database `naavik_offline_queue` (object store `pending`, keyPath `id`, autoIncrement).
  2. Listens for `htmx:sendError` events. Filters to mutating requests (`hx-post` / `hx-put` / `hx-patch` / `hx-delete`) whose path starts with `/api/v1/profile` OR `/api/v1/onboarding`. Captures request method, URL, headers (including X-CSRF-Token if present), body, target element id, retry-count (0). Writes to `pending` store.
  3. On `online` event AND on `DOMContentLoaded` (when `navigator.onLine === true`): reads all rows from `pending`, fires them via `fetch()` with `Content-Type` matching original. On 2xx: deletes the row. On non-2xx OR network error: increments retry-count, re-stores with exponential-backoff delay (setTimeout schedule). On retry-count === 5: deletes row, dispatches `naavik:offline-queue-drop` event carrying the original URL + payload so the page can surface a toast via the existing OOB pattern.
  4. Idempotency guard: `window._naavikOfflineQueueLoaded` (mirrors `base.js` pattern at `src/ui/static/base.js:14-23`) so HTMX `hx-boost` swaps don't re-attach listeners.
  5. The script is gated to `/onboarding/*` pages by checking `window.location.pathname.startsWith('/onboarding')` at the top — keeps the IndexedDB open cost off other pages until the follow-up extends it.

- `src/ui/templates/components/auth_shell.html` (or whichever shell `onboarding.html` extends — verify; `pages/onboarding.html:1` shows `{% extends "components/auth_shell.html" %}`) — add `<script src="{{ url_for('static', path='/offline_queue.js') }}" defer></script>` AFTER the existing base.js script tag, gated by `{% if request.url.path.startswith('/onboarding') %}` if the shell is shared. Verify by reading `auth_shell.html` first; if the shell unconditionally loads base.js, gate the new script via the if-block.

- `tests/test_onboarding_offline_queue.py` — new file. ~50 LOC. Three Playwright tests using the project's existing Playwright surface (engineer can verify the pattern from `tests/test_settings_observability_dashboards.py` or similar; if no Playwright tests exist for onboarding, fall back to unit-testing the JS module via `pytest-playwright` headless OR a `playwright/sync_api` `page.add_script_tag` approach):
  1. `test_offline_queue_stores_failed_submit` — load `/onboarding/3`, simulate `navigator.onLine = false` via Playwright's `page.context.set_offline(True)`, trigger an autosave field-blur, assert IndexedDB `pending` store has 1 row.
  2. `test_offline_queue_replays_on_online` — same setup, then `page.context.set_offline(False)`, trigger `window.dispatchEvent(new Event('online'))`, assert the request fires (via `page.expect_request`) and IndexedDB row is deleted.
  3. `test_offline_queue_drops_after_5_retries` — mock the endpoint to return 500 five times, assert the row is removed and a `naavik:offline-queue-drop` event fired.

  If Playwright is too heavy for the engineer's plumbing, fall back to unit tests via a minimal jsdom-flavored harness OR explicitly defer all three tests to a Playwright follow-up row (`0.2.7.13b`) and ship just the JS module + a one-line README mention. Engineer chooses based on existing Playwright wiring; deviations log captures the choice.

**LOC.** ~80 LOC JS + ~5 LOC HTML + ~50 LOC tests (or ~10 LOC deferral comment). **Risk.** MEDIUM — IndexedDB API is async/callback-heavy; subtle bugs around transaction lifetime + `onerror` handlers are common. Mitigations: (a) keep the module ~80 LOC by avoiding wrappers; use raw `indexedDB.open` + `IDBTransaction` directly per MDN. (b) The idempotency guard prevents double-listener attachment under `hx-boost`. (c) Path gate (`startsWith('/onboarding')`) keeps the IndexedDB open cost off other pages so a regression in the module can't poison Profile / Settings traffic. (d) Manual QA: nix run .#dev, navigate to /onboarding, devtools Network tab "Offline" toggle, edit a field, assert no console error + IndexedDB inspector shows the row, then toggle back online and assert the row clears.

### Item 3 — Portfolio API versioning (0.2.7.15, LOW)

**Rationale.** Filed via 2026-05-01 triage. `GET /api/portfolio/cv` (at `src/api/portfolio.py:51-103`) returns the full Profile payload; cv.astro on crypticsoul.dev consumes it at build time. Future schema changes (renamed fields, added/removed fields, EEO/visa policy changes) need a versioning surface so the portfolio site can pin to a known shape. Add `?version=v1` literal-string query param now; default to `v1` if absent; reject unknown values with 400. Reserves `v2` for the next iteration without breaking existing crypticsoul.dev fetches.

**Decision locked** (per dispatch context): literal-string version param (not numeric `v=1` integer — string is more idiomatic for API versioning, matches `/api/v1/` URL-segment convention). Reject unknown versions with 400 (not a silent fallback to v1 — silent fallback hides client bugs). Apply to `/cv` only; `/resume.pdf` returns a binary blob that's effectively version-stable.

**Files.**

- `src/api/portfolio.py` — at `:51-103` `get_cv`, add `version: Annotated[Literal["v1"], Query()] = "v1"` parameter (Pydantic v2 `Literal` validator). FastAPI auto-rejects values outside the Literal set with a 422 (close enough to 400; engineer can spec 400 explicitly via a wrapper if the spec demands). The handler body doesn't branch on `version` today — the Literal acts as a validation gate + reserves the surface. Add a one-line comment: `# version=v1 is the only shape today; v2 will branch the payload-build step at line 92.` Update the docstring summary to note the version param.

  Imports: add `from typing import Annotated, Literal` if not present; add `from fastapi import Query` if not present (already imported).

- `tests/test_portfolio_cv_versioning.py` — new file. ~30 LOC. Two tests:
  1. `test_get_cv_default_version_returns_v1_payload` — call `GET /api/portfolio/cv` (no param); assert 200, payload schema matches existing `assert_no_pii` contract.
  2. `test_get_cv_unknown_version_rejected` — call `GET /api/portfolio/cv?version=v99`; assert 422 (or 400 if engineer chose to wrap; matches Pydantic Literal behavior).

  Pattern mirrors existing `tests/test_portfolio_sync.py`.

**LOC.** ~3 LOC src + ~30 LOC tests. **Risk.** None — Pydantic `Literal` is a documented FastAPI pattern; adding a Query param with a default doesn't break existing clients (cv.astro doesn't pass `?version=`, gets default `v1`).

### Item 4 — Sibling IDOR helpers in `settings.py` (0.2.7.23, MEDIUM)

**Rationale.** Filed via PR #156 hacker observation. Three view functions in `src/ui/routes/settings.py` retain hardcoded `user_id=1` after plan 56 fixed only `_build_sources_view`:

- `_recent_scrape_runs_view` at `src/ui/routes/settings.py:224-229` — calls `job_service.list_recent_scrape_runs(session, user_id=1, limit=50)`.
- `_submission_failures_view` at `:232-236` — calls `application_service.aggregate_submission_failures(session, user_id=1)`.
- `_llm_cost_cap_view` at `:239-245` — calls `llm_tracker.today_cost_usd(session, user_id=1)`.

Same shape as `0.2.7.02` (plan 56 item 1) — not exploitable in single-user Phase 1, becomes cross-user info-disclosure on multi-user. Pattern: thread `user_id` through each helper; resolve at the route boundary via the existing `_effective_user_id(user)` helper at `src/ui/routes/settings.py:312-320` (already in place from plan 56).

**Files.**

- `src/ui/routes/settings.py` — three signature changes + three call-site updates:
  1. `_recent_scrape_runs_view(session)` → `_recent_scrape_runs_view(session, *, user_id: int)`. Thread `user_id` into `job_service.list_recent_scrape_runs(session, user_id=user_id, limit=50)`.
  2. `_submission_failures_view(session)` → `_submission_failures_view(session, *, user_id: int)`. Thread into `application_service.aggregate_submission_failures(session, user_id=user_id)`.
  3. `_llm_cost_cap_view(session, settings)` → `_llm_cost_cap_view(session, settings, *, user_id: int)`. Thread into `llm_tracker.today_cost_usd(session, user_id=user_id)`.

  At the call sites inside `_ctx_for_tab` (`:208/211/214`), pass `user_id=user_id` (the function already accepts `user_id: int = 1` at `:174`). Update `_ctx_for_tab`'s default `user_id=1` to remain (fake-session compat) — the threading change is one-line per helper.

  At the route handlers that call `_ctx_for_tab`:
  - `get_settings_sources` at `:448-464` — already threads via `user_id=_effective_user_id(_user)` (plan 56). No change.
  - `get_settings_submissions` at `:467-482` — currently calls `_ctx_for_tab(request, "submissions", session=session)` without user_id; add `, user_id=_effective_user_id(_user)`.
  - `get_settings_llm_provider` at `:485-506` — currently calls `_ctx_for_tab(request, "llm-provider", session=session)` without user_id; add `, user_id=_effective_user_id(_user)`.
  - `get_settings` (catch-all at `:442-445`) — no session/auth dep yet; `_user` not collected. Out-of-scope; the catch-all already has user_id=1 fallback and a follow-up `0.2.7.24` row can tighten it (file at archive time if hacker review re-flags).

- `tests/test_settings_idor_helpers.py` — new file. ~60 LOC. Six tests (2 per helper):
  1. `test_recent_scrape_runs_view_threads_user_id` — mock `job_service.list_recent_scrape_runs`, call `_recent_scrape_runs_view(session, user_id=42)`, assert the mock received `user_id=42`.
  2. `test_recent_scrape_runs_view_cross_user_filter` — seed two users via NAAVIK_LIVE_DB or fake session, assert user-42 doesn't see user-1's scrape runs.
  3. `test_submission_failures_view_threads_user_id` — same shape for `aggregate_submission_failures`.
  4. `test_submission_failures_view_cross_user_filter` — same shape.
  5. `test_llm_cost_cap_view_threads_user_id` — same shape for `llm_tracker.today_cost_usd`.
  6. `test_llm_cost_cap_view_cross_user_filter` — same shape.

  Mirror plan 56 item 1's `test_build_sources_view_threads_user_id` test shape; reuse the fake-session fixture pattern.

**LOC.** ~10 LOC src + ~60 LOC tests. **Risk.** LOW — pure pass-through change; `_effective_user_id` helper is already in place (plan 56). Engineer verifies no other callers of these three view functions exist outside `_ctx_for_tab` (`grep -rn _recent_scrape_runs_view src/`).

### Build sequence

1. Branch `feat/0.2.7-small-batched` from `main`.
2. Commit 1 — Item 1 (discover.py JobRead projection + tests). Body: `Refs ROADMAP 0.2.7.01`.
3. Commit 2 — Item 2 (offline_queue.js + auth_shell wire + tests). Body: `Refs ROADMAP 0.2.7.13`.
4. Commit 3 — Item 3 (portfolio.py versioning + tests). Body: `Refs ROADMAP 0.2.7.15`.
5. Commit 4 — Item 4 (settings.py IDOR helpers + tests). Body: `Refs ROADMAP 0.2.7.23`.
6. `uv run ruff check .` + `uv run ruff format --check .` + `uv run pytest -x`.
7. Optional manual QA: nix run .#dev, devtools Network "Offline" toggle on /onboarding, verify IndexedDB queue + replay round-trip.
8. `gh pr create` using `.github/pull_request_template.md`. PR body summarizes items + per-item LOC table.

### Risk + mitigation

| Risk | Likelihood | Impact | Mitigation |
| ---- | ---------- | ------ | ---------- |
| `JobRead.model_validate` fails on a sample-data `Job` field that doesn't match the schema | LOW | MEDIUM (item 1 broken) | Sample-data Job is structurally a superset of `JobRead`. Test fails loudly; engineer adjusts sample-data field shape OR temporarily adds `model_config = ConfigDict(extra="ignore")` per-call (deviation logged). |
| IndexedDB API surface bugs (transaction lifetime, error-handler misses) leak unhandled rejections | MEDIUM | MEDIUM (item 2 silent failure) | Keep module thin (~80 LOC); avoid wrapper libraries; manual QA via devtools Network "Offline" toggle before merge. Path-gate (`/onboarding` only) contains blast radius. |
| Onboarding shell already loads base.js — gated script tag doesn't pick up the path filter | MEDIUM | LOW (script loads everywhere) | Engineer reads `components/auth_shell.html` BEFORE editing; if shell is shared, gates the new `<script>` via Jinja `{% if request.url.path.startswith('/onboarding') %}`. JS-side path gate at module top is the second line of defense. |
| Pydantic `Literal["v1"]` rejection returns 422 not 400 | LOW | LOW (item 3 spec drift) | Default FastAPI behavior is 422 for Literal validation; the dispatch context said "reject unknown versions with 400" — engineer chooses 422 (default, idiomatic) and notes the deviation if user pushes back. Document in PR description. |
| Test 4-6 cross-user filter requires NAAVIK_LIVE_DB | MEDIUM | LOW (CI gates only LIVE_DB tests) | Pattern from plan 56 item 1 test 2 already gates via `@pytest.mark.live_db` or equivalent; engineer follows the existing convention in `tests/test_settings_sources_route.py`. |
| Sample-data Job model contains fields not in `JobRead` schema (e.g. `raw_meta`) — `model_validate` rejects | LOW | MEDIUM | `JobRead` has `model_config = ConfigDict(from_attributes=True)` (line 257); `from_attributes` mode ignores extra source fields. Verified plan 46 / `0.2.0.11c` shipped the same projection over a `Job` w/ `raw_meta`. |

## Open questions

(none — all decisions locked per dispatch context)

## Approval checklist

(none — auto-approve per dispatch context; manager dispatches engineer immediately on plan land)
