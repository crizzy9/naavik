---
Status: EXECUTED
Type: execution
Authored: 2026-05-20
Last updated: 2026-05-20
Depends on: -
GitHub: -
Shipped: 2026-05-20
---

# 55 · 0.2.6 tooling batched closeout

## Goal

Close four mechanical 0.2.6 paper cuts in one PR: pre-existing ruff violations (0.2.6.06), DB-test gating gap (0.2.6.07), `A.28-board-restructure.sh` hardening (0.2.6.08), and backup / disaster-recovery docs for self-hosters (0.2.6.09). Defer 0.2.6.01–0.2.6.04 (Phase 2+/6 entity work) to 0.2.7.08–0.2.7.11; route 0.2.6.05 (NAAVIK_PERSISTENCE removal) to its own plan 56.

## Why

All four rows are legacy paper cuts blocking 0.2.6 phase close. 0.2.6.06 + 0.2.6.07 are CI hygiene for first real release; the 75 pytest failures + 10 ruff violations have hidden behind `_skip_if_no_db()` propagation gaps and pre-existing `migrations/` violations since plan 09 (2026-05-02). 0.2.6.08 is defense-in-depth on a one-shot migration runbook flagged by PR #63 review (Issue #64) — small, contained, no architectural change. 0.2.6.09 closes a `POST_PHASE_1.md` cross-cutting concern (#7) filed via 2026-05-20 release-readiness audit (#145): self-hosters today have no recipe for off-site backup. Batching per `manager.md § Batching small tasks` avoids four trivial PR-review cycles. None touches CLI / vault / Settings UI / on-disk artifact surface (item 4 documents an existing path, doesn't introduce one); no new env vars or operational state.

## Proposal

Four items shipped in one PR on `chore/0.2.6-tooling-batched-closeout`. Each item independently passes `uv run ruff check . && uv run pytest -x` after engineer ships. Final commit body carries four `Closes #N` trailers (one per filed Issue). Manager's BOOKKEEPING flips ROADMAP rows + files 0.2.7.08–0.2.7.11 follow-up rows for the deferred 0.2.6.01–0.2.6.04 work; row 0.2.6.05 flips to `[—]` with Notes "Moved to plan 56 (NAAVIK_PERSISTENCE removal — bigger refactor)".

### Item 1 — Pre-existing ruff violations cleanup (0.2.6.06 / DEF-24, Issue #55)

**Rationale.** PR #53 (A.15) devops review confirmed 10 ruff violations across 4 files are pre-existing (stash technique: `git stash -u && uv run ruff check . && git stash pop` showed the violations remain when the PR diff is gone). Codes are UP007 (PEP 604 `X | None`), UP035 (deprecated `typing.X` imports), and I001 (import ordering). All auto-fixable via `ruff check --fix`. No behavior change.

**Files.**

- `migrations/versions/0001_initial.py` — 4 violations. UP007 + UP035 + I001 mix. Run `uv run ruff check --fix migrations/versions/0001_initial.py`.
- `migrations/versions/0002_settings_multi_users.py` — 4 violations. Same code mix as 0001. Same fix command.
- `.claude/naavik_ops/lib/roadmap.py` — 2 violations (UP007 + I001 per PR #53 audit). Run `uv run ruff check --fix .claude/naavik_ops/lib/roadmap.py`.
- `.claude/naavik_ops/plan.py` — engineer runs `uv run ruff check . 2>&1 | grep "naavik_ops"` first to capture current state; if violations exist here (the directive flagged this file as in-scope alongside the others), include in the fix. If clean, skip.

After fixing, run `uv run ruff format .` to normalize any line-length changes introduced by import re-ordering. Engineer then runs the FULL `uv run ruff check .` to confirm 0 violations.

**LOC.** ~10 LOC equivalent (lint-only, auto-fixed). **Tests.** `uv run ruff check .` returns 0 violations. No new test files — the existing `tests/test_no_legacy_jobsource_imports.py` + `tests/test_no_vault_imports.py` + `tests/test_no_direct_http_imports.py` already lint-guard regressions.

### Item 2 — DB-test gating gap (0.2.6.07 / DEF-25, Issue #56)

**Rationale.** PR #53 devops review identified 65 pytest failures across 11 test files when `NAAVIK_LIVE_DB` is unset (default). Root cause: tests hit DB-backed routes via TestClient without the `_skip_if_no_db()` gate, triggering asyncpg `InvalidPasswordError` on `localhost:5432` (the dev orchestrator runs on `5433`; tests fall back to default Postgres conn). Canonical pattern at `tests/test_stub_endpoints.py:13–25` (the `_LIVE_DB` env probe + helper). Goal: propagate the helper to every failing file so `uv run pytest` (default env) yields 0 failures.

**Files.** Engineer identifies the 11 files in two steps:

1. `uv run pytest 2>&1 | grep "InvalidPasswordError\|connection refused\|password authentication failed" | head -50` to surface failing tests, then `awk` / `grep` to back out the file list (the candidates from `tests/` are likely a subset of: `test_alembic_0004.py`, `test_alembic_0005.py`, `test_alembic_0006.py`, `test_alembic_0007.py`, `test_alembic_0008.py`, `test_alembic_0009.py`, `test_api_settings_sources.py`, `test_document_generator.py`, `test_job_extractor.py`, `test_json_jobstore.py`, `test_scheduler_scraping.py`, `test_typst.py`, `test_seed.py`, `test_user_agents.py`). The directive says 11; engineer reports the actual 11 in deviation log if the count differs.
2. For each file, add the canonical helper at module top (after `from __future__ import annotations` + stdlib imports) following the `tests/test_stub_endpoints.py:13–25` pattern verbatim:

   ```python
   import os
   import pytest

   _LIVE_DB = os.environ.get("NAAVIK_LIVE_DB", "").strip().lower() in {"1", "true", "yes"}


   def _skip_if_no_db() -> None:
       if not _LIVE_DB:
           pytest.skip("set NAAVIK_LIVE_DB=1 to run DB-backed integration tests")
   ```

3. Call `_skip_if_no_db()` at the FIRST line of every test function that touches the DB (TestClient calls hitting `/api/v1/profile`, `/api/v1/bullets`, `/api/v1/jobs`, `/api/v1/discover`, etc., OR direct `AsyncSession` setup). Keep tests that are pure-shape (no DB) running unconditionally.
4. Validation: `uv run pytest -x` passes with `NAAVIK_LIVE_DB=` unset (default); `NAAVIK_LIVE_DB=1 uv run pytest -x` still runs all tests + must pass against a live `localhost:5433` Postgres if the engineer has the dev orchestrator running.

**LOC.** ~50 LOC equivalent (helper block × 11 files + per-test gate calls). **Tests.** `uv run pytest -x` (default env, no DB) returns success across all collected tests. No new test files — the helper IS the test surface.

### Item 3 — `A.28-board-restructure.sh` hardening (0.2.6.08 / A.28a, Issue #64)

**Rationale.** PR #63 hacker + devops review found 3 LOW defense-in-depth findings in `.claude/migrations/A.28-board-restructure.sh` (note: script lives at `.claude/migrations/` post-A.29 cleanup, NOT `scripts/` — engineer verifies path before editing). Script is one-shot historical migration, zero exploit today (all callers pass validated numeric Issue#s + node IDs), but invites future bug if anyone interpolates Issue titles into the `run()` helper's composed string.

**Files.**

- `.claude/migrations/A.28-board-restructure.sh:49–56` — replace the `eval "$@"` pattern in `run()` with arg-array pass-through:

  ```bash
  run() {
    # In dry-run, print the command. In apply, execute via "$@" arg array
    # (no eval — composed strings can't break the quoting boundary even if
    # future callers interpolate untrusted text).
    if $DRY_RUN; then
      printf '  DRY-RUN'; printf ' %q' "$@"; echo
    else
      "$@"
    fi
  }
  ```

  Caller sites (lines 70, 86, 105, 126, 139, 147, 162, 190, 221) currently pass a single composed string to `run` — engineer rewrites each to pass arguments as discrete tokens (e.g. `run bash "$GH_PROJECT" set-status "$ITEM_ID" Backlog` instead of `run "bash $GH_PROJECT set-status $ITEM_ID Backlog"`).

- `.claude/migrations/A.28-board-restructure.sh:1–17` — extend header comment with rollback contract block (4 lines):

  ```bash
  # Rollback contract:
  #   - Script is idempotent — re-runs are safe.
  #   - To revert: manually flip affected items via `.claude/naavik-ops gh set-status <item-id> <prev>` per the milestone-status snapshot taken before --apply.
  #   - No automated rollback (one-shot historical migration; replicating the pre-state requires the snapshot).
  ```

- `.claude/migrations/A.28-board-restructure.sh:23–43` — flip default from silent-apply to explicit-apply. Change `DRY_RUN=false` (line 23) to `DRY_RUN=true`. Update the help text (lines 28–40) to read `--apply Actually execute (default: dry-run only).`. Bare invocation now prints what each step WOULD do; `--apply` is required to mutate. The dry-run banner at line 233–243 already differentiates correctly — only the default needs to flip.

- ROADMAP § Agent System mirror-conventions update — the directive references "4-status refresh." Engineer reads the current § Agent System section, verifies the 4-status convention (`Todo` / `In Progress` / `Done` / `Backlog`) is documented per the post-A.28 board status convention codified in `docs/PLAYBOOK.md § Board status convention`. If `ROADMAP.md § Agent System` still references the legacy 3-status set, append a one-line update; if already 4-status, no edit needed (deviation log says so).

**LOC.** ~50 LOC shell diff (header expansion + run-helper rewrite + caller rewrites + flag flip). **Tests.** `tests/test_idempotent_scripts.py` extension OR new `tests/test_a28_script_shape.py` — a TestClient-free shell-script linter that asserts (a) the script header contains `Rollback contract:` substring, (b) the `run()` body contains `"$@"` and does NOT contain `eval`, (c) `DRY_RUN=true` appears before `DRY_RUN=false` in the file (so dry-run is the default branch). The script smoke test runs `bash .claude/migrations/A.28-board-restructure.sh --dry-run` and asserts exit 0 + stdout contains `Mode: DRY-RUN` (idempotent dry-run); bare-invocation test asserts exit 0 + same DRY-RUN banner (proving --apply is no longer silent default). DO NOT run `--apply` in CI (it would mutate GitHub state).

### Item 4 — Backup / disaster-recovery docs (0.2.6.09 / release-readiness #145)

**Rationale.** `docs/DEPLOYMENT.md § Operations` currently says "Backups: back up `.env` alongside the DB dump. `SECRET_KEY` must be preserved." That's a one-line stub; self-hosters need the full recipe. POST_PHASE_1.md cross-cutting concern #7 flagged this 2026-05-20. Canonical backup set is `.env` + `~/.naavik/data/snapshots/` (daily SQL gzip via `admin.daily_db_snapshot` cron from 0.1.0.23 — `src/scheduler/jobs.py:105–122`; Phase 1 ships touch-marker files, real `pg_dump` piping ships Phase 6 per existing comment). Encrypted vault was deleted in 0.2.0.01; no `key.bin` to back up. Audience: self-hosters running Nix or Docker Compose paths.

**Files.**

- `docs/DEPLOYMENT.md` — replace the current 1-line "Backups" bullet (line 197) with a NEW `### Backups + disaster recovery` H3 section under `## Operations`, placed BETWEEN the current "Secrets via `.env`" bullet and the "Reset dev DB" bullet. New section ~80 LOC covering:

  1. **Canonical backup set** — table mapping artifact → location → frequency → criticality:

     | Artifact | Location | Frequency | Why |
     |---|---|---|---|
     | `.env` | repo root (Docker) or `/etc/sops-nix/secrets/naavik_env` (NixOS) | On every secret change | API keys + `SECRET_KEY`; without it, JWTs + LLM access lost |
     | DB dump (`pg_dump` -Fc) | `${DATA_DIR}/data/snapshots/` (Phase 6+); manual `pg_dump` today | Daily | Full Job / Application / Profile / Bullet state |
     | Daily marker | `${DATA_DIR}/data/snapshots/snapshot-YYYY-MM-DD.marker` | Daily 02:00 UTC (cron) | Cron-liveness proof; not a backup itself — see `src/scheduler/jobs.py:105` |
     | Generated PDFs | `${DATA_DIR}/data/pdfs/` | Per-application | Re-generable from profile + Typst templates if lost |

     Note explicitly: **`~/.naavik/secrets.enc` + `~/.naavik/key.bin` no longer exist** (deleted in plan 26 / 0.2.0.01 — vault deprecation). No vault material to back up; the only secret-of-record is `.env`.

  2. **Daily snapshot today (Phase 1 status)** — `admin.daily_db_snapshot` writes touch-markers; for real DB dumps before Phase 6, self-hosters run `pg_dump` themselves. Example cron line for Docker:

     ```bash
     # /etc/cron.daily/naavik-backup (or systemd timer for NixOS)
     0 2 * * * docker exec naavik-db-1 pg_dump -Fc -U naavik naavik > /var/lib/naavik-backups/naavik-$(date +\%F).dump
     ```

     NixOS equivalent uses `systemd.services.naavik-backup` with `ExecStart=${pkgs.postgresql}/bin/pg_dump …`.

  3. **Off-site rotation pattern** — three example recipes (one per common backup tool). Self-hoster's responsibility — Naavik doesn't ship this — but the recipe lowers friction. Each ~10 LOC of shell, no real auth keys (placeholder `<your-bucket>`):

     ```bash
     # restic (encrypted, deduplicated; recommended)
     restic -r s3:s3.amazonaws.com/<your-bucket>/naavik backup .env /var/lib/naavik-backups/

     # borgbackup (encrypted, deduplicated; alternative)
     borg create user@host:/backups/naavik::naavik-{now} .env /var/lib/naavik-backups/

     # aws s3 sync (unencrypted; cheapest for trust-the-provider setups)
     aws s3 sync /var/lib/naavik-backups/ s3://<your-bucket>/naavik/ --delete
     ```

  4. **Recovery walkthrough** — 5-step bullet list:

     1. Stop the service (`systemctl stop naavik` or `docker compose stop naavik`).
     2. Restore `.env` to repo root / sops path. `chmod 0600 .env`.
     3. `pg_restore -U naavik -d naavik /var/lib/naavik-backups/naavik-YYYY-MM-DD.dump` against a fresh DB.
     4. `uv run alembic upgrade head` to bring the schema forward if the dump is older than the current migration head.
     5. `systemctl start naavik` / `docker compose up -d`. Visit `/login` — sessions issued before the backup are still valid (same `SECRET_KEY`).

  5. **Reminder block** — `SECRET_KEY` rotation invalidates ALL JWTs (active sessions die; users re-auth). Treat `.env` as crown jewels. The cloud tier ($15/mo) handles all of this for users who don't want to operate backups.

- `docs/RUNBOOK.md` — one-line pointer addition: under the existing operations section, add "Backup + DR procedure: see `docs/DEPLOYMENT.md § Backups + disaster recovery`." (Engineer locates the right slot; current RUNBOOK doesn't carry a backup section.)
- `docs/plans/POST_PHASE_1.md` — flip cross-cutting concern #7 ("Backup story") from `[ ]` to `[x]` with note "Documented in `docs/DEPLOYMENT.md § Backups + disaster recovery` via plan 55 / 0.2.6.09."

**LOC.** ~200 LOC docs (new H3 section, table + 3 code blocks + 5-step list + reminder; 1-line RUNBOOK pointer; 1-line POST_PHASE_1.md flip). **Tests.** No tests — pure docs change. Engineer verifies links resolve via `grep -n "Backups + disaster recovery" docs/RUNBOOK.md` after edit.

### Build sequence

1. Branch `chore/0.2.6-tooling-batched-closeout` from `main`. Note `chore/` prefix per `docs/PLAYBOOK.md § H — CONTRACT_CHANGE` (tooling + docs, not product feature).
2. Land items in order item 1 → item 2 → item 3 → item 4 (lint-only → test-gating → script-hardening → docs). Each item is one commit.
3. After each item: `uv run ruff check . && uv run ruff format --check . && uv run pytest -x`.
4. Final commit body: explicit `Closes #55`, `Closes #56`, `Closes #64`, `Closes #145` trailers (one per line, lowercase `closes` per `prepare-commit-msg` hook). Engineer hand-crafts the multi-trailer commit body.
5. `gh pr create` against `main` using `.github/pull_request_template.md`.

### Risk + mitigation

| Risk | Likelihood | Impact | Mitigation |
| ---- | ---------- | ------ | ---------- |
| Item 1 `ruff check --fix` introduces import-ordering changes that break a runtime import path (alembic auto-discovery, `.claude/naavik_ops` dispatcher) | LOW | High (migrations stop applying, dispatcher entry breaks) | Engineer runs full `uv run pytest -x` after fix. Migrations have their own test files (`tests/test_alembic_0001.py`…`tests/test_alembic_0009.py`); dispatcher has `tests/test_naavik_ops/`. If any regress, engineer reverts the specific I001 reordering and fixes manually. |
| Item 2 the precise 11 failing files differ from the directive's count (could be 10 or 14) | MED | Low | Engineer reports actual count in deviation log per `engineer-deviation-log` skill. The fix pattern is identical regardless of count — propagate `_skip_if_no_db()` to every file where `uv run pytest 2>&1 \| grep "InvalidPasswordError"` flags a failure. |
| Item 3 caller rewrites in `A.28-board-restructure.sh` (lines 70, 86, 105, 126, 139, 147, 162, 190, 221) silently change the executed command (e.g. variable expansion happens at different point) | MED | Medium (one-shot migration; if mis-run on a fresh fork it could mis-mirror) | Script is one-shot historical migration — already applied once on `main` fork per 2026-05-17 PR #63. Re-runs are idempotent per the in-file design. Engineer adds the shell-script linter (item 3 § Tests) to assert the structure, and runs `bash .claude/migrations/A.28-board-restructure.sh --dry-run` post-edit to confirm dry-run still parses + prints expected actions. |
| Item 4 the `pg_dump` / `restic` recipes drift from upstream syntax (CLI flags change) | LOW | Low | Recipes are illustrative; each line documents the conceptual operation. Engineer adds "Last verified: 2026-05-20" footer to the section. Updates ship as future doc paper cuts when a self-hoster reports drift. |
| `.claude/naavik_ops/plan.py` flagged in directive may not actually have violations (engineer's ruff scan returns 0) | MED | None | Engineer reports "no violations found in plan.py" in deviation log and skips. Item 1 still ships the other 3 files' fixes. |

## ROADMAP rows (post-merge mark-done by manager BOOKKEEPING)

```
| 0.2.6.06 | [ ] → [x] | PR #<N> squash <sha> (closes #55)  |
| 0.2.6.07 | [ ] → [x] | PR #<N> squash <sha> (closes #56)  |
| 0.2.6.08 | [ ] → [x] | PR #<N> squash <sha> (closes #64)  |
| 0.2.6.09 | [ ] → [x] | PR #<N> squash <sha> (closes #145) |
```

Manager also files four new rows in BOOKKEEPING for the deferred 0.2.6.01–0.2.6.04 items + one for 0.2.6.05:

```
| 0.2.7.08 | Onboarding offline retry buffer (deferred from 0.2.6.01)             | [ ] | — | DEF-07         | Optional, not blocking MVP. From INTERACTIONS.md § H.3. Moved from 0.2.6 per 2026-05-20 sweep — Phase 2+ scope. |
| 0.2.7.09 | `ProfileAnswer` reuse cache (deferred from 0.2.6.02)                  | [ ] | — | DEF-09         | Phase 2+ entity. From DATA_MODEL.md § J. Moved from 0.2.6 per 2026-05-20 sweep — Phase 2+ scope. |
| 0.2.7.10 | Portfolio API versioning (deferred from 0.2.6.03)                    | [ ] | — | DEF-12         | Phase 2+ consumer pinning. From triage 2026-05-01. Moved from 0.2.6 per 2026-05-20 sweep. |
| 0.2.7.11 | `JobEmbedding` semantic match (deferred from 0.2.6.04)                | [ ] | — | DEF-14         | Phase 6 pgvector work. From DATA_MODEL.md § H. Moved from 0.2.6 per 2026-05-20 sweep — Phase 6 scope. |
```

Then flips:

- 0.2.6.01–0.2.6.04 rows → `[—]` with Notes "Moved to 0.2.7.08–0.2.7.11 via 2026-05-20 sweep (Phase 2+/6 scope)".
- 0.2.6.05 row → `[—]` with Notes "Moved to plan 56 (NAAVIK_PERSISTENCE removal — bigger refactor)".
- 0.2.6 phase Status from "🟡 Queued" → "✅ Complete (2026-05-20)" after the four items merge + the five flips above land in the same BOOKKEEPING commit.

## Open questions

(none — all four items have unambiguous specs from the filing reviews; auto-approved per user directive 2026-05-20)

## Approval checklist

(manager auto-approved per user directive 2026-05-20 — no PLAN_GATE pause)

## Deviations from plan

(to be filled in at archive time by `.claude/naavik-ops plan archive`)
- **Item 2 (0.2.6.07 DB-test gating) is** — what: Item 2 (0.2.6.07 DB-test gating) is already satisfied by prior PRs. why: pytest -q already returns 1205 passed 26 skipped with no failures or InvalidPasswordError; plan was based on PR #53 audit and other PRs (likely #91 W6, #134, #138, #150) propagated _skip_if_no_db gates between then and 2026-05-20. impact: No code edits needed for item 2; manager will mark 0.2.6.07 done; ROADMAP row flips to [x]. surface: none.
- **Item 1 expanded scope by 3** — what: Item 1 expanded scope by 3 SIM rules in .claude/naavik_ops/{lib/roadmap.py,plan.py}. why: ruff scan returned 11 violations (plan said 10) — 8 UP007/UP035 auto-fixed plus 3 manual SIM108/SIM103/SIM105 in the two naavik_ops files the plan explicitly flagged. impact: Fully clean ruff state; no new operational surface; contextlib import added to plan.py. surface: port.
- **Item 3 fixed a pre-existing REPO_ROOT** — what: Item 3 fixed a pre-existing REPO_ROOT path bug in A.28-board-restructure.sh while hardening. why: script's REPO_ROOT was '../' relative to .claude/migrations/ instead of '../../' — file was moved by plan 24 (A.29) but the path math wasn't updated; smoke test caught it. impact: Script now resolves repo root correctly when invoked from any cwd; without the fix the new dry-run smoke test could not exit 0 from a fresh fork. surface: none.
- **Item 3 handled the special-case add-subissue** — what: Item 3 handled the special-case add-subissue caller (line 147) outside the run() helper. why: caller carried '2>/dev/null || true' shell redirections that cannot fit in an arg array; inlined the dry-run branch + direct-exec branch with the existing best-effort suppression. impact: Behavior identical to original; future callers that need shell control flow follow this pattern instead of run(). surface: none.
- **ROADMAP § Agent System mirror-conventions already** — what: ROADMAP § Agent System mirror-conventions already shows 4-status (line 527: Backlog / Todo / In Progress / Done). why: Plan said 'if already 4-status, no edit needed (deviation log says so)'. impact: No ROADMAP edit needed for item 3 sub-task. surface: none.
- **POST_PHASE_1.md item #7 is a numbered** — what: POST_PHASE_1.md item #7 is a numbered list, not a checkbox. why: Plan said flip '[ ]' to '[x]' but the actual format is a numbered prose item. impact: Inlined the documented-status with strikethrough on the prior 'no off-site backup story yet' clause + a pointer to docs/DEPLOYMENT.md § Backups + disaster recovery. surface: none.
- **Pre-existing ruff format misses in tests/test_naavik_ops/...** — what: Pre-existing ruff format misses in tests/test_naavik_ops/{test_plan,test_roadmap}.py left unfixed. why: Plan focused on 'ruff check' clean (achieved); these format issues are unrelated to the 4 named files and engineer guidance is 'Fix only issues your changes caused'. impact: ruff check . is fully clean; ruff format --check . still flags 2 unrelated test files — observation only, not a blocker for this PR. surface: none.

