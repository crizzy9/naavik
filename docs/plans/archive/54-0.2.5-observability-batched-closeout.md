---
Status: EXECUTED
Type: execution
Authored: 2026-05-20
Last updated: 2026-05-20
Depends on: 0.2.0.10 (scheduler shipped consecutive_scrape_failures); 0.2.0.05 (JobScrapeRun model); 0.2.0.13 (scraper_rate_limits)
GitHub: -
Shipped: 2026-05-20
---

# 54 · 0.2.5 observability batched closeout

## Goal

Close out the 0.2.5 release-version (operator-facing observability cleanup) in a single PR. Three small dashboard items ship: submission-result aggregates (0.2.5.02), daily LLM cost-cap widget (0.2.5.03), scraper-run history table (0.2.5.04). The fourth row (0.2.5.01 `Settings.scraper_aggressiveness`) closes-as-moot — superseded by the JSONB rate-limit dial that already shipped in 0.2.0.13. Two new release-readiness rows (0.2.5.05/06) deferred to 0.2.7.

## Why

0.2.5 has been queued since the 2026-05-01 paper-cut triage. The 2026-05-20 0.2.x sweep is the closeout window — these three are all small, read-only HTMX additions over data models that already exist (`Application.submission_artifacts.last_failure`, `ApiUsage.cost_usd` aggregates, `JobScrapeRun` rows). None touches the CLI / vault / Settings UI input surface; none adds env vars, on-disk artifacts, or migrations. Manager batched per `manager.md § Batching small tasks` — mechanical, no design decisions, no security-sensitive surfaces (read-only displays + per-user IDOR via existing `require_authed_session`).

The 0.2.5.01 close-as-moot reconciliation is the cleanup move: `Settings.scraper_rate_limits` JSONB shipped in 0.2.0.13 (PR #118 squash `19a3dbf`) supersedes the single-dial "aggressiveness" knob with per-source `rpm` / `delay_lo` / `delay_hi` tunables — strictly more expressive. Manager flips `0.2.5.01 → [x]` with `closed-as-moot — superseded by 0.2.0.13` deliverable note during BOOKKEEPING; no code ships for it.

## Proposal

Three items shipped in one PR on `feat/0.2.5-observability-batched-closeout`. Each item must independently pass `uv run ruff check . && uv run pytest -x`. Each closes a filed ROADMAP row via the BOOKKEEPING mark-done. Engineer drives the work; architect + hacker review in parallel.

All routes async; AsyncSession DI; `require_authed_session` user-gated; aggregation queries filter `Application.user_id == current_user.id` / `JobScrapeRun.user_id == current_user.id` / `ApiUsage.user_id == current_user.id`. No raw SQL — service-layer helpers only. DaisyUI table + chip + cost-card partials only; Lucide icons stroke 1.5.

### Item 1 — Submission-result observability dashboard (0.2.5.02, ROADMAP `0.2.5.02`)

**Rationale.** Operator needs to see "ATS X has been failing with kind=CAPTCHA on board=GREENHOUSE 14 times this week" at a glance. The data already exists in `Application.submission_artifacts["last_failure"]` JSONB (shape: `{kind, message, captured_at}`, written by `application_service._record_failure` at `src/services/application_service.py:354`). Today it's only surfaced inline on the Failed-DRAFT row in the tracking page. The closeout adds an aggregated dashboard at Settings · Submissions sub-tab.

**Files.**

- `src/services/application_service.py` — new `async def aggregate_submission_failures(session, user_id, *, since_days: int = 30) -> list[dict]`. Single `SELECT` over `Application` (soft-delete-aware via `deleted_at IS NULL` filter) returning rows shaped `{board: str, failure_kind: str, count: int, latest_at: datetime}`. Source data: JSONB path-extract `submission_artifacts->'last_failure'->>'kind'`. `GROUP BY board, failure_kind`. Order: `count DESC`. Postgres-native `jsonb_extract_path_text` via `sqlalchemy.func` — no raw SQL. Empty result for users with zero failed DRAFTs.
- `src/ui/routes/settings.py` — register new tab key `"submissions"` in `_TAB_TEMPLATES` mapping to `pages/_settings_submissions.html`. Extend `_ctx_for_tab` to call `aggregate_submission_failures` when `tab == "submissions"` and inject `submission_failures: list[dict]` into ctx. Settings page tab strip auto-renders new tab via existing pattern (see existing `_TAB_TEMPLATES` enumeration).
- `src/ui/templates/pages/_settings_submissions.html` — new partial. DaisyUI table: columns `BOARD / FAILURE KIND / COUNT / LATEST AT`. Chip-style severity coloring by `failure_kind` (CAPTCHA = amber, AUTH_REQUIRED = rose, RATE_LIMITED = slate, others = neutral). Empty state: card with `inbox` Lucide icon + "No submission failures recorded yet."
- `src/ui/templates/pages/settings.html` — add `submissions` to tab-strip enumeration (mirror the existing tab loop; static label "SUBMISSIONS").
- (No new env var, no migration, no CLI extension.)

**LOC.** ~50 LOC service helper (sqlalchemy `func.jsonb_extract_path_text`) + ~60 LOC template + ~15 LOC route wiring. **Tests** (~50 LOC): empty state (user with no failed DRAFTs returns `[]`); populated state (3 boards × 2 kinds → 6 aggregate rows, count + latest_at correct); per-user IDOR (User A's failures don't leak to User B's aggregate).

### Item 2 — Daily LLM cost-cap dashboard widget (0.2.5.03, ROADMAP `0.2.5.03`)

**Rationale.** Wave 6 will ship cap enforcement (cron flip from eager → lazy on cap-trigger). This item ships the visible progress UI as Settings · LLM Provider tab polish. Today the cap field (`Settings.daily_llm_cost_cap_usd`) exists in the DB but is invisible to operators — they discover it only in `.env.example`. The widget surfaces it next to the existing THIS MONTH / AVG / RATE LIMIT cost cards.

**Files.**

- `src/services/llm_tracker.py` — new `async def today_cost_usd(session, user_id) -> float`. Single `SELECT COALESCE(SUM(cost_usd), 0.0) FROM api_usage WHERE user_id = :u AND occurred_at >= :midnight_utc`. `midnight_utc` computed as `datetime.now(UTC).replace(hour=0, minute=0, second=0, microsecond=0)`. Returns `0.0` for users with no calls today.
- `src/ui/routes/settings.py` — `_ctx_for_tab` extends ctx with `today_cost_usd: float` and `cost_cap_usd: float | None` (passthrough from `settings.daily_llm_cost_cap_usd`) on the `llm-provider` tab path only (skip the query for other tabs — N+1 dodge).
- `src/ui/templates/pages/_settings_llm.html` — new card-row after the existing 3-cost-card grid (lines 103–119). DaisyUI progress bar via `<progress class="progress progress-primary">` with `value=today_cost_usd` `max=cost_cap_usd`. Pct label, "$X.XX of $Y.YY today" subtext, "resets at midnight UTC" hint. Cap unset (`cost_cap_usd is None`) → render "No daily cap set" placeholder card linking to `.env.example` comment ("Set `DAILY_LLM_COST_CAP_USD` in .env"). Over-cap state (`today_cost_usd >= cost_cap_usd`) → swap progress color to `progress-error` + amber `triangle-alert` Lucide icon + "Cap exceeded — Wave 6 will throttle new LLM calls when enforcement ships."
- (No new env var — `daily_llm_cost_cap_usd` already config-loaded via `.env`. No migration. No CLI extension.)

**LOC.** ~25 LOC service helper + ~30 LOC template addition + ~10 LOC route wiring. **Tests** (~30 LOC): cap unset → widget renders placeholder card; cap set + zero spend → 0% bar; cap set + partial spend → correct pct; over-cap state → progress-error color + warning text; midnight UTC boundary (sum excludes yesterday's calls).

### Item 3 — Scraper-run history UI page (0.2.5.04, ROADMAP `0.2.5.04`)

**Rationale.** `JobScrapeRun` model (shipped 0.2.0.05) has been silently accumulating rows for every scheduled scrape since 0.2.0.10 wired the scheduler. No UI surface today. Operators debugging "why is LinkedIn returning zero jobs" have to grep logs or open a psql session. The closeout adds a recent-runs table to Settings · Sources sub-tab (fold-in — same tab being extended by 0.2.0.16 first-run walkthrough; engineer scopes the merge as additive partial).

**Files.**

- `src/services/job_service.py` — new `async def list_recent_scrape_runs(session, user_id, *, limit: int = 50) -> list[JobScrapeRun]`. Single `SELECT FROM job_scrape_run WHERE user_id = :u ORDER BY started_at DESC LIMIT :limit`. SQLModel `select(JobScrapeRun)` per `count_jobs_by_source` pattern at line 359. Returns `list[JobScrapeRun]` — caller projects via `JobScrapeRunRead.model_validate` to avoid `raw_meta` JSONB exposure (defense-in-depth, same pattern as PR #146 item 3 / 0.2.0.11c).
- `src/ui/routes/settings.py` — `_ctx_for_tab` extends ctx with `recent_scrape_runs: list[JobScrapeRunRead]` (projected) on the `sources` tab path only. Pass through to template.
- `src/ui/templates/pages/_settings_sources.html` — APPEND new `<section>` after the existing "Workday companies" section. DaisyUI table: columns `SOURCE / STATUS / STARTED / DURATION / LISTINGS / NEW / ERRORS`. Status chip-colored via existing JobScrapeStatus enum (SUCCESS = emerald, PARTIAL = amber, FAILED = rose, TIMED_OUT = rose, RUNNING = sky). Duration formatted via `{{ run.duration_ms or '—' }}ms`; errors as truncated `errors[0] | truncate(60)` with `... +N more` suffix if `errors | length > 1`. Empty state: "No scraper runs yet — runs appear here when the scheduler fires."
- (No new env var, no migration, no CLI extension. Tab key already registered.)

**LOC.** ~40 LOC service helper + ~50 LOC template section + ~10 LOC route wiring. **Tests** (~50 LOC): empty state; populated with mixed statuses (5 runs spanning all 5 status enum values + 2 sources); per-user IDOR (User A's runs don't leak); LIMIT clause caps at 50 even when 60 rows exist; `raw_meta` not in projection output.

### Build sequence

1. Branch `feat/0.2.5-observability-batched-closeout` from `main`.
2. Engineer lands items in any order (independent). Recommendation: alphabetical-by-file (`application_service.py` → `llm_tracker.py` → `job_service.py` → routes → templates → tests).
3. After each item: `uv run ruff check . && uv run pytest -x`.
4. Final commit body: explicit `Closes` trailers for any GH Issues filed against 0.2.5.02/03/04 (manager files Issues at PLAN_APPROVE if not already present via `.claude/naavik-ops gh create-issue`). 0.2.5.01 / 0.2.5.05 / 0.2.5.06 are NOT closed via this PR — manager handles via BOOKKEEPING (see § Manager BOOKKEEPING below).
5. `gh pr create` against `main` using `.github/pull_request_template.md`.

### Risk + mitigation

| Risk | Likelihood | Impact | Mitigation |
| ---- | ---------- | ------ | ---------- |
| Item 1 JSONB path-extract `submission_artifacts->'last_failure'->>'kind'` syntax slips between SQLAlchemy expression API and raw text. | LOW | Medium | Use `sqlalchemy.func.jsonb_extract_path_text(Application.submission_artifacts, 'last_failure', 'kind')` — typed expression-API call, not text SQL. Existing application_service.py already imports `func`. |
| Item 2 `today_cost_usd` boundary off-by-one at midnight UTC rolls today's spend into tomorrow's window. | LOW | Low | Test the UTC midnight boundary explicitly — fix the seam to `datetime.now(UTC).replace(hour=0,...)` not local-tz `combine(date.today(), time.min)`. Tests assert sum excludes `occurred_at` rows at `midnight - 1µs`. |
| Item 3 fold-in into `_settings_sources.html` collides with 0.2.0.16 first-run walkthrough also extending the same tab. | LOW | Low | 0.2.0.16 hasn't shipped yet; this PR lands first per the 0.2.x sweep ordering. 0.2.0.16's plan/architect will rebase on this tab body. Plan 49 (`0.2.0.16`) is the rebase target. |
| Item 1+3 N+1 query risk if tab switcher pulls ALL helpers per tab visit. | LOW | Medium | `_ctx_for_tab` already routes per-tab — only the requested tab's helper runs. Confirmed by reading `src/ui/routes/settings.py:166` (`_ctx_for_tab(request, tab)` is per-call). |
| Item 1 `submission_artifacts` JSONB is nullable — Postgres `jsonb_extract_path_text(NULL, ...)` returns NULL, GROUP BY may swallow legit rows or surface phantom NULL bucket. | LOW | Low | Service helper adds `WHERE submission_artifacts IS NOT NULL AND submission_artifacts ? 'last_failure'` filter — same `is_not(None)` + JSON-contains pattern used by `list_failed_draft_applications` at `application_service.py:827`. |

## Manager BOOKKEEPING (post-merge)

Plan ships 3 of 6 rows. Manager handles the other 3 via direct ROADMAP edits + GH Issue closures (no PR):

| Row | Action | Rationale |
|---|---|---|
| 0.2.5.01 | `[ ] → [x]` deliverable note: `closed-as-moot — superseded by 0.2.0.13 \`Settings.scraper_rate_limits\` JSONB dial (PR #118 squash 19a3dbf). Single-dial "aggressiveness" knob replaced by per-source rpm/delay_lo/delay_hi tunables — strictly more expressive.` | The original DEF-11 spec ("rate-limit dial, default conservative") is functionally subsumed by the JSONB editor that already shipped. No code change needed. |
| 0.2.5.05 | MOVE to 0.2.7.06 — JSONB rate-limit editor UI for `Settings.scraper_rate_limits` (filed 2026-05-20 by plan 49 architect during 0.2.0.16 design). | Defer — UI editor isn't release-blocking for 0.2.5 closeout. Settings UI surface design pulls toward plan 49 (Sources tab refactor). Cleanest delivery is together. |
| 0.2.5.06 | MOVE to 0.2.7.07 — LinkedIn/Indeed keywords + location editor UI (filed 2026-05-20 by plan 49 architect during 0.2.0.16 design). | Defer — same reasoning as 0.2.5.05. These are scraper-config editor UIs that compose with 0.2.0.16's Sources tab refactor; engineer effort is much smaller when batched with that work. |

Use `.claude/naavik-ops task move 0.2.5.05 0.2.7.06` + `move 0.2.5.06 0.2.7.07` for the cross-release moves (per `AGENTS.md § Patch-version positions are not stable identifiers` — moves don't renumber siblings; positions 02/03/04 in 0.2.5 stay as-is even though .05/.06 vacate).

## Cross-cutting deliverables

- 3 new HTMX page partials OR sub-panels (Items 2 + 3 fold into existing tab bodies; Item 1 is a new tab body).
- 3 new service helpers (one each in `application_service.py`, `llm_tracker.py`, `job_service.py`) — single SELECT per dashboard, AsyncSession, no raw SQL.
- 1 new tab key in `_TAB_TEMPLATES` (`submissions`).
- Templates follow DaisyUI table + chip + cost-card patterns; Lucide icons stroke 1.5.
- ~280 LOC total source + ~130 LOC tests.

## Open questions

(none — specs are unambiguous; user pre-approved per 2026-05-20 directive)

## Approval checklist

(manager auto-approved per user directive 2026-05-20 — no PLAN_GATE pause)

## Deviations from plan

(engineer fills in pre-archive per `AGENTS.md § Workflow step 7`)
- **service-helper renamed `aggregate_submission_results` → `...** — what: service-helper renamed `aggregate_submission_results` → `aggregate_submission_failures` (matches plan-body). why: plan-body § Item 1 wrote the latter name + `list[dict]` return shape ("rows shaped {board, failure_kind, count, latest_at}"); dispatch-prompt restated an older sketch (`aggregate_submission_results -> dict[(board, failure_kind), count]`) that no longer matches plan-body. impact: callers import the new name; `list[dict]` is the only typed surface — plan-body intent preserved end-to-end' surface='none. surface: port.
- **added dedicated /settings/llm-provider route alongside ca...** — what: added dedicated /settings/llm-provider route alongside catch-all /settings/{tab}. why: Item 2 cost-cap widget needs Depends(get_session) to call llm_tracker.today_cost_usd; folding session into the catch-all broke 6+ existing tests in test_settings_llm_form.py + test_pages.py that hit /settings/llm-provider without overriding get_session. Mirrors existing /settings/sources dedicated-route pattern (plan 49). impact: catch-all + dedicated route co-exist; FastAPI ordering matches dedicated first. Plan § Item 2 LOC budget +1 stub route handler' surface='new HTTP route /settings/llm-provider (idempotent — same URL, same template, new dependencies). surface: none.
- **_ctx_for_tab grew three new view helpers** — what: _ctx_for_tab grew three new view helpers (_recent_scrape_runs_view, _submission_failures_view, _llm_cost_cap_view). why: plan called the helpers inline; extracted for parity with existing _build_sources_view pattern and to make each session=None fallback explicit. impact: no behavior change; ~12 LOC structural cleanup, no template impact' surface='none. surface: none.
- **cost-cap widget partial lives at components/_llm_cost_cap...** — what: cost-cap widget partial lives at components/_llm_cost_cap_widget.html (single-underscore prefix). why: plan said "new partial `_llm_cost_cap_widget.html`" without specifying directory; chose components/ per the established "reusable partials" convention from AGENTS.md § Frontend. impact: template include path is components/_llm_cost_cap_widget.html; no architectural drift' surface='none. surface: none.
- **test count is 26 (15 route** — what: test count is 26 (15 route + 11 service-helper) — exceeds plan estimate (~50+30+50=130 LOC test budget translates to roughly 13-15 tests). why: route-test autouse fixture conflicts with direct service-helper monkeypatch — split across two files keeps both pure; service-helper tests use compiled-SQL substring assertions which are tight + dialect-free. impact: test surface broader but tighter; no production cost' surface='none. surface: none.
- **test_settings_all_six_tabs renamed to test_settings_all_s...** — what: test_settings_all_six_tabs renamed to test_settings_all_seven_tabs. why: plan adds the submissions tab key, bumping settings tab count from 6 → 7. impact: test name update only; assertion loop now iterates 7 tabs; needed to stub today_cost_usd + aggregate_submission_failures + list_recent_scrape_runs to keep the test DB-free' surface='none. surface: none.
- **test_settings_llm_form.py gained autouse _patch_db_depend...** — what: test_settings_llm_form.py gained autouse _patch_db_dependencies fixture. why: dedicated /settings/llm-provider route now Depends(get_session); without this stub the module-scoped client would 500 on the LLM tab smoke tests. impact: test-only change; no production code touched. Override is teardown-clean per app.dependency_overrides.pop(...)' surface='none. surface: none.
- **test_pages.py:test_settings_llm_tab_renders_env_indicator...** — what: test_pages.py:test_settings_llm_tab_renders_env_indicators_not_api_key_input gained monkeypatch arg + per-test get_session override. why: same root cause as the LLM-form fixture above. impact: test-only' surface='none. surface: none.

