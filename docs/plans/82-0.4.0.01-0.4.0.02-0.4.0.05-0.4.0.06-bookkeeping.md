---
Status: APPROVED
Type: execution
Authored: 2026-05-21
Last updated: 2026-05-21
Depends on: docs/plans/76-81 (entire 0.4.0 cycle)
GitHub: #184 + #185 + #188 + #189
Owner: architect (dispatch 2026-05-21T07-54-17_362de279)
---

# 82 · 0.4.0.01 + 02 + 05 + 06 — Cycle-close bookkeeping

## Goal

Close out the 0.4.0 cycle by:

- Marking 0.4.0.01 (state model) + 0.4.0.02 (manual logger) as
  `[x]` close-as-shipped — both already shipped via Wave 5 / plan 56.
- Deferring 0.4.0.05 (Playwright form fill) + 0.4.0.06 (Google Sheets
  sync) to 0.8.0.NN per owner extended-autonomy directive (see
  `docs/design/research/0.4.0-AUDIT.md § B`).
- Updating Notes columns on 0.4.0.03/04/07 to reflect the refined
  scope shipped in plans 77/78/81.
- Bumping ROADMAP "Last updated" line with the 0.4.0 closeout summary.

## Why

The 7 original 0.4.0 rows were authored 2026-04-25, pre-Wave-5.
2 of them shipped during 0.1.0 Wave 5 + 0.2.x already. 2 of them
are correctly deferred per owner directive defer-to-0.8.0. The
remaining 3 require scope refinement that plans 77/78/81 ship.

This plan is the BOOKKEEPING closer — direct push to main per
PLAYBOOK § I (no code changes; only ROADMAP table edits).

## Proposal

Single commit; ROADMAP edits only. Direct push to main.

### D.1 Mark 0.4.0.01 + 0.4.0.02 as shipped

Edit ROADMAP § 0.4.0 table rows:

```
| 0.4.0.01 | Application multi-axis state model: ... | [x] | — | 4.1 |
  **CLOSED-AS-SHIPPED 2026-05-XX via 0.4.0 cycle closeout.**
  Substrate shipped via 0.1.0 Wave 5 / plan 10 (Application SQLModel
  with all 5 state axes + CHECK constraints + state-machine transitions
  in application_service). Canonical reference: `docs/design/DATA_MODEL.md § C
  Application` + `docs/design/DATA_MODEL.md § E state transitions`.
  Sponsorship-gate belt-and-suspenders shipped via 0.4.0.08 / plan 76. |

| 0.4.0.02 | Manual application logger ... | [x] | — | 4.2 |
  **CLOSED-AS-SHIPPED 2026-05-XX via 0.4.0 cycle closeout.**
  Shipped via 0.2.7.19 / plan 56 (POST /api/v1/applications/manual,
  CSRF-gated, /_modal/manual-job HTMX modal). Tracking +Add manually
  button on tracking.html:34-41 is the entry point. |
```

### D.2 Defer 0.4.0.05 + 0.4.0.06 to 0.8.0

Edit ROADMAP § 0.4.0 table rows:

```
| 0.4.0.05 | Playwright form filling ... | [x] | — | 4.5 |
  **DEFERRED-AS-MOOT 2026-05-XX per owner extended-autonomy directive.**
  Production Workday/LinkedIn/Indeed/Generic adapters tracked at
  `0.8.0.24-27`. Plan 63 / 0.2.7.10 shipped scaffolds (PR #164).
  Per-adapter implementation work is multi-week per adapter; out of
  scope for 0.4.0 cycle. Re-engage when explicitly scheduled. |

| 0.4.0.06 | Google Sheets sync ... | [x] | — | 4.6 |
  **DEFERRED-AS-MOOT 2026-05-XX per owner extended-autonomy directive.**
  Owner closed-as-moot the n8n historical-data import (`Backlog`
  section / 0.2.0.14) — Naavik used as fresh-start; no existing
  spreadsheet data to import. Sheets sync remains an option for
  multi-tenant cloud (0.8.0.NN) but isn't pulling its weight in
  the self-hosted single-user MVP. |
```

### D.3 Refine 0.4.0.03 + 0.4.0.04 + 0.4.0.07 row descriptions

Edit Notes columns on the 3 remaining-as-active rows to reflect what
plans 77/78/81 actually ship:

```
| 0.4.0.03 | Semi-auto flow ... | [x] | — | 4.3 |
  **SHIPPED via plan 77 (PR #XXX).** Apply-preview wiring (M1 /
  0.3.3.08a closed) + notification parity (manual submit → Discord/
  Telegram echo same as auto-apply). Foundation: `apply_preview_card.html`
  + `/_fragments/apply/{preview,confirm,cancel}/<id>` routes (plan 75).
  Default operator mode. |

| 0.4.0.04 | Auto-apply flow ... | [x] | — | 4.4 |
  **SHIPPED via plan 78 (PR #XXX).** Score-threshold belt-and-suspenders +
  adapter-confidence threshold consumption (graduated from 0.8.0.17) +
  per-board daily caps (new Settings.auto_apply_per_board_daily_caps
  JSONB col, Alembic 0017) + pause/resume (per-job + global drain) +
  dry-run mode (Settings.auto_apply_dry_run). Configurable threshold
  via Settings · Auto-apply tab. |

| 0.4.0.07 | Application analytics dashboard | [x] | — | 4.7 |
  **SHIPPED via plan 81 (PR #XXX).** 4 KPIs (Response/Onsite/Offer
  Rate · 90d + Funnel · 90d) computed in services/application_analytics.py
  + 3 breakdowns (by company, role-family, tag). uplot funnel chart
  + KPI strip + company table partials. New /tracking/analytics route +
  page. AppEvent history is the source of truth (max-status-reached per
  app over the window). |
```

### D.4 ROADMAP "Last updated" bump

```
> **Last updated:** 2026-05-XX. (**0.4.0 cycle SHIPPED.** 7 plans
> (76-82) shipped 14 commits across 12 engineer days; 13 new rows
> 0.4.0.08-20 closed; 5 rows close-as-shipped or close-as-deferred
> per audit memo. Headline: sponsorship-gate belt-and-suspenders at
> submission (plan 76) + apply-preview UI wiring + notification parity
> (plan 77) + auto-apply hardening cluster (plan 78: score-gate +
> confidence threshold + per-board caps + pause/resume + dry-run) +
> retry-failed-application (plan 79) + bulk actions on /tracking
> (plan 80) + application detail polish + postmortem UI + timeline
> + analytics dashboard (plan 81). New operational surfaces: alembic
> 0017 (Settings.auto_apply_per_board_daily_caps + auto_apply_dry_run
> JSONB), alembic 0018 (ClosedReason.USER_ARCHIVED enum value),
> new partials (postmortem_modal, tracking_bulk_action_bar,
> visa_status_chip, analytics_kpi_strip, analytics_funnel_card,
> analytics_company_table — 6 net partials, COMPONENTS.md 93 → 99).
> Test count: 2080 → ~2160 (+80 net). Foundation reuse: 70-80% of
> code reused from prior cycles (Wave 5 application_service, plan 75
> apply-preview routes, plan 73 score_history + uplot dep,
> plan 52 postmortem capture). **Next: 0.5.0 (Email Monitoring & Outreach).**
> Owner extended-autonomy directive honored — sponsorship-gate at
> submission enforces AGENTS.md § Owner Profile H1B rule.)
```

### D.5 Defer rows 0.4.0.18 + 0.4.0.19

Per audit memo § C, these were filed but never claimed by a plan:

```
| 0.4.0.18 | Cover letter regen + bullet selection override on
  /tracking edit | [x] | — | (audit memo 2026-05-21) |
  **DEFERRED-AS-MOOT 2026-05-XX per owner extended-autonomy + audit
  recommendation.** LOW priority polish; not blocking the 0.4.0
  cycle. Move to `0.8.0.NN` if scheduled. |

| 0.4.0.19 | Recruiter contact link surfacing on Application detail |
  [x] | — | (audit memo 2026-05-21) |
  **DEFERRED-AS-MOOT 2026-05-XX per owner extended-autonomy + audit
  recommendation.** Couples with Contact + ContactApplicationLink
  CRUD surfaces in 0.5.0 (outreach). Re-engage in plan 13. |
```

### Build sequence

1. Read latest ROADMAP § 0.4.0 (after 76-81 all merged + rows flipped
   `[~]` during their cycles).
2. Apply D.1-D.5 edits.
3. Direct-push to main via `chore(roadmap): close 0.4.0 cycle —
   shipped 7 plans, deferred 4 rows`.

### Risk + mitigation

| Risk | Likelihood | Impact | Mitigation |
|---|---|---|---|
| Direct push to main during active reviewer cycle | NONE | NONE | This plan only runs AFTER 76-81 all merged; per PLAYBOOK § I bookkeeping is direct-push allowed |
| ROADMAP row state mid-flight (some `[~]`) | LOW | LOW | Verify all 76-81 PRs merged before edit |
| GitHub Project board drift on close-as-shipped rows | LOW | LOW | Manager invokes `.claude/naavik-ops gh set-status <id> Done` per row during the closeout |

## Open questions

(empty)

## Approval checklist

(empty — pre-approved, per § I PLAYBOOK direct-push pattern)

## Test plan

- No code changes. No tests.
- Manual verify: `cat ROADMAP.md | grep "0.4.0"` shows all 0.4.0
  rows flipped `[x]` with deliverable notes.
- Bookkeeping commit pushed; `.claude/naavik-ops gh set-status` ran
  per row (manager bookkeeping per PLAYBOOK § I step 5).
